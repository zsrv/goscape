# rev-274 Port Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Port the 254→274 Engine-TS server delta onto the `rev-274` Go branch and re-baseline all parity gates (pack byte-parity, 16 unpack manifests, live client smoke) against the 274 pins.

**Architecture:** Spec `docs/superpowers/specs/2026-06-12-rev274-port-design.md` (commit `18ee4c0e`, user-approved). Five phases: Phase 0 infra (mostly DONE during spec session), Phase 1 engine core delta (wire renumber ×3 tables, tracking-packet deletion, login extended-revision read, MINIMAP_TOGGLE, 4 script ops + rename, skillLevel appearance, Inventory dirty-slot rewrite, NPC patrol/escape rework, AllowRepath deletion, FontType 256-glyph, CrcTable rolling hash), Phase 2 pkg/rsbuf re-audit (NPC jump bit), Phase 3 pack pipeline (gzip-baseline decision FIRST, CRCs, font renames, worldmap LUT) + byte parity + live smoke, Phase 4 unpack + manifest regen. Work list = `git -C Engine-TS diff 2e3bcf43..dee467c8` (188 files, +13,792/−3,694). All TS citations refer to the 274 pin `dee467c8` in `/home/owner/Code/github.com/LostCityRS/Engine-TS` unless tagged; rsbuf crate citations to `66911610` in `/home/owner/Code/github.com/2004scape/rsbuf` (cross-check only — the TS port at `src/network/rsbuf/` is authoritative).

**Tech Stack:** Go (pkg/io/protocol, pkg/io/packet, pkg/script, pkg/objtype, pkg/inventory, pkg/fonttype, pkg/cache, pkg/rsbuf, pkg/pack, pkg/packall, pkg/unpack, modules/{world,login,friends,ondemand}); upstream toolchain (**Node 24 + npm**, NOT bun — the 274 runtime swap) for reference generation only.

---

## Conventions (read first)

- **Go commands:** prefix every `go` invocation with `GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache`; `CGO_ENABLED=0` for build (`-race` tests need CGO_ENABLED=1, the default).
- **Commits:** `git commit --no-gpg-sign`, message suffix `[rev-274]`. NEVER `git add -A` (sandbox phantom dotfiles) — add files by name.
- **TS reads:** `git -C /home/owner/Code/github.com/LostCityRS/Engine-TS show dee467c8:<path> | cat -n` — verify every cited line number BEFORE writing code. The local Engine-TS working tree is on `254-GOSCAPE` — NEVER read it directly. Same rule for Content (`7f97b0a5`), Client-Java (`32f30626`), rsbuf (`66911610`, repo `/home/owner/Code/github.com/2004scape/rsbuf`, working tree on branch `225`).
- **Upstream reference runs:** `Server274-ref/engine` uses Node 24 (`node -v` → v24.x) + `npm install`/`npm run build`. There is NO bun and NO compiler jar at 274 — the compiler is the `@lostcityrs/runescript` 0.9.6 npm package (unchanged from 254).
- **Go test code:** write with Write/Edit tools, never `cat <<EOF` heredocs (sandbox mangles `!=`).
- **Tests pinning 254 behavior** that the 274 pin supersedes get UPDATED to the new contract, never preserved (hard rule #2).
- Run the **modules/world suite** in any task that changes a contract world tests exercise.
- Reference worktrees are read via `git show <pin>:…` or run in place, never edited; `Server274-ref` is build territory only for `npm run build` outputs.
- Plan line-number citations for *goscape* files were captured at branch point `c3b0ed7a` — re-anchor with a read before editing.
- **Implementer briefs:** FOREGROUND tests only (no backgrounded test runs), COMMIT YOURSELF (rev-254 lesson — 2 agents lost to this).

---

## Phase 0 — Infrastructure

### Task 1: REFERENCES.md §rev-274 on main + config repoint (worktrees + reference cache already DONE)

**Files:**
- Already created (spec session 2026-06-12): worktrees `~/Code/github.com/LostCityRS/Server274-ref/{engine,content,javaclient}` at the pins; reference cache `Server274-ref/engine/data/pack` (38 MB, build log `Server274-ref/build.log`, `.env` = `BUILD_STARTUP_UPDATE=false` + `BUILD_VERIFY=true`)
- Modify: `REFERENCES.md` (on `main` via temp worktree — main takes docs only)
- Modify: `config.yaml` (`world.content_path`)

- [ ] **Step 1: Verify the existing worktrees + cache** (idempotent re-check):

```bash
for d in engine content javaclient; do git -C ~/Code/github.com/LostCityRS/Server274-ref/$d log --oneline -1; done
ls ~/Code/github.com/LostCityRS/Server274-ref/engine/data/pack/main_file_cache.dat
ls ~/Code/github.com/LostCityRS/Server274-ref/engine/data/symbols/*.sym | wc -l
```

Expected: `dee467c8` / `7f97b0a53` / `32f3062`; the .dat exists; **33** .sym files (varbit.sym included — same census as 254). If the symbols dir is missing or count differs, investigate before proceeding (the 274 build may emit symbols elsewhere — find them; the Phase-3 symbols gate depends on this location).

- [ ] **Step 2: REFERENCES.md §rev-274 on main.** `git worktree add $TMPDIR/goscape-main main`, edit `$TMPDIR/goscape-main/REFERENCES.md`: append `## rev-274 — Go branch \`rev-274\`` after §rev-254 with the pin table (Engine-TS `dee467c868e694a2d5a931e3d19e580c83666cb2` branch `274`; Content `7f97b0a535a885bff9846631ca78438b6a731274`; Client-Java `32f30626156783de9f142306eb73a2243909dacf`; rsbuf `https://github.com/2004scape/rsbuf` branch `274` commit `669116109588ab5f5d9de8c24aace1d335da5399` — **cross-check only**, see note; RuneScriptTS row: `@lostcityrs/runescript 0.9.6` unchanged from §rev-254) plus notes: (1) Go branch cut from `rev-254` at `c3b0ed7a`; (2) work list = `git diff 2e3bcf43..dee467c8` (188 files, +13,792/−3,694, merge-base `93b6e557`); (3) **toolchain: bun → Node 24** (`node:zlib` 1.3.1 replaces bun/cloudflare-zlib `886098f3` as the gzip parity baseline — see the Phase-3 outcome for what gziputil tracks at 274); (4) **rsbuf crate + rsmod-pathfinder WASM deps DROPPED upstream** — both internalized into TS (`src/network/rsbuf/`, `src/engine/routefinder/`); the TS files are the authoritative reference from 274 on.
- [ ] **Step 3: Commit on main, remove temp worktree**

```bash
cd $TMPDIR/goscape-main && git add REFERENCES.md && git commit --no-gpg-sign -m "docs(references): rev-274 pins — Engine-TS dee467c8, Content 7f97b0a5, Client-Java 32f30626; bun→node toolchain note"
cd /home/owner/Code/github.com/zsrv/goscape && git worktree remove $TMPDIR/goscape-main
```

- [ ] **Step 4: config.yaml** — change `world.content_path` to `/home/owner/Code/github.com/LostCityRS/Server274-ref/content`. Check trackedness (`git ls-files config.yaml`); commit only if tracked: `chore(config): content path → Server274-ref [rev-274]`.

---

## Phase 1 — Engine core delta

### Task 2: ClientGameProt renumber — EVENT_TRACKING deleted

**Files:**
- Modify: `pkg/io/protocol/game/client/prot.go` (the `Opc*` const block + the `set(...)` size/category registry; DELETE the EVENT_TRACKING row)
- Modify: `pkg/io/protocol/game/client/prot_test.go` (pinned values)
- Modify: `modules/world/handlers_game.go` (drop any EVENT_TRACKING skip/registration row — grep `OpcEventTracking`)

- [ ] **Step 1: Read the TS table** `git show dee467c8:src/network/game/client/ClientGameProt.ts | cat -n` and cross-check the table below line by line (two independent transcriptions; on mismatch TS wins, note the correction).
- [ ] **Step 2: Update the const block + registry.** Every ID is reassigned; `EVENT_TRACKING` is DELETED at 274 (no replacement — the 4 discrete event packets survive, renumbered). No new client packets. Full 274 table (name = wire/size):

```text
NO_TIMEOUT=120/0             IDLE_TIMER=209/0
EVENT_MOUSE_CLICK=20/4       EVENT_MOUSE_MOVE=222/-1      EVENT_APPLET_FOCUS=73/1
EVENT_CAMERA_POSITION=53/4   (EVENT_TRACKING: DELETED)
ANTICHEAT_OPLOGIC1=219/4     ANTICHEAT_OPLOGIC2=201/2     ANTICHEAT_OPLOGIC3=41/4
ANTICHEAT_OPLOGIC4=80/1      ANTICHEAT_OPLOGIC5=235/1     ANTICHEAT_OPLOGIC6=250/2
ANTICHEAT_OPLOGIC7=25/4      ANTICHEAT_OPLOGIC8=0/1       ANTICHEAT_OPLOGIC9=24/3
ANTICHEAT_CYCLELOGIC1=12/-1  ANTICHEAT_CYCLELOGIC2=149/-1 ANTICHEAT_CYCLELOGIC3=52/1
ANTICHEAT_CYCLELOGIC4=230/1  ANTICHEAT_CYCLELOGIC5=100/0  ANTICHEAT_CYCLELOGIC6=188/1
ANTICHEAT_CYCLELOGIC7=89/0
OPOBJ1=247/6  OPOBJ2=169/6  OPOBJ3=108/6  OPOBJ4=62/6  OPOBJ5=117/6  OPOBJT=91/8  OPOBJU=39/12
OPNPC1=236/2  OPNPC2=233/2  OPNPC3=223/2  OPNPC4=147/2  OPNPC5=189/2  OPNPCT=181/4  OPNPCU=150/8
OPLOC1=215/6  OPLOC2=103/6  OPLOC3=187/6  OPLOC4=157/6  OPLOC5=127/6  OPLOCT=213/8  OPLOCU=60/12
OPPLAYER1=109/2  OPPLAYER2=166/2  OPPLAYER3=196/2  OPPLAYER4=98/2  OPPLAYER5=174/2
OPPLAYERT=240/4  OPPLAYERU=36/8
OPHELD1=185/6  OPHELD2=2/6  OPHELD3=123/6  OPHELD4=216/6  OPHELD5=42/6  OPHELDT=135/8  OPHELDU=136/12
INV_BUTTON1=74/6  INV_BUTTON2=82/6  INV_BUTTON3=239/6  INV_BUTTON4=179/6  INV_BUTTON5=46/6
IF_BUTTON=9/2  RESUME_PAUSEBUTTON=72/2  CLOSE_MODAL=51/0  RESUME_P_COUNTDIALOG=102/4
TUT_CLICKSIDE=94/1
MAP_BUILD_COMPLETE=214/0
MOVE_OPCLICK=138/-1  REPORT_ABUSE=137/10  MOVE_MINIMAPCLICK=86/-1  INV_BUTTOND=93/7
IGNORELIST_DEL=101/8  IGNORELIST_ADD=255/8  IDK_SAVEDESIGN=125/13  CHAT_SETMODE=154/3
MESSAGE_PRIVATE=139/-1  FRIENDLIST_DEL=106/8  FRIENDLIST_ADD=13/8  CLIENT_CHEAT=224/-1
MESSAGE_PUBLIC=253/-1  MOVE_GAMECLICK=207/-1
```

Categories are unchanged (the TS ctor's second/third args — verify while editing). Delete `OpcEventTracking` + its registry row; sweep `grep -rn "OpcEventTracking\|EventTracking" --include="*.go" pkg modules` — at 254 the row existed handler-less; at 274 it must be GONE (an unknown opcode from a 274 client is a protocol error, matching TS where the repository has no row). Update the header comment `(254 pin 2e3bcf43)` → `(274 pin dee467c8)`.

- [ ] **Step 3: Update `prot_test.go`** pins to the table (swap numbers, delete the EVENT_TRACKING row, update headers).
- [ ] **Step 4: Run** `go test ./pkg/io/protocol/game/client/... -count=1` → PASS, then full suite — any other failure means a test hardcodes an old wire byte: verify against TS, update.
- [ ] **Step 5: Commit** `feat(protocol): 274 client→server opcode renumber; EVENT_TRACKING deleted (TS ClientGameProt.ts @dee467c8) [rev-274]`

### Task 3: ServerGameProt + zone renumber + MINIMAP_TOGGLE row + tracking rows deleted + rsbuf zone fork

**Files:**
- Modify: `pkg/io/protocol/game/server/prot.go` (every `Op*` value; new `OpMinimapToggle`; DELETE `OpEnableTracking`/`OpFinishTracking`; `AllOps()` entries)
- Modify: `pkg/io/protocol/game/server/prot_test.go`
- Modify: `pkg/rsbuf/zone_encoders.go` (the zone-op value fork) + its cross-table pin test
- Modify: `modules/world/player.go` — delete `WriteEnableTracking`/`WriteFinishTracking` if they still exist (grep; at 254 they were kept only because the Op rows existed)

- [ ] **Step 1: Read the TS tables** `git show dee467c8:src/network/game/server/ServerGameProt.ts | cat -n` and `…ServerGameZoneProt.ts | cat -n`; cross-check the table below.
- [ ] **Step 2: Update every Op value in place** (keep var-block structure/doc comments; update citations to `(274)`). Sizes unchanged except where noted. Full 274 table (name = opcode/size):

```text
IF_OPENCHAT=166/2      IF_OPENMAIN_SIDE=158/4  IF_CLOSE=171/0         IF_SETTAB=215/3
IF_SETTAB_ACTIVE=241/1 IF_OPENMAIN=211/2       IF_OPENSIDE=16/2       IF_OPENOVERLAY=240/2
IF_SETCOLOUR=183/4     IF_SETHIDE=10/3         IF_SETOBJECT=28/6      IF_SETMODEL=129/4
IF_SETANIM=134/4       IF_SETPLAYERHEAD=192/2  IF_SETTEXT=44/-2       IF_SETNPCHEAD=142/4
IF_SETPOSITION=77/6    IF_SETSCROLLPOS=54/4
TUT_FLASH=90/1         TUT_OPEN=130/2
UPDATE_INV_STOP_TRANSMIT=227/2  UPDATE_INV_FULL=106/-2  UPDATE_INV_PARTIAL=172/-2
CAM_LOOKAT=233/6       CAM_SHAKE=64/4          CAM_MOVETO=200/6       CAM_RESET=101/0
NPC_INFO=197/-2        PLAYER_INFO=167/-2
(FINISH_TRACKING / ENABLE_TRACKING: DELETED)
FRIENDLIST_LOADED=185/1  MESSAGE_GAME=161/-1   UPDATE_IGNORELIST=3/-2
CHAT_FILTER_SETTINGS=114/3  MESSAGE_PRIVATE=235/-1  UPDATE_FRIENDLIST=247/9
UNSET_MAP_FLAG=115/0   UPDATE_RUNWEIGHT=67/2   HINT_ARROW=156/6      UPDATE_REBOOT_TIMER=89/2
UPDATE_STAT=105/6      UPDATE_RUNENERGY=83/1   RESET_ANIMS=47/0      UPDATE_PID=133/3
LAST_LOGIN_INFO=91/10  LOGOUT=88/0             P_COUNTDIALOG=210/0   SET_MULTIWAY=207/1
SET_PLAYER_OP=17/-1    MINIMAP_TOGGLE=194/1 (NEW)
REBUILD_NORMAL=231/4
VARP_SMALL=203/3       VARP_LARGE=245/6        RESET_CLIENT_VARCACHE=190/0
SYNTH_SOUND=34/5       MIDI_SONG=23/2          MIDI_JINGLE=15/4
UPDATE_ZONE_PARTIAL_FOLLOWS=32/2  UPDATE_ZONE_FULL_FOLLOWS=153/2  UPDATE_ZONE_PARTIAL_ENCLOSED=195/-2
zone: LOC_MERGE=176/14  LOC_ANIM=48/4  OBJ_DEL=52/3  OBJ_REVEAL=219/7  LOC_ADD_CHANGE=138/4
      MAP_PROJANIM=107/15  LOC_DEL=173/2  OBJ_COUNT=95/7  MAP_ANIM=85/6  OBJ_ADD=81/5
```

- [ ] **Step 3: Add the new wire op:**

```go
	// MINIMAP_TOGGLE sets the client minimap state. 1-byte payload:
	// p1(type) — 0 normal, 1 click-disabled, 2 blacked out. New in 274 —
	// TS MinimapToggleEncoder.ts @dee467c8, ServerGameProt.ts: 194/1.
	OpMinimapToggle = Op{Opcode: 194, PayloadSize: 1}
```

Add the `AllOps()` row. Delete `OpEnableTracking`/`OpFinishTracking` + their `AllOps()` rows + the `WriteEnableTracking`/`WriteFinishTracking` helpers (grep `EnableTracking\|FinishTracking` — zero non-test Go references must remain).

- [ ] **Step 4: pkg/rsbuf zone fork** — update the 10 zone-op constants in `pkg/rsbuf/zone_encoders.go` to the 274 values above. The cross-table pin test (rsbuf constants vs `gameserver.Op*`) must still compare AGAINST the gameserver table — run it; it is the proof the fork moved with the table.
- [ ] **Step 5: Update `prot_test.go`** pins (new values; MINIMAP_TOGGLE row added; tracking rows removed).
- [ ] **Step 6: Run** `go test ./pkg/io/protocol/... ./pkg/rsbuf/... -count=1` → PASS, then full suite; fix any test pinning old bytes (verify vs TS first).
- [ ] **Step 7: Commit** `feat(protocol): 274 server→client + zone opcode renumber; MINIMAP_TOGGLE 194/1; ENABLE/FINISH_TRACKING deleted; rsbuf zone fork follows (TS @dee467c8) [rev-274]`

### Task 4: Login extended-revision read (0xff + u2)

**Files:**
- Modify: the world login decoder reading the revision byte (grep `Expected` / `revision` in `modules/world/client.go` — the opcode-16/18 login-request parse path)
- Modify: `pkg/io/protocol/revision/revision.go` (`Expected = 274`) + its pin test
- Test: the login-flow test pinning the revision check

- [ ] **Step 1: Read the TS hunk** — `git show dee467c8:src/engine/World.ts | cat -n` around lines 2133-2141:

```ts
let rev = World.loginBuf.g1();
if (rev === 0xff) {
    rev = World.loginBuf.g2();
}
if (rev !== Environment.engine.revision) { … send([6]); close; }
```

274 > 255, so the 274 client transmits `0xff` then `p2(274)`. The single-byte path survives for hypothetical rev ≤ 254 clients (which then fail the equality check).

- [ ] **Step 2: Failing tests** — revision pin test wants 274 (cite TS WorldConfig.ts:91 `revision: 274` @dee467c8). Login-flow test: a login payload carrying bytes `ff 01 12` (0xff, u2 274) passes the revision gate; bytes `fe` (254) → reply `[6]` + close; bytes `ff 00 fe` (0xff, u2 254) → reply `[6]` + close. Run → FAIL.
- [ ] **Step 3: Implement** — in the login parse path:

```go
	// TS World.ts:2135-2138 @dee467c8: revision is g1; 0xff escapes to a
	// g2 extended read (274 does not fit in one byte).
	rev := int(buf.G1())
	if rev == 0xff {
		rev = int(buf.G2())
	}
	if rev != revision.Expected {
		// existing [6]-reply + close path
	}
```

Set `revision.Expected = 274` (keep the const's origin-story comment, append the 274 line).

- [ ] **Step 4: Infra-constant grep** (B6 lesson): `grep -rn "\b254\b" --include="*.go" pkg/io modules cmd | grep -v _test | grep -vi "rev-254\|TS \|254 pin"` — review every hit for a wire/infra constant that should now read 274; record the conclusion in the commit message. **Save-load array grep** (245.2 `f4334477` lesson): `grep -rn "make(" --include="*.go" modules/world/player_load.go modules/world/*save*.go` — confirm no array is sized from the SAVE where the registry can outgrow it across this boundary (SAV stays v7 at 274 — verify `git diff 2e3bcf43..dee467c8 -- src/engine/entity/PlayerLoading.ts` shows only the dotenv-import removal).
- [ ] **Step 5:** `go test ./pkg/io/... ./modules/world/... -count=1` → PASS. Commit `feat(world): 274 login extended-revision read (0xff+u2); wire revision 254→274 (TS World.ts:2135, WorldConfig.ts:91 @dee467c8) [rev-274]`

### Task 5: MINIMAP_TOGGLE emitter

**Files:**
- Modify: `modules/world/player_interface.go` (new `MinimapToggle` emitter beside the existing single-byte emitters)
- Modify: `pkg/script/active.go` (+interface method) + every fake the compiler flags (grep `SetPlayerOp(` across `*_test.go` for the fake list — same sweep set as the 254 SET_PLAYER_OP addition)
- Test: emitter wire test beside the existing single-byte emitter tests

- [ ] **Step 1: Read the TS** — `git show dee467c8:src/network/game/server/codec/MinimapToggleEncoder.ts | cat -n` and `…model/MinimapToggle.ts | cat -n` (payload: `p1(type)`).
- [ ] **Step 2: Failing test** — `MinimapToggle(2)` writes opcode 194 + byte `02`. Run → FAIL.
- [ ] **Step 3: Implement:**

```go
// MinimapToggle emits MINIMAP_TOGGLE (p1 type): 0 normal, 1 click-disabled,
// 2 blacked out. TS MinimapToggleEncoder.ts @dee467c8.
func (p *Player) MinimapToggle(minimapType int) {
	buf := packet.NewPacket(nil)
	buf.P1(uint8(minimapType))
	p.writeOut(gameserver.OpMinimapToggle, buf.Bytes())
}
```

(Match the file's actual emitter idiom — read a neighboring single-byte emitter first.) Add the ActivePlayer interface method + fake sweep.

- [ ] **Step 4:** `go test ./modules/world/... ./pkg/script/... -count=1` → PASS. Commit `feat(world): MINIMAP_TOGGLE emitter (TS MinimapToggleEncoder.ts @dee467c8) [rev-274]`

### Task 6: Script ops — MAP_LOC, MINIMAP_TOGGLE, SET_SKILL_LEVEL, NPC_DESTINATION, SETSKINCOLOUR→SETIDKCOLOUR; opcode-map pin regen; MAP_LOCADDUNSAFE rework

**Files:**
- Modify: `pkg/script/opcode.go` (4 inserts + 1 rename; the implicit-numbering shifts; String() cases)
- Modify: `pkg/script/opcode_map.go` (+4 entries, SETSKINCOLOUR→SETIDKCOLOUR), `pkg/script/opcode_pointers.go` (+4 rows, rename row)
- Modify: dispatch table + handler files: `pkg/script/handlers.go` (or wherever MAP_LOCADDUNSAFE / MIDI_SONG / STAT handlers live — grep `OpMapLocAddUnsafe\|OpMidiSong`), NPC ops file (grep `OpNpcInRange`)
- Modify: `pkg/script/active.go` (ActivePlayer gains `SetSkillLevel`; the MinimapToggle method landed in Task 5) + fakes; ActiveNpc side needs the waypoint accessor (grep the ActiveNpc interface for an existing waypoints/destination getter first)
- Modify: validators (DELETE `SkinColourValid`; grep `SkinColourValid` in `pkg/script`)
- Modify: `pkg/script/opcode_map_254_pin_test.go` → rename `opcode_map_274_pin_test.go`, full regen
- Test: handler tests beside the existing MAP_LOCADDUNSAFE / SETSKINCOLOUR tests

- [ ] **Step 1: Read the TS** — `git show dee467c8:src/engine/script/ScriptOpcode.ts | cat -n` (the WHOLE enum — do not trust the exploration agent's insertion points; the 4 inserts shift implicit ranges) and the handler hunks: `…ServerOps.ts | cat -n` lines 214-270 (MAP_LOCADDUNSAFE 9-zone rework + new MAP_LOC), `…PlayerOps.ts | cat -n` lines 855-865 (MINIMAP_TOGGLE), 1165-1180 (SET_SKILL_LEVEL + SETIDKCOLOUR), `…NpcOps.ts | cat -n` lines 570-585 (NPC_DESTINATION), and `git diff 2e3bcf43..dee467c8 -- src/engine/script/ScriptOpcodePointers.ts src/engine/script/ScriptValidators.ts`.
- [ ] **Step 2: Failing handler tests:**
  - MAP_LOCADDUNSAFE now scans the 3×3 zone neighborhood (offsets −8..0 over x and z per the TS loop — transcribe the exact loop bounds from the pin) and treats all active locs uniformly: a loc whose footprint crosses a zone boundary from the south-west IS found (this is the behavioral fix; build the fixture with a 2×2 loc straddling a zone edge).
  - MAP_LOC: coord covered by an active loc → pushes 1; uncovered → 0; uses the safe-iteration accessor (`getAllLocsSafe` analog — grep the Go zone API for the safe/unsafe iterator pair the existing handler uses).
  - MINIMAP_TOGGLE: pops type, records `MinimapToggle(type)` on the fake player; no active player → error.
  - SET_SKILL_LEVEL: pops level, sets the player's skillLevel field (Task 7 adds the field — write the test against the ActivePlayer method and let Task 7 land the world side; if ordering bites, land Tasks 6+7 in one commit).
  - NPC_DESTINATION: NPC with waypoints → pushes packed coord of `waypoints[0]` (NOTE: TS waypoints array semantics — verify whether index 0 is the NEXT or LAST waypoint in goscape's representation; the goscape waypoint stack may be reversed — port the BEHAVIOR: the next destination tile); without waypoints → pushes the NPC's current packed coord.
  - SETIDKCOLOUR: pops 2 ints (slot, colour); slot out of bounds [0, len(colors)) → range error per the TS validator; sets `colors[slot]`. The old single-arg SETSKINCOLOUR contract test is DELETED.
  Run → FAIL.
- [ ] **Step 3: Implement** — opcode inserts per the TS enum (regenerate the implicitly-numbered ranges by transcription, never hand-shift); map/pointers rows copied from TS at the pin; handlers following the neighboring handlers' idiom; delete `SkinColourValid`.
- [ ] **Step 4: Pin regen** — `git mv pkg/script/opcode_map_254_pin_test.go pkg/script/opcode_map_274_pin_test.go`; regenerate the whole table from the TS enum at `dee467c8` (header: "Generated from TS ScriptOpcode.ts at pin dee467c8"). Entry count = 254's count +4 (`grep -c '": ' …` before/after; SETSKINCOLOUR renames in place, net 0).
- [ ] **Step 5: Old-value grep** — numeric-literal grep over the shifted opcode values (read the actual shifted range from the TS enum): every non-test hit must route through named constants.
- [ ] **Step 6:** `go test ./pkg/script/... ./pkg/pack/... ./modules/world/... -count=1` → PASS (pack tests prove zero compiler fallout — the compiler reads ScriptOpcodeMap dynamically). Commit `feat(script): MAP_LOC, MINIMAP_TOGGLE, SET_SKILL_LEVEL, NPC_DESTINATION ops; SETSKINCOLOUR→SETIDKCOLOUR(slot,colour); MAP_LOCADDUNSAFE 9-zone rework; opcode-map pin regen (TS @dee467c8) [rev-274]`

### Task 7: Player skillLevel field + appearance p2

**Files:**
- Modify: `modules/world/player.go` (new `skillLevel int` field, zero default)
- Modify: `modules/world/appearance.go` (the appearance stream write after combatLevel)
- Modify: `pkg/script/active.go` ActivePlayer `SetSkillLevel(int)` + fakes (if not landed with Task 6)
- Test: the appearance-bytes pin test (grep `combatLevel` in `modules/world/*_test.go`)

- [ ] **Step 1: Read the TS** — `git show dee467c8:src/engine/entity/Player.ts | cat -n` line ~317 (field) and ~1422 (the appearance serializer: `p1(combatLevel)` then `p2(skillLevel)` — read the surrounding function to confirm exact order and that nothing else moved).
- [ ] **Step 2: Failing test** — the appearance-blob pin test gains 2 bytes: `00 00` (skillLevel 0) after the combat-level byte; a fixture with skillLevel=1500 emits `05 DC`. Run → FAIL.
- [ ] **Step 3: Implement** field + write + `SetSkillLevel` setter (marks appearance dirty the way other appearance mutators do — read a neighbor like the gender setter for the rebuild flag idiom).
- [ ] **Step 4:** `go test ./modules/world/... -count=1` → PASS (rsbuf consumes the appearance blob opaquely — no Phase-2 coupling). Commit `feat(world): player skillLevel + appearance p2 after combat level (TS Player.ts:317,1422 @dee467c8) [rev-274]`

### Task 8: Inventory rewrite — dirty-slot tracking, transaction machinery deleted

**Files:**
- Modify: `pkg/inventory/inventory.go` (the container: delete the transaction type + validation params; add dirty-slot tracking)
- Modify: every call site the compile break enumerates (expect: `modules/world/player_script.go` invAdd/invDel, `modules/world/server_invs.go` stock loops, `pkg/script` inv handlers, `modules/world/player_inv_cheat.go`, trade/bank paths)
- Modify: `modules/world/inv_update.go` (the per-tick inv sync: full vs partial fork + `resetTracking`)
- Test: `pkg/inventory/inventory_test.go` (rewrite to the new contract), `modules/world/inv_update_test.go`

- [ ] **Step 1: Read the TS whole** — `git show dee467c8:src/engine/Inventory.ts | cat -n` (the complete 274 file, not the diff — this is a rewrite-class change; port the file, don't patch). Then the consumer hunks: `git diff 2e3bcf43..dee467c8 -- src/engine/entity/NetworkPlayer.ts` (lines 339-383: `needsFullUpdate = listener.firstSeen`; full update on firstSeen; else if `inv.update` → `UpdateInvPartial(com, inv, ...inv.getDirtySlots())`; player-inv variant sets `firstSeen = true` on full update so weight still sends post-login; weight flag = `inv.update || needsFullUpdate`), `git diff 2e3bcf43..dee467c8 -- src/engine/World.ts` (resetTracking at lines ~1140/1151; 3-param add/remove in the shop-stock loops ~1167-1186).
- [ ] **Step 2: Failing tests first** — rewrite `pkg/inventory` tests to the 274 contract:
  - `Add(id, count, beginSlot)` returns the COMPLETED count (partial fill when the container runs out of space — no all-or-nothing validation, no transaction object); each touched slot is dirty.
  - `Remove(id, count, beginSlot)` returns the removed count; partial removal when fewer present; touched slots dirty.
  - `GetDirtySlots()` returns the touched slot indices SORTED; `ResetTracking()` clears both the update flag and the set.
  - `Set`/`Delete` mark dirty; `RemoveAll` marks each previously-occupied slot dirty (loop, not fill).
  - DELETED API: the transaction type, `Contains`, `Swap`/`Shift`/`Transfer` IF the TS deletion list applies (verify each against the 274 file — TS deleted `contains`, `occupiedSlotCount`, `isFull/isEmpty/hasAny/hasSpace`, `swap`, `shift`, `transfer`; goscape deletes its analogs ONLY where goscape callers don't depend on them for ported-elsewhere behavior — any survivor gets a `// Go-extension:` comment + tracker row).
  - `modules/world/inv_update_test.go`: firstSeen listener → UPDATE_INV_FULL; dirty-slot change on a seen listener → UPDATE_INV_PARTIAL containing exactly the dirty slots; the partial encoder's per-slot wire format verified against `git show dee467c8:src/network/game/server/codec/UpdateInvPartialEncoder.ts` (slot id width, obj id+count encoding — transcribe, don't assume the 254 layout).
  Run → FAIL.
- [ ] **Step 3: Implement** the container rewrite, then chase the compile cascade through every call site, translating each TS consumer hunk (`invAdd`/`invDel` in Player drop the validation unpacking — TS Player.ts:1543/1573; shop restock/desupply loops go 3-param — World.ts:1167-1186; cheat handler's `invAdd(…, false)` sites simplify). **The TS semantic change is real:** partial adds now succeed where 254 refused them (`assureFullInsertion` died) — any goscape caller that branched on the transaction's success/failure must be re-read against its TS 274 counterpart, not mechanically adapted.
- [ ] **Step 4: Per-tick sync** — `modules/world/inv_update.go`: port the full/partial fork + `ResetTracking()` in the world cleanup pass (TS World.ts:1140,1151).
- [ ] **Step 5:** `go test ./pkg/inventory/... ./modules/world/... ./pkg/script/... -count=1` → PASS; `-race` on modules/world. Full suite. Commit `feat(inv): 274 Inventory rewrite — dirty-slot tracking + partial updates; transaction machinery deleted (TS Inventory.ts, NetworkPlayer.ts:339-383, World.ts @dee467c8) [rev-274]`

### Task 9: NPC patrol countdown + stuckCounter + escape-mode rewrite

**Files:**
- Modify: `modules/world/npc.go` (fields at lines ~109-112: `wanderCounter`→`stuckCounter`, `nextPatrolTick`+`delayedPatrol`→`patrolDelayTicksRemaining int` default −1)
- Modify: the patrol-mode + escape-mode functions (grep `patrol` in `modules/world/npc_player_modes.go`, `modules/world/npc_ai.go`, `modules/world/npc_interaction.go` — find the Go analogs of TS `patrolMode()` and `playerEscapeMode()`)
- Test: the patrol/wander tests (grep `TestTeleportHomeAfterStuck\|patrol` in modules/world tests)

- [ ] **Step 1: Read the TS** — `git show dee467c8:src/engine/entity/Npc.ts | cat -n` lines 700-850 (the whole mode block) AND the raw diff `git diff 2e3bcf43..dee467c8 -- src/engine/entity/Npc.ts` (rev-254 lesson: read handler diffs yourself). Contracts:
  - Idle stuck: `if (this.stuckCounter++ > 500)` → teleport to startCoord, reset 0 (increment-then-compare: the 501st no-move tick fires — verify against goscape's current wanderCounter comparison direction).
  - `stuckCounter = 0` on successful movement (TS line ~368) and when aimode runs (~723).
  - patrolMode: early-return on empty patrolPoints; `stuckCounter` increments every patrol tick; force-teleport when `stuckCounter >= 32` OR `level !== dest.level`; arrival starts `patrolDelayTicksRemaining = type.patrolDelay[nextPatrolPoint]` when < 0, decrements per tick, advances the patrol point when ≤ 0; `updateMovement()` moved to the END of the function.
  - playerEscapeMode rewritten: direction deltas; `maxrange` from NpcType; diagonal `canTravel(dx,dz)` first; else axis-preference fallback (primary horizontal, secondary vertical per the TS direction split — transcribe the exact preference table); `stuckCounter++` when `updateMovement()` returns false; reset-to-defaults when stuck ≥ 5 AND not at max-range in both axes.
- [ ] **Step 2: Failing tests** — patrol fixture: NPC reaching a patrol point with delay 3 waits exactly 3 ticks then advances; NPC blocked 32 ticks en route teleports to the patrol dest; cross-level patrol point teleports immediately. Stuck fixture: the existing teleport-home-after-stuck test updated to the new counter semantics (verify threshold off-by-one against TS, then pin). Escape fixture: NPC fleeing a player with the diagonal blocked steps along the preferred axis; fully-boxed NPC resets mode after 5 stuck ticks. Run → FAIL.
- [ ] **Step 3: Implement** field + both mode rewrites. Then `grep -rn "wanderCounter\|nextPatrolTick\|delayedPatrol" --include="*.go" modules pkg` → zero hits.
- [ ] **Step 4:** `go test ./modules/world/... -count=1` → PASS; this area is RNG-adjacent — run the touched tests with `-count=20` (Arc-30 flaky-masking lesson) and capture the real exit code. Commit `feat(world): 274 NPC patrol countdown + stuckCounter + escape-mode per-axis rewrite (TS Npc.ts:700-850 @dee467c8) [rev-274]`

### Task 10: AllowRepath deletion + MoveClick simplification (#100)

**Files:**
- Modify: `modules/world/movement_consts.go` (delete the AllowRepath enum block, ~lines 50-66)
- Modify: `modules/world/movement.go` (delete `setAllowRepath` + its calls in queueWaypoint/queueWaypoints, ~lines 17-49)
- Modify: `modules/world/handlers_game.go` (MoveClick: delete the click-own-tile special case + the `setAllowRepath(AllowRepathNone)` call, ~lines 335-345)
- Modify: `modules/world/interaction.go` (~line 1002: the naive-repath guard drops the AllowRepath condition) + `modules/world/npc_ai.go`/`npc_interaction.go`/`player.go` (remaining writes — compile errors enumerate)
- Test: movement/pathing tests pinning the old click-own-tile or repath-gate behavior

- [ ] **Step 1: Read the TS** — `git diff 2e3bcf43..dee467c8 -- src/engine/entity/AllowRepath.ts src/engine/entity/PathingEntity.ts src/engine/entity/Player.ts src/network/game/client/handler/MoveClickHandler.ts` (the file deletion + every consumer hunk; this is upstream #100, the 274 tip commit). Player.ts:1077 becomes `if (this.isLastWaypoint())` (unconditional naive repath at the last waypoint); MoveClickHandler's clientRoutefinder branch always rebuilds `userPath` from the message and queues it (no own-tile special case); PathingEntity loses the field, the setter, and the SCRIPT-interaction set-site.
- [ ] **Step 2: Failing tests** — clicking the player's own tile now queues the (single-tile) waypoint path like any other click (update the test pinning the old special case); the naive-repath gate test drops the AllowRepath precondition. Run → FAIL.
- [ ] **Step 3: Implement** the deletions; `grep -rn "AllowRepath\|allowRepath" --include="*.go" modules pkg` → zero hits.
- [ ] **Step 4:** `go test ./modules/world/... -count=1` → PASS; full suite. Commit `fix(world): delete AllowRepath — unconditional last-waypoint repath + MoveClick simplification (upstream #100; TS @dee467c8) [rev-274]`

### Task 11: exactMove early teleport; IF_SETANIM −1; IF_SETTEXT colour persistence; STRONGQUEUE popInts

**Files:**
- Modify: the exactMove implementation (grep `exactMove\|ExactMove` in `modules/world/player_masks.go` / `modules/world/masks.go`)
- Modify: `pkg/script/handlers.go` IF_SETANIM + IF_SETTEXT handlers; the STRONGQUEUE handler (grep `OpStrongQueue`)
- Test: the masks test pinning exactMove positioning; the if-handler tests

- [ ] **Step 1: Read the TS** — `git diff 2e3bcf43..dee467c8 -- src/engine/entity/Player.ts` (the exactMove hunk at ~2105: `this.teleport(endX, endZ, this.level)` BEFORE setting the exactMove mask fields; the old inline x/z + tele-flag + lastStep adjustments deleted — read the full old and new function bodies and port the NEW one) and `git diff 2e3bcf43..dee467c8 -- src/engine/script/handlers/PlayerOps.ts` (three hunks: IF_SETANIM drops the `seq === -1` early-return — −1 now transmits and clears the anim; IF_SETTEXT gains the multi-line colour-persistence transform — split on `\n`, track `savedCol` from `@xxx@` codes, re-apply to continuation lines without their own leading code, the `@str@`→`@bla@` special case — transcribe the EXACT loop, this is fiddly string logic; STRONGQUEUE arg-pop becomes `popInts(3)` — check whether goscape's handler pops in the same order, the swap can reorder side effects).
- [ ] **Step 2: Failing tests** — exactMove fixture asserts the player's x/z equal the END tile immediately after the call (and the mask fields still carry start/end/delays); IF_SETANIM(−1) writes the packet with seq 0xFFFF instead of returning early; IF_SETTEXT with `"@red@line one\nline two"` emits `"@red@line one\n@red@line two"` (plus the `@str@` case from the TS code); STRONGQUEUE behavior unchanged observably (the popInts refactor is internal — assert the existing contract still passes). Run → FAIL (first three).
- [ ] **Step 3: Implement**; `go test ./pkg/script/... ./modules/world/... -count=1` → PASS. Commit `feat(script,world): exactMove early teleport; IF_SETANIM -1 passthrough; IF_SETTEXT colour persistence; STRONGQUEUE popInts (TS Player.ts:2105, PlayerOps.ts @dee467c8) [rev-274]`

### Task 12: Hunt `&` comparator

**Files:**
- Modify: `pkg/objtype/hunttype.go` (the check-condition evaluator)
- Test: `pkg/objtype/hunttype_test.go`

- [ ] **Step 1: Read the TS** — `git show dee467c8:src/cache/config/HuntType.ts | cat -n` lines 65-80: new case `'&'` → `(value & checkValue) === 0` (true when NO bits in common — transcribe the comparison direction EXACTLY; it is the inverse of the intuitive "has bits").
- [ ] **Step 2: Failing test** — `&` condition with value 0b0101 vs check 0b1010 → true; vs 0b0100 → false. Run → FAIL.
- [ ] **Step 3: Implement** + verify the hunt PACK side accepts `&` (grep `pkg/pack/hunt.go` for the comparator whitelist — if the packer validates operators, add `&` there in the same commit, citing `git diff 2e3bcf43..dee467c8 -- tools/pack/config/HuntConfig.ts`).
- [ ] **Step 4:** `go test ./pkg/objtype/... ./pkg/pack/... -count=1` → PASS. Commit `feat(objtype): hunt '&' no-common-bits comparator (TS HuntType.ts @dee467c8) [rev-274]`

### Task 13: FontType 256-glyph rework + *_full font names (runtime side)

**Files:**
- Modify: `pkg/fonttype/fonttype.go` (glyph arrays 94→256, CHAR_LOOKUP/drawWidth deletion, load-name changes, quill space-advance, height guard, splitText colour persistence)
- Modify: runtime consumers (grep `fonttype.` across modules — chat/message line-split users)
- Test: `pkg/fonttype` tests

- [ ] **Step 1: Read the TS whole** — `git show dee467c8:src/cache/config/FontType.ts | cat -n` (rewrite-class — port the file, not the diff). Key contracts: fonts load by name `p11_full/p12_full/b12_full/q8_full` with a `quill` flag (true only for q8_full); 256-glyph arrays; charAdvance indexed directly by char code; height trusted only from glyphs < 128; space advance copies glyph 73 (I) for quill else 105 (i); stringWidth uses charAdvance directly; split/wrap logic carries `savedCol` colour codes across breaks (same transform family as Task 11's IF_SETTEXT — but a SEPARATE implementation site; do not share code unless TS does).
- [ ] **Step 2: Failing tests** — decode a fixture font blob (build from the 274 reference cache's actual `p11_full` jagfile entry if the existing tests use real-cache fixtures — check how current fonttype tests source their data; a 254-cache fixture will FAIL to find `p11` under the new names, which is itself the red test); stringWidth over a >127 char-code string; split with colour carry. Run → FAIL.
- [ ] **Step 3: Implement**; sweep consumers for the name change (`grep -rn '"p11"\|"p12"\|"b12"\|"q8"' --include="*.go" pkg modules` → every hit becomes `_full` or is justified).
- [ ] **Step 4:** `go test ./pkg/fonttype/... ./modules/world/... -count=1` → PASS (real-cache fixture tests reading the 254 cache may fail on the renamed entries — defer with `t.Skip("awaiting 274 cache — plan Task 22")` + tracker note; do NOT weaken assertions). Commit `feat(fonttype): 274 256-glyph fonts, *_full names, colour-persistent split (TS FontType.ts @dee467c8) [rev-274]`

### Task 14: CrcTable rolling hash (ondemand /crc surface)

**Files:**
- Modify: `pkg/cache/crctable.go` (buffer 36→40 bytes; exported per-archive table; the rolling hash)
- Modify: `modules/ondemand/handler.go` if it post-processes the table (read it — it may serve the prebuilt bytes opaquely)
- Test: `pkg/cache/crctable_test.go`

- [ ] **Step 1: Read the TS** — `git show dee467c8:src/cache/CrcTable.ts | cat -n` (whole file, ~40 lines): 9 group CRCs written p4 each, then `hash = 1234; for i in 0..8: hash = (hash << 1) + CrcTable[i]` (JS 32-bit overflow semantics — Go: `hash = hash<<1 + crc[i]` on `int32`, letting natural wraparound match the JS `|0` behavior; CHECK whether TS applies `|0` or stays in float — read the exact source and match bit-for-bit, this is the risk point), `p4(hash)`, and the final whole-buffer CRC computed over `length − 4`.
- [ ] **Step 2: Failing test** — build the table from the 274 reference cache and byte-compare against the reference engine's actual crc response: extract the expected bytes by running the upstream web /crc route OR by hand-computing from `git show dee467c8:src/cache/CrcTable.ts` semantics over the reference cache's group CRCs (prefer the hand-computed fixture: 9 known CRCs in, 40 known bytes out). Run → FAIL.
- [ ] **Step 3: Implement**; verify the ondemand module serves the new 40-byte body (its handler test pins the response — update).
- [ ] **Step 4:** `go test ./pkg/cache/... ./modules/ondemand/... -count=1` → PASS. Commit `feat(cache): 274 CRC table — 40-byte buffer with rolling hash (TS CrcTable.ts @dee467c8) [rev-274]`

### Task 15: World perf gates + GameMap active-loc gate + max-players hardcode

**Files:**
- Modify: `modules/world/tick.go` (hunt pass + info pass gates)
- Modify: the static-loc zone-registration site (grep `AddStaticLoc\|addStaticLoc` in modules/world — the map-load loc loop)
- Modify: `modules/world/config.go` / `modules/world/server.go` (max-players: TS hardcodes 2047 — if goscape has a config knob, pin it to 2047 and mark the knob deprecated, mirroring TS dropping the env var)
- Test: world tests touching hunt/info ticks; a loc-registration test

- [ ] **Step 1: Read the TS** — `git diff 2e3bcf43..dee467c8 -- src/engine/World.ts` hunks at 573-590 (`if (this.getTotalPlayers() > 0)` around the npc-hunt pass), 979-981 (info-processing early return when 0 players), 118 (`NODE_MAX_PLAYERS` → hardcoded 2047); `git diff 2e3bcf43..dee467c8 -- src/engine/GameMap.ts` lines 281-286 (static loc added to the ZONE only when `type.active === 1`; collision flags added regardless — check what goscape's loader does today; the 254 behavior added all locs).
- [ ] **Step 2: Failing tests** — a zone-registration test: an inactive-typed loc (active=0 fixture) contributes collision but is absent from the zone's loc list; hunt-pass test: with zero players online the hunt pass does not run (observable via a hunt-trigger fixture). Run → FAIL.
- [ ] **Step 3: Implement** the three gates. NOTE the GameMap zip-first map loading (`.cache/maps-server.zip`) is NOT-PORTED build-infra (goscape loads its own pack layout) — record the disposition for the Task-29 audit, port only the active-gate.
- [ ] **Step 4:** `go test ./modules/world/... -count=1` → PASS. Commit `feat(world): 274 perf gates (hunt/info skip at 0 players), zone active-loc gate, max-players 2047 (TS World.ts, GameMap.ts @dee467c8) [rev-274]`

### Phase 1 exit gate

- [ ] `CGO_ENABLED=0 go build -trimpath ./... && go vet ./...`
- [ ] `go test ./... -count=1` (capture the real exit code)
- [ ] `go test -race ./pkg/io/... ./pkg/script/... ./pkg/objtype/... ./pkg/inventory/... ./pkg/fonttype/... ./pkg/cache/... ./modules/... -count=1`

---

## Phase 2 — pkg/rsbuf re-audit

### Task 16: NPC jump bit (info add-leaf 36→37 bits)

**Files:**
- Modify: `pkg/rsbuf/npc.go` (or wherever the rsbuf-side Npc struct lives — grep `type Npc struct` in pkg/rsbuf: new `Jump bool` field + ctor init + cleanup reset)
- Modify: `pkg/rsbuf/npcinfo.go` (the add-leaf encoder gains `PBit(1, jump)` between the dz 5-bit write and the extend bit; the `npcBitsAdd` constant/comment 36→37)
- Modify: `pkg/rsbuf/buf.go` ComputeNpc signature (+`jump bool` param) + the modules/world caller (grep `ComputeNpc(` — World threads `npc.jump`; goscape's world-side Npc needs the jump source — check what sets it: `git grep -n 'jump' dee467c8 -- src/engine/entity/Npc.ts src/engine/entity/PathingEntity.ts` to find the TS write sites (teleport/jump semantics) and mirror onto goscape's npc teleport path)
- Test: the npcinfo wire pin test

- [ ] **Step 1: Verify both references** — crate: `git -C /home/owner/Code/github.com/2004scape/rsbuf diff origin/254..origin/274 --stat` (expect 8 files, +14/−7) then `git -C … show 66911610:src/info.rs | cat -n` lines 520-555 (add() gains the jump param; `pbit(1, jump)` before extend). TS: `git show dee467c8:src/network/rsbuf/info.ts | cat -n` (the NpcInfoEncoder add method — same layout). Confirm NOTHING else changed in the crate delta (player paths, capacities, prot tables — the spec records them verified-unchanged; re-verify with the diff).
- [ ] **Step 2: Failing pin test** — extend the npcinfo wire test: an add-block for an NPC with jump=true carries bit layout `14(nid) 11(ntype) 5(dx) 5(dz) 1(jump=1) 1(extend)`; jump=false emits the 0 bit (the 254 layout had NO bit there — existing pin tests get UPDATED, hard rule #2). Hand-compute the expected bytes. Run → FAIL.
- [ ] **Step 3: Implement** struct + encoder + signature + the world-side jump source (TS: find where `npc.jump = true` is set — likely the teleport/teleJump path mirroring the player's jump flag; verify and cite). `grep -rn "BITS_ADD\|npcBitsAdd" pkg/rsbuf` comments updated.
- [ ] **Step 4:** `go test ./pkg/rsbuf/... ./modules/world/... -count=1` → PASS; `-race` both. Commit `feat(rsbuf): 274 NPC jump bit in info add-leaf (rsbuf 66911610 info.rs, TS rsbuf/info.ts @dee467c8) [rev-274]`

### Phase 2 exit gate

- [ ] `go test ./... -count=1` + `-race` on `pkg/rsbuf/... modules/world/...`.

---

## Phase 3 — Pack pipeline + byte parity + live smoke

### Task 17: Gzip baseline decision (FIRST — gates everything)

**Files:** experiment in `$TMPDIR`; outcome decides whether `pkg/io/gziputil` gains a stock-zlib mode.

- [ ] **Step 1: The experiment** — extract every gzip member from the 274 reference cache (the gziputil corpus harness already walks cache gzip members — read `pkg/io/gziputil`'s corpus test for the walker) and for each: decompress, re-compress with the current (cloudflare) gziputil, byte-compare. Run via the existing corpus gate pointed at the 274 ref:

```bash
GOSCAPE_REF274_DIR=~/Code/github.com/LostCityRS/Server274-ref/engine \
  GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./pkg/io/gziputil/ -run RefCorpus -count=1 -v
```

(This needs the Task-19 env rename first IF the harness hardcodes REF254 — if so, do the rename for gziputil in THIS task and note it.) Also record `node -p process.versions.zlib` (1.3.1) and the TS call sites: `GZip.ts`/`Packet.ts` use `zlib.gzipSync` with DEFAULT options + `byte[9]=0` OS-field clear (verify at the pin: `git show dee467c8:src/io/GZip.ts`, `…Packet.ts | cat -n` line ~197).

- [ ] **Step 2: Decision fork** —
  - **Bit-equal (unlikely):** document in the tracker, done.
  - **Mismatch (expected):** port stock zlib 1.3.1's level-6 deflate into gziputil as the 274 mode, following the rev-244 B6 cloudflare port playbook: pin the zlib source (`https://github.com/madler/zlib` tag `v1.3.1`), port `deflate.c`'s level-6 path (longest_match, fill_window, the static/dynamic tree emit) bit-exactly, corpus-verify against ALL reference-cache members. Keep the cloudflare implementation selectable (rev≤254 branches use it; the gziputil API grows a mode/constructor, default per-branch). THIS IS THE LARGEST SINGLE WORK ITEM IF IT FIRES — treat it as its own sub-arc with RED corpus → port → GREEN corpus; the corpus gate is the only acceptance test that matters.
  - **Mismatch that resists a deterministic port** (e.g. node uses non-zlib internals — it doesn't, but verify): STOP and escalate to the user with the parity-definition amendment options from the spec (DoD (c) caveat).
- [ ] **Step 3:** Commit(s): experiment conclusion recorded in the tracker + (if fired) the port with its own commit trail. `test(gziputil): 274 gzip baseline — node/zlib-1.3.1 corpus {outcome} [rev-274]`

### Task 18: Pack CRC bumps + font renames + worldmap LUT + map/PackFile behavioral hunks

**Files:**
- Modify: `pkg/pack/pack_configs.go` (CRC const block + header)
- Modify: `pkg/pack/clientinterface/pack.go` (interface CRC; the nameToFont table → `*_full`)
- Modify: `pkg/pack/audio/sound.go` (sound CRC)
- Modify: `pkg/pack/compiler/typeinfo.go` (fontmetrics names → `*_full`; grep `"p11"` across pkg/pack for every font-name site: `clientinterface/names.go`, title/sprite packers)
- Modify: `pkg/pack/worldmap/refcolors.go` (regenerate the LUT from TS)
- Modify: the maps packer + PackFile-analog sites for OUTPUT-AFFECTING hunks only
- Modify: `pkg/pack/pack_configs_test.go` + buildverify pins
- Test: per-area tests

- [ ] **Step 1: Verify every CRC from TS** (do not trust this table without reading `git show dee467c8:tools/pack/config/PackShared.ts` checkcrc sites + `…interface/PackClient.ts` + `…sound/pack.ts`):

```go
	clientConfigCRCSeq      int32 = -753410077  // was -716271600
	clientConfigCRCLoc      int32 = 452815002   // was -826309209
	clientConfigCRCFlo      int32 = 960212554   // was -1566957964
	clientConfigCRCSpotAnim int32 = -1587698939 // was -555849646
	clientConfigCRCNpc      int32 = -1249602232 // was 1077655221
	clientConfigCRCObj      int32 = 128627047   // was 535204494
	clientConfigCRCIdk      int32 = -359342366  // unchanged at 274
	clientConfigCRCVarp     int32 = 703279713   // was 1039564548
	clientConfigCRCVarbit   int32 = -234977015  // was -1387031023
```

interface CRC → `2041671134` (was `1728499832`); sound CRC → `2127412105` (was `831919863`).

- [ ] **Step 2: Font renames** — `grep -rn '"p11"\|"p12"\|"b12"\|"q8"' --include="*.go" pkg/pack` → every site to `*_full` per `git diff 2e3bcf43..dee467c8 -- tools/pack/Compiler.ts tools/pack/interface/PackShared.ts tools/pack/sprite/title.ts tools/pack/graphics/pack.ts` (read each hunk; title/sprite may rename ASSET lookups too).
- [ ] **Step 3: Worldmap LUT** — regenerate `refcolors.go` from `git show dee467c8:tools/pack/map/Worldmap.ts` (78 changed pairs + 2 new entries: slayer_tower `[0x01203c0f, 0x00303224]`, morytania_dark_green `[0x02605c07, 0x00030704]`) — transcribe the WHOLE table mechanically (throwaway script), never hand-edit deltas. Also read the rest of the Worldmap.ts diff (±273 — there may be logic hunks beyond the LUT; port output-affecting ones).
- [ ] **Step 4: The remaining tools/pack behavioral walk** — read `git diff 2e3bcf43..dee467c8 -- tools/pack/PackFile.ts tools/pack/PackFileBase.ts tools/pack/config/PackShared.ts tools/pack/map/Pack.js tools/pack/PackAll.ts tools/pack/graphics/pack.ts tools/pack/midi/pack.ts tools/pack/sound/pack.ts tools/pack/sprite/ tools/pack/versionlist/pack.ts tools/pack/NameMap.ts` IN FULL (rev-254 lesson — own eyes on pack diffs). Classify each hunk: ArtifactCache/FsCache/SourceSnapshot/ModelFlags/revalidation = build-infra NOT-PORTED (list them for the audit); anything touching emitted bytes = port now with a test. Expect mostly infra — but the byte-parity gate (Task 20) is the backstop that catches anything misclassified.
- [ ] **Step 5:** `go test ./pkg/pack/... -count=1` → PASS. Commit `feat(pack): 274 CRC bumps (9 config + interface + sound), *_full font renames, worldmap refColors regen (TS @dee467c8) [rev-274]`

### Task 19: Parity-gate env rename GOSCAPE_REF254_DIR → GOSCAPE_REF274_DIR

**Files:** mechanical sweep of `pkg/io/gziputil` + `pkg/pack/compiler` ONLY (packall lands in Task 20, unpack in Task 23 — the green-tree split from prior ports). Skip whatever Task 17 already renamed.

- [ ] **Step 1:** `grep -rln "REF254\|Ref254\|ref254\|Server254-ref" --include="*.go" pkg cmd modules` — in gziputil + compiler: env var → `GOSCAPE_REF274_DIR`, doc paths → `Server274-ref`, identifiers → `Ref274`.
- [ ] **Step 2:** `go test ./pkg/io/gziputil/... ./pkg/pack/compiler/... -count=1` (unset env: both skip with the NEW message); then with `GOSCAPE_REF274_DIR` set, the corpus + symbols gates run (corpus state per Task 17's outcome).
- [ ] **Step 3:** Commit `chore(parity): GOSCAPE_REF254_DIR → GOSCAPE_REF274_DIR (gziputil, compiler gates) [rev-274]`

### Task 20: Full-tree byte parity vs the 274 reference cache

**Files:**
- Regenerate: `pkg/packall/testdata/ref274_manifest.txt`, `ref274_ondemand_entries.txt` (replacing the ref254 pair)
- Modify: `pkg/packall/parity_test.go` (env name, paths, identifiers, headers)
- Modify: `pkg/pack/compiler/symbols_export_ref_parity_test.go` (33 .sym — count unchanged from 254)

- [ ] **Step 1:** Regenerate both manifests from `Server274-ref/engine/data/{pack,symbols}` + `ondemand.zip` in the EXACT ref254 format (read its header first; throwaway `$TMPDIR` script). `git rm` the ref254 pair, add ref274, update `parity_test.go`.
- [ ] **Step 2: The convergence gate:**

```bash
GOSCAPE_REF274_DIR=~/Code/github.com/LostCityRS/Server274-ref/engine \
  GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./pkg/packall/ -run Ref274FullTreeParity -v -count=1
```

Expected: RED first run. Iterate the Arc-26 byte-diff loop: per mismatching file, diff goscape output vs reference (`.sym` anchors compiler-input diffs; config jags decompose per-type; FONT-dependent outputs — interface, title — are the likely first movers given the rename; gzip members reflect Task 17's outcome). Each root cause = own fix commit citing TS. Existing exemptions (server/build timestamp, ondemand.zip container bytes, free2play/multiway.csv) carry forward unchanged.

- [ ] **Step 3:** Un-skip any Task-13-deferred real-cache fixture tests, repoint at the 274 cache → PASS.
- [ ] **Step 4:** Symbols gate → PASS (33/33 .sym byte-parity).
- [ ] **Step 5:** Commit(s): fixes individually, then `test(packall): re-baseline full-tree parity vs 274 reference cache (Engine-TS dee467c8 + Content 7f97b0a5) [rev-274]`

### Task 21: data/ working cache refresh

Check `git ls-files data | head`; refresh tracked 254-derived artifacts from the new reference cache; full suite to catch stale-cache assumptions. Commit if anything tracked changed: `chore(data): refresh 274 cache-derived fixtures [rev-274]`.

### Task 22: Live 274 client smoke (user-driven)

- [ ] **Step 1:** `CGO_ENABLED=0 go build -trimpath -o $TMPDIR/goscape ./cmd/goscape`; start `$TMPDIR/goscape --target all --config.file config.yaml` (content path → Server274-ref/content from Task 1).
- [ ] **Step 2:** Ask the USER to run Client-Java from `Server274-ref/javaclient` (`32f30626`) against it: login (**the 0xff+u2 revision handshake is the first live test**), walk, open shop/bank, hear midi, hop two map squares, kill an NPC → despawn/respawn. 274-specific probes: watch a wandering NPC (patrol countdown + stuck rework); drop/pick items and watch a shop restock (Inventory partial updates — watch for client-side inv desync); `::setvar`/`::getvar`; trigger any content using minimap toggle if reachable; click own tile (the #100 path).
- [ ] **Step 3:** Triage live failures (suspect silent-degradation paths first); each fix = own commit.
- [ ] **Step 4:** Record the smoke result for PORTING.md (DoD (b)).

### Phase 3 exit gate

- [ ] Full-tree parity GREEN; gziputil corpus GREEN (under the Task-17 resolution); symbols 33/33 GREEN; live smoke PASSED.
- [ ] `go test ./... -count=1` + `-race` on `pkg/pack/... pkg/packall/... modules/...`.

---

## Phase 4 — Unpack + manifest regeneration

### Task 23: Unpack deltas — revision tag, font names, family signatures

**Files:**
- Modify: `cmd/goscape-cli/cmd_unpack.go` (default `"254"` → `"274"` + help text)
- Modify: `pkg/unpack/config/driver.go` + family files per the TS diff
- Modify: `pkg/unpack/clientinterface/` (font-name output `font=p11_full` etc.)
- Test: per-family unit tests

- [ ] **Step 1: Read the TS** — `git diff 2e3bcf43..dee467c8 -- tools/unpack/` IN FULL. Expected shape (verify, don't assume): revision tag `'254'`→`'274'` (Unpack.ts:356); font-name emission in interface/Unpack.ts; Environment-path migrations (no-op for Go); small signature/formatting deltas in {Idk,Npc,Obj,SpotAnim}Config.ts + graphics/map/midi/sound unpackers; NO new config codes. Port each behavioral hunk; formatting-only hunks recorded NOT-PORTED.
- [ ] **Step 2: Failing tests** per changed family (synthetic-blob idiom); run → FAIL; implement; `go test ./pkg/unpack/... ./cmd/goscape-cli/... -count=1` → PASS.
- [ ] **Step 3:** Commit `feat(unpack): 274 deltas — revision tag, *_full font output, family signature parity (TS tools/unpack @dee467c8) [rev-274]`

### Task 24: Regenerate the 16 TS-output manifests at the 274 pin

**Files:** outside goscape: `Server274-ref/unpack-ref/` scratch; inside: `pkg/unpack/testdata/ref274/*` (replacing `ref254/`), `pkg/unpack/unpacktest/harness.go` + family `parity_test.go` updates (the unpack-side `Ref254`→`Ref274` renames deferred from Task 19).

- [ ] **Step 1:** Build the scratch layout under `Server274-ref/unpack-ref/` mirroring `Server254-ref/unpack-ref` (read its layout): pristine content copy + the reference cache where the TS tools expect it; establish the PackFile WROTE-noise baseline with a no-op family first. **Runner is `npx tsx <entrypoint>` (or `npm run` scripts) under Node 24 — NOT bun**; verify one family end-to-end before scripting the rest.
- [ ] **Step 2:** For each of the 16 family entrypoints: run at the pin, capture output, diff post-run trees → changed-set manifests in the EXACT ref254 format. Cautions carried forward: **config** — run with `data/pack` moved aside (vacuous-merge trap); **worldmap** — check `maps/ignore.csv` exists at Content `7f97b0a5`; **identity-roundtrip families** (map/sound/models) — empty changed-sets legitimate, WROTE mtimes are the guard; **interface** — changed-set WILL differ (font names).
- [ ] **Step 3:** `git rm -r pkg/unpack/testdata/ref254`, add `testdata/ref274/`; sweep `pkg/unpack` path strings + env var + identifiers. Commit manifests: `test(unpack): 274 TS-reference manifests (Engine-TS dee467c8) [rev-274]`
- [ ] **Step 4: All 16 parity tests:**

```bash
GOSCAPE_REF274_DIR=~/Code/github.com/LostCityRS/Server274-ref/engine \
  GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./pkg/unpack/... -count=1 -v
```

Iterate to 16/16 GREEN — divergences are bugs in Task 23 (fix where they live, own commits).

### Task 25: Pack-parity re-verification + full gates

- [ ] Re-run Task 20 full-tree parity + symbols + gziputil corpus (unpack runs must not have disturbed the reference cache; rebuild via `npm run build` in Server274-ref/engine if they did — then RE-VERIFY the manifests still match).
- [ ] `CGO_ENABLED=0 go build -trimpath ./... && go vet ./...`
- [ ] `go test ./... -count=1` (real exit code) + `go test -race ./pkg/... ./modules/... -count=1`
- [ ] `grep -rn "GOSCAPE_REF254_DIR\|ref254\|Ref254" --include="*.go" pkg cmd modules` → zero hits.

---

## Close-out

### Task 26: PORTING.md §rev-274 audit trail + close-out

**Files:**
- Modify: `PORTING.md` (new `## rev-274 audit trail` after §rev-254)
- Modify: `docs/superpowers/specs/2026-06-12-rev274-port-design.md` (Status → COMPLETE)

- [ ] **Step 1: Diff-correspondence audit** (DoD (a)): walk `git -C Engine-TS diff 2e3bcf43..dee467c8` hunk-by-hunk against `git diff rev-254..rev-274` and build the correspondence table (TS file:hunk → Go commit). The 274 disposition buckets are larger than prior ports — record them as grouped rows: `src/engine/routefinder/*` (14 files) → pre-existing `pkg/pathfinder` port, constants spot-verified, caller deltas in Tasks 9-10; `src/network/rsbuf/*` (13 files) → pre-existing `pkg/rsbuf`, delta in Task 16; bun→node/db-dialect/bcrypt/ws-sync/prisma/web-setup/prom/Environment-migration files → NOT-PORTED (architecture-mapped per spec table); ArtifactCache/FsCache/SourceSnapshot/ModelFlags + FileStream write-skip → NOT-PORTED build-infra (parity gate is the proof of output-neutrality). Run the close-out correspondence audit pass over the table (rev-254: it caught 3 real gaps).
- [ ] **Step 2:** Write the audit section: pins, phase summaries with commit ranges, the (a)-(e) DoD checklist with evidence (parity logs, gzip-baseline resolution, smoke result, suite exit codes), carried-forward exceptions, new PORTING-EXCEPTION rows.
- [ ] **Step 3:** Spec Status → `COMPLETE (date — phases 0-4 shipped; definition-of-done (a)-(e) met)`. Commit `docs(porting): rev-274 audit trail + close-out [rev-274]`

---

## Self-review notes (resolved during writing)

- **Worktrees + reference cache pre-exist** (built during the spec session, packability verified) — Task 1 verifies instead of creating; Task 17/20 consume them directly.
- **EVENT_TRACKING row is DELETED at 274** (at 254 it survived handler-less) — Task 2 removes the row outright; an unknown opcode is now a protocol error, matching the TS repository having no binding.
- **MINIMAP_TOGGLE is both a wire packet (Task 5) and a script op (Task 6)** — split so the script-op task owns the whole opcode-map regen.
- **SETIDKCOLOUR renames SETSKINCOLOUR in place** — map pin entry count moves only +4 (MAP_LOC, MINIMAP_TOGGLE, SET_SKILL_LEVEL, NPC_DESTINATION).
- **The Inventory change is semantic, not just structural** — `assureFullInsertion` deletion means partial adds now succeed; Task 8 Step 3 requires re-reading each branching caller against TS rather than mechanical adaptation.
- **NPC stuck-counter off-by-one directions** (`++ > 500` vs `>= 500`, `>= 32`) are flagged transcription points in Task 9 — pin exact tick counts in tests.
- **The gzip decision (Task 17) runs FIRST in Phase 3** and may inflate into a sub-arc (stock-zlib 1.3.1 port); the user-escalation path is pre-authorized in the spec's DoD (c) caveat only for the resists-deterministic-port case.
- **Symbols census stays 33** (varbit.sym arrived at 254; 274 adds no families) — Task 1 Step 1 verifies the count in the fresh reference build.
- **Pathfinder needs no Phase-2 task**: constants verified identical during spec exploration; ALL behavioral change is caller-side (AllowRepath deletion, Task 10). If Task 20/22 surface pathfinding divergence, re-open against `src/engine/routefinder/` at the pin.
- **CrcTable JS arithmetic** (`(hash << 1) + crc` under JS number semantics vs Go int32) is the highest-precision-risk hunk — Task 14 Step 1 requires reading whether TS coerces with `|0` and matching bit-for-bit.
- **jagFileVersion stays 27** (compiler unchanged at 0.9.6) — no task touches it; the parity gate would catch a surprise.
- **OnDemand chunk protocol verified unchanged at 274** (6-byte header + ≤500-byte parts; the worker-thread refactor is scheduling-only) — the only client-visible ondemand delta is the CRC-table body (Task 14). The ondemand handler tests + live smoke (Task 22) are the backstop; if a chunk-shape surprise appears, read `git show dee467c8:src/engine/OnDemandThread.ts` lines around the 6-byte header encode.
- **The env/identifier rename is split** (Task 17/19 gziputil+compiler, Task 20 packall, Task 24 unpack) so every intermediate commit stays green — same as prior ports.

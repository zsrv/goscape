# rev-244 B7 — `goscape-cli unpack` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Port TS `tools/unpack` (31 files, +3,793 at pin `9aadcec4` — all new; read whole files, not diffs) as `pkg/unpack/*` libraries behind one sub-dispatched `goscape-cli unpack` verb, pinned 1:1 to the TS implementation's output by a per-family byte-parity gate.

**Architecture:** Spec `docs/superpowers/specs/2026-06-06-rev244-b7-unpack-design.md` (commit `5a467988`). Phase 0 (TS reference unpack runs) first — the parity contract for everything else. Then the two out-of-slice dep ports (Model, Pix), then per-family ports in dependency order (config → interface → maps/midi/sound/sprites/graphics/versionlist/worldmap), each closing its own parity subtest, then the CLI verb, then gates + §B7 correspondence audit. All TS citations refer to the pin `9aadcec4`; the pinned worktree `/home/owner/Code/github.com/LostCityRS/Server244-ref/engine` IS the pin — read files there directly.

**Tech Stack:** Go (`pkg/unpack`, reusing `pkg/io/{filestream,jagfile,packet}`, `pkg/pack` registries/Parse, `pkg/colorconv`, `pkg/objtype`); bun for Phase 0 only.

**Worker ground rules (B2-B6-proven, bake into every implementer prompt):**
- Every go command: `GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache` prefix. Build with `CGO_ENABLED=0 go build -trimpath ./...`; `-race` needs `CGO_ENABLED=1`.
- Every commit: `git commit --no-gpg-sign` + `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>` trailer.
- Verify every `// TS <File>.ts:<lines>` citation against `cat -n /home/owner/Code/github.com/LostCityRS/Server244-ref/engine/<file>` BEFORE writing — cite FINAL-FILE numbers. Line numbers in this plan came from scouting agents; re-verify, don't trust.
- **Write Go code with the Write/Edit tools, never bash heredocs** (sandbox mangles `!=`; same for inline `python3 -c`).
- Sandbox `git status` shows phantom `??` dotfiles — never stage them; never `git add -A`. Stage explicit paths only.
- Stale LSP diagnostics false-alarm whole files — trust real build/vet/test runs only.
- "Wire a function" means CALLED FROM THE LIVE PATH — grep call sites, not just existence (B6 lesson).
- When porting a gate, ask what happens on the EMPTY side of it (B6 lesson — silent-degradation paths).
- Ask "what does TS's CONSUMER do?" before modeling a producer.
- Reject-path tests must seed earlier-gate prerequisites.
- A test can pin a bug — verify against TS before preserving any pinned contract.
- NEVER check out branches or dirty the working tree in ANY LostCityRS repo. The pinned worktrees (`Server244-ref/{engine,content,javaclient}`) are read-only references; unpack runs operate on SCRATCH COPIES only.

**Key shared facts (verified 2026-06-06, controller):**
- TS unpack reads `FileStream('data/unpack')` — the dat/idx client cache. B6's pack output IS that format; the byte-parity cache lives at `Server244-ref/engine/data/pack/main_file_cache.{dat,idx0-4}`.
- TS scripts write into `Environment.BUILD_SRC_DIR` (default `../content` at the pin) and MUTATE it: `fs.renameSync` of model files, `<Pack>.register(...)` + `.save()` rewrites of `pack/*.pack`, deletions of stale `.base`/`.frame` files. Reference runs and Go runs must both start from a pristine scratch copy of the Content worktree.
- Go API surface (all existing): `filestream.New(dir, createNew, readOnly)` / `.Read(archive, file, decompress)` / `.Count(archive)` / `.Has`; `jagfile.NewJagfile(pkt)` / `.Read(name)` / `LoadJagfile(path)`; `pack.PackFile{Register,Save,GetByID,GetByName,Size}` + `pack.Registry.Ensure{Model,Anim,Base,AnimSet,Map,Midi,Synth,Texture,Interface,Loc,Npc,Obj,Seq,Idk,Flo,SpotAnim,Varp}`; `pack.ListFilesExt(path, ext)`; `colorconv.ReverseHsl(hsl) []int`; `objtype.LoadFloTypes(dir)`.
- TS registry name mapping: `AnimSetPack`→`animset.pack`, `BasePack`→`base.pack`, `FramePack`→`anim.pack` (Go `Registry.Anim`), `SynthPack`→`synth.pack` + `synth.order`.
- **PNG outputs are NOT byte-comparable** (TS uses Jimp's encoder; Go uses `image/png`). Parity for `.png` = decoded-RGBA equality. PORTING-EXCEPTION row `rev244-b7-png-bytes` lands in Task 14. Everything else (text, `.pack`, `.order`, `.opt`, `.jm2`, `.if`, `.mid`, `.synth`, `.ob2`, `.anim`, `title.jpg`, stdout) is raw-byte-comparable.
- `cmd/goscape-cli/goscape-cli` (a stray built binary INSIDE the cmd dir) is committed? NO — verify in Task 0; never stage binaries.
- NOT-PORTED set (spec, user-approved): `sound/{Generate,Match,Reorganize,RenameFile,PrintDirectory,PrintOrderDirectory}.ts` — decision rows in Task 18, no code.

---

### Task 0: Housekeeping — stray binaries

**Files:**
- Delete: `goscape-cli` (repo root, 20 MB ELF, untracked B6 smoke artifact)
- Check: `cmd/goscape-cli/goscape-cli` (another stray binary visible in `find` output)

- [ ] **Step 1: Verify and delete**

```bash
file goscape-cli                      # expect: ELF ... Go BuildID (untracked build artifact)
git log --oneline -1 -- goscape-cli   # expect: no output (never tracked)
rm goscape-cli
git ls-files cmd/goscape-cli/goscape-cli   # if EMPTY: rm cmd/goscape-cli/goscape-cli too
git status --short                    # expect both gone; ignore phantom dotfiles; stage NOTHING
```

No commit (untracked files).

### Task 1: Phase 0 — TS reference unpack runs (controller-direct)

**Files (all OUTSIDE goscape except the manifests):**
- Create: `/home/owner/Code/github.com/LostCityRS/Server244-ref/unpack-ref/` (per-family snapshots)
- Create: `pkg/unpack/testdata/ref244/<family>.manifest.txt` (changed-set sha256 manifests, committed)

Controller-direct (sandbox bypasses, bun). The TS scripts have NO driver — each is a standalone entrypoint. Run each family against a FRESH scratch so families verify independently (no cross-family ordering contract).

- [ ] **Step 1: Build the pristine inputs ONCE**

```bash
cd /home/owner/Code/github.com/LostCityRS/Server244-ref
mkdir -p unpack-ref
# pristine content snapshot (the worktree must stay clean):
git -C content status --short            # expect clean BEFORE starting
du -sh content                           # record size (decides full-copy vs subtree-copy in Task 2)
# cache for data/unpack: the byte-parity client cache + worldmap.jag
mkdir -p unpack-ref/cache
cp engine/data/pack/main_file_cache.dat engine/data/pack/main_file_cache.idx* unpack-ref/cache/
cp engine/data/pack/mapview/worldmap.jag unpack-ref/cache/   # worldmap family input
```

- [ ] **Step 2: Per-family run loop**

For each family F in `config compare checksum interface map midi sound sprite-media sprite-textures sprite-title models anims versionlist-anim versionlist-midi versionlist-model worldmap` (script mapping in the table below):

```bash
cd /home/owner/Code/github.com/LostCityRS/Server244-ref
rm -rf unpack-ref/scratch && cp -r content unpack-ref/scratch && rm -rf unpack-ref/scratch/.git
cd engine
rm -rf data/unpack && mkdir -p data/unpack
cp ../unpack-ref/cache/main_file_cache.* data/unpack/
cp ../unpack-ref/cache/worldmap.jag data/unpack/           # harmless for other families
BUILD_SRC_DIR=../unpack-ref/scratch bun run <script> > ../unpack-ref/<F>.stdout 2> ../unpack-ref/<F>.stderr
echo "exit: $?"   # record per family; investigate non-zero before moving on
# snapshot the post-state:
cd ..
mv unpack-ref/scratch unpack-ref/<F>.post
cp -r engine/data/unpack unpack-ref/<F>.cachepost          # versionlist-model + checksum write here
```

| family | script | notes |
|---|---|---|
| config | `tools/unpack/config/Unpack.ts` | hardcodes revision '244' |
| compare | `tools/unpack/config/Compare.ts` | also reads `data/pack/client/config` — run from engine/ as-is (engine data/pack is the parity cache); type hardcoded `npc` |
| checksum | `tools/unpack/checksum.ts` | extracts into data/unpack/<jag>/ |
| interface | `tools/unpack/interface/Unpack.ts` | |
| map | `tools/unpack/map/Unpack.ts` | |
| midi | `tools/unpack/midi/Unpack.ts` | |
| sound | `tools/unpack/sound/Unpack.ts` | |
| sprite-media | `tools/unpack/sprite/media.ts` | |
| sprite-textures | `tools/unpack/sprite/textures.ts` | |
| sprite-title | `tools/unpack/sprite/title.ts` | |
| models | `tools/unpack/graphics/UnpackModels.ts` | |
| anims | `tools/unpack/graphics/UnpackAnims.ts` | |
| versionlist-anim | `tools/unpack/versionlist/anim_index.ts` | stdout-only |
| versionlist-midi | `tools/unpack/versionlist/midi_index.ts` | stdout-only |
| versionlist-model | `tools/unpack/versionlist/model_index.ts` | writes data/unpack/model_index{,.txt} |
| worldmap | `tools/unpack/worldmap/Unpack.ts` | reads `data/unpack/worldmap.jag` + FloType from `data/pack`; stdout-only |

If a script crashes on a missing input (e.g. Pix font members), record the failure VERBATIM in this plan file under Task-1 findings and continue — a TS-side crash is itself the contract (Go must be told what the reference actually did, or the family becomes a decision row).

- [ ] **Step 3: Changed-set manifests into goscape**

For each family, diff `<F>.post` against pristine `content` (and `<F>.cachepost` against the pristine cache inputs): record `ADDED/MODIFIED <path> <sha256>`, `DELETED <path>`, plus `STDOUT <sha256>` lines into `pkg/unpack/testdata/ref244/<F>.manifest.txt` with a header comment (engine pin, content pin, cache provenance, date). A small throwaway script is fine; keep it in `$TMPDIR`.

- [ ] **Step 4: Record findings + commit**

Append per-family facts to this plan under "Task-1 findings": exit codes, file counts, which top-level dirs each family touched (drives Task 2's scratch-copy scope), surprises. Commit manifests:
`test(unpack): 244 TS-reference unpack changed-set manifests (Engine-TS 9aadcec4) [rev-244 B7]`

### Task 2: Parity harness skeleton — `pkg/unpack/unpacktest`

**Files:**
- Create: `pkg/unpack/unpacktest/harness.go`, `pkg/unpack/unpacktest/harness_test.go`

Shared helpers every family's parity subtest uses (internal test-support package — name it `unpacktest`, import only from `_test.go` files):
- `RefDir(t) string` — `GOSCAPE_REF244_DIR` env gate (`t.Skip` when unset), returns `<env>/..` resolved to the `Server244-ref` root.
- `Scratch(t, contentDir string, subtrees []string) string` — copy the named top-level subtrees (from Task-1 findings; `nil` = full tree) of the pristine content worktree into `t.TempDir()`.
- `CacheDir(t) string` — temp dir seeded with `unpack-ref/cache/*` (main_file_cache.* + worldmap.jag).
- `ChangedSet(t, pristine, post string) []Entry` — walk both trees, emit ADDED/MODIFIED (+sha256) and DELETED entries, sorted.
- `AssertManifest(t, entries []Entry, stdout []byte, manifestPath string)` — compare against `testdata/ref244/<F>.manifest.txt`; `.png` entries compare by decoded-RGBA equality against the reference file under `unpack-ref/<F>.post` instead of sha (the `rev244-b7-png-bytes` exception).

- [ ] **Step 1: Failing unit tests** (NOT env-gated — synthetic dirs): ChangedSet detects add/modify/delete and sorts deterministically; AssertManifest passes on a matching synthetic manifest and fails with a useful diff on mismatch.
- [ ] **Step 2: RED.** `go test ./pkg/unpack/... -run Harness -v`
- [ ] **Step 3: Implement.**
- [ ] **Step 4: GREEN** (`go test ./pkg/unpack/... -count=1`).
- [ ] **Step 5: Commit** `test(unpack): parity-harness helpers — scratch copy, changed-set diff, manifest assert [rev-244 B7]`.

### Task 3: `pkg/unpack/internal/model` — Model metadata + texture detection

**Files:**
- Create: `pkg/unpack/internal/model/model.go`
- Test: `pkg/unpack/internal/model/model_test.go`

TS contract: `src/cache/graphics/Model.ts` (354 lines; unchanged since 225 — first Go consumer). Read the WHOLE file with `cat -n`. Port:
- `Unpack(id int, data []byte)` (Model.ts:49-141): parse the 18-byte trailer at `len(data)-18` (vertexCount u2, faceCount u2, texturedFaceCount u1, hasInfo u1, priority u1, hasAlpha u1, hasFaceLabels u1, hasVertexLabels u1, dataLengthX/Y/Z u2×3, dataLengthFaceOrientations u2), compute the section offsets exactly as TS does, store metadata keyed by id. nil data → zeroed metadata entry (TS posture).
- `FromID(id int)` lazy full decode (Model.ts:~143-345) — `modelsHaveTexture` needs `faceInfo`/`faceColour`; port the full decode faithfully (it's the only correct source of those arrays).
- `ModelsHaveTexture(modelIds []int, textureId int) bool` (Model.ts:346-354): any model whose decoded faces have `(faceInfo[i]&0x3) > 1 && faceColour[i] == textureId`.

Go shape: a `Store` struct (not package globals) holding `meta []*Metadata` + decode cache; the config driver owns one instance. Raw `data []byte` access via `pkg/io/packet` where it helps; offsets are absolute — plain slice indexing is fine and closer to the TS.

- [ ] **Step 1: Failing tests** — hand-built synthetic model blobs: (a) trailer parse pins every metadata field; (b) a minimal 2-face model where face 0 is textured (`faceInfo&0x3 == 2`, colour == textureId) → ModelsHaveTexture true for that texture, false for another; (c) nil data → zero metadata, no panic; (d) FromID decode of the synthetic model pins faceInfo/faceColour arrays. Build the synthetic blob bytes in the test via a writer helper mirroring the offset layout (cite the trailer layout lines).
- [ ] **Step 2: RED.**
- [ ] **Step 3: Implement** (faithful field-for-field; cite line ranges per block).
- [ ] **Step 4: GREEN** + spot-check vs the real cache (env-gated subtest: open the parity cache, `Unpack` every archive-1 entry, assert no panic and a couple of known counts — record the counts from the reference at implementation time).
- [ ] **Step 5: Commit** `feat(unpack): Model metadata unpack + modelsHaveTexture (TS Model.ts, first Go consumer) [rev-244 B7]`.

### Task 4: `pkg/unpack/internal/pix` — sprite decode + PNG/opt writer

**Files:**
- Create: `pkg/unpack/internal/pix/pix.go`
- Test: `pkg/unpack/internal/pix/pix_test.go`

TS contract: `src/cache/graphics/Pix.ts` (310 lines; unchanged since 225 — first Go consumer; `pkg/pack/sprites` is the ENCODE side, do not touch it). Port the decode/export half only (YAGNI — `packHeader`/`pack` writers stay unported; record in Task 18 audit):
- `UnpackJag(jag, name, i)` (Pix.ts:72-139): index.dat-driven sprite decode — palette (g3 per colour, slot 0 transparent), per-sprite crop/croppedW/croppedH/pixelOrder, row-major vs column-major pixel fill.
- `UnpackFull(jag, path, name)` (Pix.ts:33-70): loop sprites until nil; single sprite → one PNG; multi → spritesheet (sheet dimension search incl. the isPrime helper, Pix.ts:10-18, 163-189), magenta `0xff00ff` background = transparent; write `<path>/<name>.png` + `<path>/meta/<name>.opt` (`{w}x{h}` header + per-sprite `{cropX},{cropY},{width},{height}` lines — verify exact .opt shape against Pix.ts:50-66).
- Go imaging: `image.NRGBA` + stdlib `image/png`. PNG bytes will differ from Jimp — pixel-level parity only (harness handles it).

- [ ] **Step 1: Failing tests** — synthetic jag with `index.dat` + one `<name>.dat`: (a) single 2×2 sprite, both pixel orders, palette + transparency pinned per-pixel; (b) multi-sprite sheet → sheet dimensions + blit positions pinned (decode the written PNG back); (c) `.opt` contents byte-pinned.
- [ ] **Step 2: RED.** → **Step 3: Implement.** → **Step 4: GREEN.**
- [ ] **Step 5: Commit** `feat(unpack): Pix sprite decode + PNG/opt export (TS Pix.ts, first Go consumer) [rev-244 B7]`.

### Task 5: `pkg/unpack/config` — ConfigIdx + simple type unpackers (flo, varp, seq)

**Files:**
- Create: `pkg/unpack/config/common.go`, `pkg/unpack/config/flo.go`, `pkg/unpack/config/varp.go`, `pkg/unpack/config/seq.go`
- Test: `pkg/unpack/config/flo_test.go`, `varp_test.go`, `seq_test.go`

TS contracts (read each whole file): `config/Common.ts` (ConfigIdx type), `config/Unpack.ts:22-45` (readConfigIdx — count=g2, per-entry len=g2, pos accumulates from 2), `config/FloConfig.ts`, `config/VarpConfig.ts`, `config/SeqConfig.ts`. These three have NO model renames — pure `(ConfigIdx, id) → []string` functions plus registry lookups (FloPack/TexturePack; VarpPack; SeqPack/AnimPack/ObjPack with `anim_`/`obj_`-prefixed fallbacks and the `replaceheld*` `-512` offset + `hide` cases — port EXACTLY what the listing says, including any oddities like fallback-string spacing; they're parity-load-bearing).

Unknown-opcode posture per type: flo/varp/seq → warning (match the TS print text; warnings go through a `Warnf`-style sink param so the CLI can route to slog and parity tests can capture). Position check: warn when `dat.pos != pos[id]+len[id]` where TS does.

- [ ] **Step 1: Failing tests** — table-driven per type: synthetic dat/idx packets covering every opcode → exact emitted line slices (incl. `[debugname]` headers from a fake registry); unknown opcode → warning text; truncation-position warning.
- [ ] **Step 2: RED.** → **Step 3: Implement.** → **Step 4: GREEN** (`go test ./pkg/unpack/... -count=1`).
- [ ] **Step 5: Commit** `feat(unpack): config idx reader + flo/varp/seq unpackers (TS tools/unpack/config 244) [rev-244 B7]`.

### Task 6: `pkg/unpack/config` — renaming type unpackers (idk, npc, obj, spotanim, loc)

**Files:**
- Create: `pkg/unpack/config/rename.go` (the shared renameModel shape), `idk.go`, `npc.go`, `obj.go`, `spotanim.go`, `loc.go`
- Test: matching `*_test.go`

TS contracts: `IdkConfig.ts`, `NpcConfig.ts`, `ObjConfig.ts`, `SpotAnimConfig.ts`, `LocConfig.ts` (+ `LocShapeSuffix` enum + `unpackLocModels`). Each has a per-type `renameModel` variant (prefix `idk_`/`npc_`/`obj_`/`spot_`; loc strips shape/_ld suffixes) — TS duplicates the helper per file with small differences; mirror the differences, don't unify beyond what TS shares (fork-drift rule, but here TS itself forks — match TS). renameModel: only acts when the current ModelPack name `startsWith("model_")`; finds collision-free names via GetByName; `fs.renameSync` the `.ob2` found via ListFilesExt(models, ".ob2") endsWith match; registers the new name.

Recol/retex post-processing (identical block in idk/loc/npc/obj; spotanim uses threshold **50** not 100): raw value ≥ threshold on either side → emit recol as `0x`-hex RGB via `colorconv.ReverseHsl` (verify which element of the returned slice TS takes); else if `ModelsHaveTexture(modelIds, src)` → `retex{i}s/d` with TexturePack names; else RGB. Loc's unknown opcode → **printFatalError** (error return), others warn — port the asymmetry.

Deps injected as a small `Env` struct: registries (Model/Seq/Texture/Obj + own pack), `*model.Store`, src-dir for renames, warn sink. `unpackLocModels` returns the `{models, ldModels}` shape-tuples (ldModels stays always-empty at the pin — port as-is, cite it).

- [ ] **Step 1: Failing tests** — per type: every opcode → exact lines (fixture registries + fake FS layout under `t.TempDir()` for rename assertions); recol-vs-retex branch matrix (raw≥threshold / texture-detected / RGB fallback) with a stub texture-checker; loc fatal on unknown opcode; renameModel collision loop (`name`, `name_i2`, ...) pinned.
- [ ] **Step 2: RED.** → **Step 3: Implement.** → **Step 4: GREEN.**
- [ ] **Step 5: Commit** `feat(unpack): idk/npc/obj/spotanim/loc unpackers + model renaming + recol/retex (TS tools/unpack/config 244) [rev-244 B7]`.

### Task 7: `pkg/unpack/config` — driver (`Unpack`) + config parity

**Files:**
- Create: `pkg/unpack/config/driver.go`
- Test: `pkg/unpack/config/driver_test.go`, `pkg/unpack/config/parity_test.go`

TS contract: `config/Unpack.ts:47-368` — `unpackConfigNames` (idx-count registration loop + `<pack>.save()`), `reorderUnpacked` (debugname/name/desc/model/recol bucket reorder, per-type settings switch), `unpackConfig` (all.{type} emission, blank-line separators, the optional cache2 `.merge` compare path), `unpackModelNames` ('loc' + seenAsNonCentrepiece shape-10 filter + `_8` suffix skip + rename/registration flow), `unpackConfigs` driver (cache open, Model.Unpack preload loop over `ModelPack` max, name passes, dir creation, the loc-models pass, the eight `unpackConfig` calls in TS ORDER: loc, obj, spotanim, idk, npc, seq, flo, varp; final ModelPack.save).

Exported API: `config.Unpack(opts Options) error` with `Options{CacheDir, PackDir, SrcDir, Revision string, Log *slog.Logger}` — PackDir optional (merge-compare path active only when its cache exists, mirroring TS's existsSync gate; remember the empty-side question: absent PackDir = plain unpack, NOT an error).

- [ ] **Step 1: Failing driver unit tests** — reorderUnpacked bucket order pinned per settings combo; unpackConfigNames registers `type_N`-style defaults only for unnamed ids (verify TS's actual default-name behavior in :47-79 first); merge path emits `.merge` blocks on differing entries.
- [ ] **Step 2: RED.** → **Step 3: Implement.**
- [ ] **Step 4: Parity subtest** — env-gated: scratch content (subtrees per Task-1 findings) + cache dir → `config.Unpack` → `ChangedSet` vs `testdata/ref244/config.manifest.txt`. Iterate until GREEN — this is the task's real gate; divergences are bugs in Tasks 5-7 (fix where they live, one commit each if substantive).
- [ ] **Step 5: Commit** `feat(unpack): config unpack driver + all.<type> emission + TS-output parity (TS config/Unpack.ts 244) [rev-244 B7]`.

### Task 8: `pkg/unpack/config` Compare + `pkg/unpack/checksum` (debug aids)

**Files:**
- Create: `pkg/unpack/config/compare.go`, `pkg/unpack/checksum/checksum.go`
- Test: `pkg/unpack/config/compare_test.go`, `pkg/unpack/checksum/checksum_test.go`

TS contracts: `config/Compare.ts` (readConfigIdx local copy; per-entry length + CRC compare; `Packet.getcrc` = the existing Go packet CRC; output via printInfo/printWarning — emit to an `io.Writer`), `checksum.ts` (printCrcs over config/interface/synth jags: per-member `jagName name crc` lines + extraction into `<cache-dir>/<jag>/<name>`). Go API: `config.Compare(cacheDir, packDir, typ string, out io.Writer) error` (typ default `npc` at the CLI); `checksum.Run(cacheDir string, out io.Writer) error`. Member iteration order = the jag's directory order (Go jagfile preserves it — if no ordered-names accessor exists yet, add an exported one in `pkg/io/jagfile` in this task, with its own unit test).

- [ ] **Step 1: Failing tests** — synthetic two-cache fixtures: equal → `exact match` line; size/length/crc mismatches → exact warning lines; checksum writes extracted files + prints crc lines in member order.
- [ ] **Step 2: RED.** → **Step 3: Implement.**
- [ ] **Step 4: Parity subtests** for `compare` + `checksum` (stdout sha + cachepost changed-set vs manifests). GREEN.
- [ ] **Step 5: Commit** `feat(unpack): config compare + cache checksum debug aids (TS Compare.ts/checksum.ts 244) [rev-244 B7]`.

### Task 9: `pkg/unpack/clientinterface` — binary component decode

**Files:**
- Create: `pkg/unpack/clientinterface/decode.go` (component struct + unpack)
- Test: `pkg/unpack/clientinterface/decode_test.go`

TS contract: `interface/Unpack.ts:86-288` (the IfType binary unpack — the biggest single contract; read it whole, twice). Per-component: comType/buttonType/clientCode/width/height/trans, the `((v-1)<<8)+g1` overLayer/model/anim encodings, comparator + script arrays (nested g2 opcode lists), then the per-type tails (layer children w/ g2s coords; inv slots; rect/text colours + strings; graphic strings; model/anim ids; invtext slots+options), the 65535 layer-marker root loop, and the `order` array capture. Decode into an explicit `Component` struct mirroring the TS field set; preserve LOAD ORDER in a slice.

- [ ] **Step 1: Failing tests** — synthetic `data` member exercising each comType (one fixture per type, byte-built in the test) → struct fields pinned; the layer-marker/order semantics pinned with a two-root fixture.
- [ ] **Step 2: RED.** → **Step 3: Implement.** → **Step 4: GREEN.**
- [ ] **Step 5: Commit** `feat(unpack): interface binary component decode (TS interface/Unpack.ts:86-288) [rev-244 B7]`.

### Task 10: `pkg/unpack/clientinterface` — naming + .if export + parity

**Files:**
- Create: `pkg/unpack/clientinterface/export.go`, `pkg/unpack/clientinterface/unpack.go` (the `Unpack(opts)` entry)
- Test: `pkg/unpack/clientinterface/export_test.go`, `parity_test.go`

TS contract: `interface/Unpack.ts:11-35` (renameModel → `com_iN` names into `models/com/`), `:294-357` (name assignment — `inter_N` roots, `inter_P:com_N` children, InterfacePack registration + save, interface.order export, per-root `.if` file write), `:359-807` (the export emission — every key in the scouting report's list: type/buttontype/clientcode/width/height/overlayer, script ops with operand symbol resolution via Varp/Obj/Seq/Interface packs (`stat_*`, `inv_count`, `pushvar`, `testbit`, ...), comparators (eq/lt/gt/neq), per-type fields (scroll/hide; draggable/margins/slots; fill; center/font/shadowed/text/colours; graphic; model/anim/zoom/xan/yan; invtext) , actionverb/actiontarget bitfield names, option, layer-child recursion with child x/y).

- [ ] **Step 1: Failing export tests** — fixture components (from Task 9 structs directly) → exact `.if` text; naming pass collision/ordering pinned; interface.order bytes pinned.
- [ ] **Step 2: RED.** → **Step 3: Implement.**
- [ ] **Step 4: Parity subtest** vs `interface.manifest.txt`. Iterate to GREEN.
- [ ] **Step 5: Commit** `feat(unpack): interface naming + .if export + TS-output parity (TS interface/Unpack.ts 244) [rev-244 B7]`.

### Task 11: `pkg/unpack/maps` + parity

**Files:**
- Create: `pkg/unpack/maps/maps.go`
- Test: `pkg/unpack/maps/maps_test.go`, `parity_test.go`

TS contract: `map/Unpack.ts` whole file — versionlist `map_index` walk (region g2, landFile g2, locFile g2, member gbool), archive-4 reads (decompress=true), `readLand` code ladder (0 break / 1 height / 2-49 overlay+shape+rot / 50-81 flags / 82+ underlay), `readLocs` gsmart delta decoding, the `.jm2` emission formats (`==== MAP ====` land lines `level x z: h.. o..[;shape][;rot] f.. u..`, `==== LOC ====` lines, PRESERVATION of existing `==== NPC ====`/`==== OBJ ====` sections from a pre-existing file), MapPack register (`m/l` names) + save, missing-file warning.

- [ ] **Step 1: Failing tests** — synthetic land/loc payloads → exact `.jm2` text incl. section preservation (seed a pre-existing file with NPC/OBJ sections); code-ladder edge values (2, 49, 50, 81, 82) pinned.
- [ ] **Step 2: RED.** → **Step 3: Implement.** → **Step 4: Parity subtest GREEN.**
- [ ] **Step 5: Commit** `feat(unpack): maps → .jm2 with NPC/OBJ section preservation + parity (TS map/Unpack.ts 244) [rev-244 B7]`.

### Task 12: `pkg/unpack/midi` + `pkg/unpack/versionlist` + parity

**Files:**
- Create: `pkg/unpack/midi/midi.go`, `pkg/unpack/versionlist/versionlist.go`
- Test: matching `*_test.go` + `parity_test.go` per package

TS contracts: `midi/Unpack.ts` (archive-3 loop, midi_index g1 jingle flag → songs/ vs jingles/, MidiPack register/save, missing warning); `versionlist/anim_index.ts` (g2 flags binary+decimal print), `midi_index.ts` (g1 prefetch + MidiPack name print), `model_index.ts` (raw `model_index` byte copy + `model_index.txt` flag-decode lines — the 8-name bitmask table; output paths are CACHE-dir-relative). All print output through `io.Writer`.

- [ ] **Step 1: Failing tests** — synthetic versionlist jag + archive entries: midi routing by flag; each index tool's exact output lines (pin the model_index flag-name combinations incl. multi-bit values).
- [ ] **Step 2: RED.** → **Step 3: Implement.** → **Step 4: Parity subtests (midi, versionlist-anim/midi/model) GREEN.**
- [ ] **Step 5: Commit** `feat(unpack): midi extraction + versionlist index tools + parity (TS midi/versionlist 244) [rev-244 B7]`.

### Task 13: `pkg/unpack/sound` + parity

**Files:**
- Create: `pkg/unpack/sound/sound.go`
- Test: `pkg/unpack/sound/sound_test.go`, `parity_test.go`

TS contract: `sound/Unpack.ts` whole file — Wave static unpack over `sounds.dat` (id g2 until 65535, per-sound byte-range extraction via the Tone/Envelope structural parse that ADVANCES the read position — port Wave/Tone/Envelope unpack faithfully; they exist to find each sound's extent), CRC-vs-existing-synth matching branch (note `keepNames` default and which branch the entrypoint actually exercises — verify, the scouting report was unsure), SynthPack register/save, `synth.order` write, `.synth` file emission.

- [ ] **Step 1: Failing tests** — synthetic sounds.dat with two tones/envelopes → extraction boundaries pinned (the parse-extent IS the contract); order file bytes; registry default `sound_N` naming.
- [ ] **Step 2: RED.** → **Step 3: Implement.** → **Step 4: Parity subtest GREEN.**
- [ ] **Step 5: Commit** `feat(unpack): synth sound extraction (Wave/Tone/Envelope extents) + parity (TS sound/Unpack.ts 244) [rev-244 B7]`.

### Task 14: `pkg/unpack/sprite` + parity (PNG exception lands here)

**Files:**
- Create: `pkg/unpack/sprite/sprite.go` (Media/Textures/Title entries)
- Test: `pkg/unpack/sprite/sprite_test.go`, `parity_test.go`

TS contracts: `sprite/media.ts` (archive 0:4, per-jag-member Pix.UnpackFull → sprites/), `sprite/textures.ts` (archive 0:6, ids 0-49, TexturePack name overrides → textures/), `sprite/title.ts` (archive 0:1 — title.dat → binary/title.jpg raw; fonts b12/p11/p12/q8 → fonts/; logo/runes/titlebox/titlebutton → title/). Jag member iteration in directory order (the Task-8 ordered-names accessor).

- [ ] **Step 1: Failing tests** — synthetic jag with two sprite members → output paths + .opt bytes; title.dat passthrough bytes.
- [ ] **Step 2: RED.** → **Step 3: Implement.**
- [ ] **Step 4: Parity subtests** (media/textures/title) GREEN — `.png` via pixel comparison. Add the `PORTING-EXCEPTION (rev244-b7-png-bytes, Jimp-vs-image/png encoder)` marker at the PNG write site + PORTING.md row.
- [ ] **Step 5: Commit** `feat(unpack): media/textures/title sprite extraction + pixel-level PNG parity (TS sprite 244) [rev-244 B7]`.

### Task 15: `pkg/unpack/graphics` (models + anims) + parity

**Files:**
- Create: `pkg/unpack/graphics/models.go`, `pkg/unpack/graphics/anims.go`
- Test: matching tests + `parity_test.go`

TS contracts: `graphics/UnpackModels.ts` (versionlist model_index flags; archive-1 gunzip → existing `.ob2` path via ListFilesExt endsWith match, else `models/_unpack/<name>.ob2`; `model_N` default registration; missing-model warn (flagged) vs debug (unflagged); ModelPack save; `Extracting N models` stdout) and `graphics/UnpackAnims.ts` (archive-2 loop → raw `.anim` save under models/ + AnimSetPack `anim_N` registration; trailer offset table (last 8 bytes → head/tran1/tran2/del sections); base-skeleton skip walk; frame-table walk registering FramePack ids + deleting stale `.base`/`.frame` files; BasePack/FramePack/AnimSetPack saves). The deletion pass MUTATES the tree — exercised by parity.

- [ ] **Step 1: Failing tests** — synthetic gzip'd archive-1 entries + model_index → routing (existing path vs _unpack), warnings; synthetic anim-set blob with a 2-frame head/tran walk → FramePack registrations + extent of tran2 consumption pinned; stale-file deletion pinned.
- [ ] **Step 2: RED.** → **Step 3: Implement.** → **Step 4: Parity subtests (models, anims) GREEN.**
- [ ] **Step 5: Commit** `feat(unpack): model + animset extraction with registry maintenance + parity (TS graphics 244) [rev-244 B7]`.

### Task 16: `pkg/unpack/worldmap` + parity

**Files:**
- Create: `pkg/unpack/worldmap/worldmap.go`
- Test: `pkg/unpack/worldmap/worldmap_test.go`, `parity_test.go`

TS contract: `worldmap/Unpack.ts` (33 lines) — `FloType.load(packDir)` (Go: `objtype.LoadFloTypes`), `Jagfile.load(<cacheDir>/worldmap.jag)`, floorcol.dat table print (8-hex-padded underlay/overlay + flo metadata comment incl. TexturePack texture name when set), labels.dat print (`=text,x,y,font`). Pure print tool — `io.Writer`.

- [ ] **Step 1: Failing tests** — synthetic worldmap.jag + flo fixtures → exact stdout lines (texture and no-texture variants; hex padding pinned).
- [ ] **Step 2: RED.** → **Step 3: Implement.** → **Step 4: Parity subtest (stdout sha) GREEN.**
- [ ] **Step 5: Commit** `feat(unpack): worldmap floorcol/labels dump + parity (TS worldmap/Unpack.ts 244) [rev-244 B7]`.

### Task 17: `cmd/goscape-cli` — the `unpack` verb

**Files:**
- Create: `cmd/goscape-cli/cmd_unpack.go`
- Modify: `cmd/goscape-cli/main.go` (verbs slice entry)
- Test: `cmd/goscape-cli/cmd_unpack_test.go`

House conventions (cmd_pack.go / cmd_jag.go): `runUnpack(args, stdout, stderr) int`; first positional arg = family (`config interface map midi sound models anims sprite-media sprite-textures sprite-title versionlist-anim versionlist-midi versionlist-model worldmap checksum compare`); flags `-cache-dir` (default `data/unpack`), `-src-dir` (default `data/src`), `-revision` (default `244`, config only), `-pack-dir` (default `data/pack`; consumed by compare/worldmap/config-merge), `-type` (default `npc`, compare only), `-log.level`/`-log.format`. Exit codes 0/1/2. Print-style families write to stdout; logs to stderr. Usage text lists families (jag-verb style).

- [ ] **Step 1: Failing tests** — dispatch: unknown family → 2 + usage; `-h` → 0; each family routes to its library func (smallest viable: run `checksum`/`worldmap` against tiny synthetic fixtures end-to-end through the verb; the rest assert flag plumbing via a focused fake or tiny fixture, matching how cmd_pack_test.go does it — read it first).
- [ ] **Step 2: RED.** → **Step 3: Implement.** → **Step 4: GREEN** (`go test ./cmd/goscape-cli/... -count=1`).
- [ ] **Step 5: Commit** `feat(cli): goscape-cli unpack verb — 16 families, TS tools/unpack surface [rev-244 B7]`.

### Task 18: Gates, §B7 correspondence audit, umbrella close-out

- [ ] **Step 1: Full gates** (capture real exit codes):

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache CGO_ENABLED=0 go build -trimpath ./... && \
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go vet ./... ; echo "vet: $?"
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./... -count=1 ; echo "test: $?"
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache CGO_ENABLED=1 go test -race ./pkg/unpack/... ./cmd/goscape-cli/... -count=1 ; echo "race: $?"
GOSCAPE_REF244_DIR=/home/owner/Code/github.com/LostCityRS/Server244-ref/engine \
  GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./pkg/unpack/... ./pkg/packall/... -count=1 ; echo "parity: $?"
```

vet failures must be pre-existing-only; the packall parity gate re-run proves B7 didn't disturb B6.

- [ ] **Step 2: PORTING.md §B7 correspondence audit** — table mapping ALL 31 `tools/unpack` files + the 2 src/ dep files (Model.ts, Pix.ts — note the unported Pix encode half as in-scope-omitted with YAGNI rationale) → commit SHA or decision row. Decision rows: 6× NOT-PORTED synth-curation (missing artifacts: rs2client.jar SoundSynth, synths.json, 377-synth dump); `rev244-b7-png-bytes` exception row. Cross-check `grep -rn "PORTING-EXCEPTION" modules pkg cmd internal | wc -l` (expect 23 = B6's 22 + png-bytes).
- [ ] **Step 3: Final whole-bundle integration review** (subagent: full `git diff <pre-B7>..HEAD` + spec + audit table; verdict READY/NOT-READY; verify "missing X" findings before acting — false positives happen).
- [ ] **Step 4: Umbrella close-out.** B7 is the LAST bundle — record in PORTING.md §B7 that umbrella items (a)-(d) plus the B7 scope decision are all met; update the umbrella spec Status line; note the B6-inherited follow-ups that remain open (config.yaml absolute content path; `TestDecodeRealCacheBlob` decoder bug — confirm whether B7's script-blob reads tripped over it, close or carry the row). Write a short next-steps handoff ONLY if open work remains; otherwise the rev-244 port is COMPLETE — say so plainly.
- [ ] **Step 5: Commit docs** `docs(porting): rev-244 B7 audit trail + umbrella close-out [rev-244 B7]`.

---

## Self-review notes (controller, written at plan time)

- **Spec coverage:** scope table → Tasks 5-16 (all 25 in-scope TS files); NOT-PORTED rows → Task 18; CLI surface → Task 17; package layout → Tasks 3-16 match the spec tree (internal/{model,pix} for the src/ deps); TS-output parity gate → Tasks 1, 2, and per-family subtests; cache-dir-output nuance (versionlist-model, checksum) → Task 1 Step 2 cachepost + Task 2 CacheDir/ChangedSet; determinism hazards → ground rules + ListFilesExt order note (Task 6/15 implementers must verify Go ListFilesExt ordering matches TS's readdirSync usage — flag any divergence as a finding, not a silent fix).
- **PNG byte-parity is impossible** (Jimp encoder) — resolved as pixel-level comparison + exception row (Task 14), consistent with the B6 ondemand.zip precedent.
- **Type consistency:** `Options{CacheDir, PackDir, SrcDir, Revision, Log}` shape shared across family `Unpack` entries; print tools take `io.Writer`; harness API named once in Task 2 and reused verbatim in Tasks 7-16.
- **Deliberate deferrals to extraction commands:** per-opcode Go code for the 9 config unpackers, the interface decode/export field lists, and Wave/Tone/Envelope — the numbered TS listings are the contract (B1/B6 precedent); the plan pins shapes, orders, thresholds (spotanim 50 vs 100), asymmetries (loc fatal), and test fixtures instead of transcribing 3,800 lines.
- **Risk watch:** Task 1 may surface TS-side crashes or env surprises (Pix/Jimp under bun, BUILD_SRC_DIR env override not taking effect) — findings are recorded in-plan and downstream tasks adjust; if BUILD_SRC_DIR cannot be redirected, fall back to a scratch ENGINE copy whose `../content` sibling is the scratch (record which).

# NPC Entity + NpcInfo Encoder Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add NPC entities + NpcInfo bitstream encoder so players see NPCs walking, patrolling, and emoting in the world.

**Architecture:** Vendor `NpcType` from `rs-server-225` (~280 LOC) into `pkg/objtype`. Add a tracked-set + index for NPCs to existing `pkg/buildarea` and `pkg/grid`. Add 7 NPC mask payload encoders + 3-phase `EncodeNpc` to `pkg/rsbuf`. Implement `*Npc` struct with WANDER + PATROL AI in `modules/world/`. Spawn NPCs at startup from the existing `gamemap` n-file parser (currently a stub — implement now). Wire into a new `processNpcs` tick phase.

**Tech Stack:** Go standard library only — `math/rand/v2`, `hash/fnv` (already imported). Reference: `github.com/2004scape/rsbuf` branch 225.

> All `go` commands must use the prefix: `GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache`

---

## File Map

| File | Action |
|------|--------|
| `pkg/objtype/npctype.go` | **New (vendored)** — `NpcType`, `NPCTypeConfigs`, `LoadNPCTypes` |
| `pkg/objtype/npctype_test.go` | New — smoke load |
| `pkg/gamemap/load.go` | Modify — implement `loadNPCs` |
| `pkg/gamemap/gamemap.go` | Modify — add `npcSpawns []NpcSpawn` + `NpcSpawns()` accessor |
| `pkg/gamemap/gamemap_test.go` | Modify — `TestLoadNpcs` with synthetic n-file |
| `pkg/grid/grid.go` | Modify — add `npcZones`, `AddNpc`, `RemoveNpc`, `NearbyNpcs` |
| `pkg/grid/grid_test.go` | Modify — `TestNpcAddRemove`, `TestNearbyNpcsLevelFilter` |
| `pkg/buildarea/buildarea.go` | Modify — add `Npcs map[int]struct{}` |
| `pkg/buildarea/buildarea_test.go` | Modify — `TestNpcsSetAddRemove` |
| `pkg/rsbuf/npc_source.go` | **New** — `NpcSource` interface + 7 mask constants |
| `pkg/rsbuf/npc_mask_payload.go` | **New** — payload writers |
| `pkg/rsbuf/npc_mask_payload_test.go` | **New** — byte-level tests |
| `pkg/rsbuf/renderer.go` | Modify — add npc caches + `ComputeNpcs` |
| `pkg/rsbuf/renderer_test.go` | Modify — npc tests |
| `pkg/rsbuf/npcinfo.go` | **New** — `EncodeNpc` |
| `pkg/rsbuf/npcinfo_test.go` | **New** — golden-byte encoder tests |
| `pkg/io/protocol/game/server/prot.go` | Modify — add `OpNpcInfo` |
| `modules/world/npc.go` | **New** — `Npc` struct + `NewNpc` |
| `modules/world/npc_ai.go` | **New** — `turn`, `wanderMode`, `patrolMode`, `Kill`, `queueWaypoint`, `advanceWaypoint` |
| `modules/world/npc_masks.go` | **New** — setters + `ResetMasks` |
| `modules/world/npc_source.go` | **New** — `*Npc` accessors satisfying `rsbuf.NpcSource` |
| `modules/world/npc_registry.go` | **New** — `addNpc`, `removeNpc`, `allocNpcSlot` |
| `modules/world/npc_test.go` | **New** — defaults + setters |
| `modules/world/npc_ai_test.go` | **New** — wander/patrol/kill/respawn |
| `modules/world/server.go` | Modify — load NpcTypes; spawn NPCs from `gamemap.NpcSpawns()` |
| `modules/world/tick.go` | Modify — `processNpcs` phase; extend `processInfo`/`processCleanup` |
| `modules/world/player.go` | Modify — replace `updateNpcs` stub |
| `modules/world/player_npc_info.go` | **New** — `(p *Player) updateNpcs()` calling `EncodeNpc` |
| `modules/world/player_npc_test.go` | **New** — integration: player sees NPC + NPC says hello |

---

## Vendoring Notes

- Follow sub-spec 3a's pattern: `cp` the file, `sed` package + import paths, build, fix any `entity.MoveRestrict`/`entity.NPCMode` references by replacing them with plain `int` fields.
- The vendored `LoadNPCTypes` may take a `*ParamTypeConfigs` param like `LoadObjTypes`. If so, accept it from the existing `s.paramTypes` (loaded in sub-spec 3a startup).

---

## Task 1: Vendor `pkg/objtype/npctype.go`

**Files:**
- Create: `pkg/objtype/npctype.go`
- Create: `pkg/objtype/npctype_test.go`

- [ ] **Step 1: Copy and rewrite imports**

```bash
cp /home/owner/Code/github.com/zsrv/rs-server-225/cache/config/npctype.go \
   /home/owner/Code/github.com/zsrv/goscape/pkg/objtype/
cd /home/owner/Code/github.com/zsrv/goscape/pkg/objtype
sed -i '1s/^package config$/package objtype/' npctype.go
sed -i 's|github.com/zsrv/rs-server-225/io/packet|github.com/zsrv/goscape/pkg/io/packet|g' npctype.go
sed -i 's|"github.com/zsrv/rs-server-225/io"|io "github.com/zsrv/goscape/pkg/io/jagfile"|g' npctype.go
```

- [ ] **Step 2: Build and fix any errors**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go build ./pkg/objtype/... 2>&1 | head -20
```

Common adjustments:
- If `entity.MoveRestrict` / `entity.NPCMode` referenced: replace with `int`. Trim related code paths if needed.
- If `varplayertype` / unvendored siblings referenced: trim those branches.
- If `LoadNPCTypes` calls a sibling that needs `*ParamTypeConfigs`, leave the parameter — `LoadNPCTypes(dir string, ptc *ParamTypeConfigs) (*NPCTypeConfigs, error)` or `LoadNPCTypes(dir string) (*NPCTypeConfigs, error)` — match whichever the vendored signature is. If the loader signature is `LoadNPCTypes(dir string) (*NPCTypeConfigs, error)` (no params), even better.

- [ ] **Step 3: Write smoke test**

Create `pkg/objtype/npctype_test.go`:

```go
package objtype

import (
	"path/filepath"
	"testing"
)

func TestLoadNPCTypesFromPack(t *testing.T) {
	cacheDir := filepath.Join("..", "..", "data", "pack")
	configs, err := LoadNPCTypes(cacheDir)
	if err != nil {
		t.Skipf("no cache data: %v", err)
	}
	if len(configs.Configs) == 0 {
		t.Fatal("expected at least one NpcType, got 0")
	}
}
```

> If the vendored `LoadNPCTypes` signature requires `*ParamTypeConfigs`, change the test to load params first:
>
> ```go
> params, err := LoadParams(cacheDir)
> if err != nil { t.Skipf("...") }
> configs, err := LoadNPCTypes(cacheDir, params)
> ```

- [ ] **Step 4: Run tests**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./pkg/objtype/... -v 2>&1 | tail -10
```

Expected: pass or skip (skip if no cache data).

- [ ] **Step 5: Commit**

```bash
cd /home/owner/Code/github.com/zsrv/goscape
git add pkg/objtype/
git commit --no-gpg-sign -m "$(cat <<'EOF'
feat(objtype): vendor NpcType + LoadNPCTypes from rs-server-225

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: Implement `loadNPCs` + `NpcSpawns` accessor

**Files:**
- Modify: `pkg/gamemap/gamemap.go`
- Modify: `pkg/gamemap/load.go`
- Modify: `pkg/gamemap/gamemap_test.go`

- [ ] **Step 1: Add `NpcSpawn` type + field**

In `pkg/gamemap/gamemap.go`, after the existing types, add:

```go
// NpcSpawn records an NPC spawn position from a mapsquare's n-file.
type NpcSpawn struct {
	TypeID int
	X, Z, Level int
}
```

Add field to `GameMap`:

```go
npcSpawns []NpcSpawn
```

Initialise as `nil` in `New()` (zero value works — leave the existing initializer alone).

Add accessor at the end of `gamemap.go`:

```go
// NpcSpawns returns the list of NPC spawn records collected during Init.
func (gm *GameMap) NpcSpawns() []NpcSpawn { return gm.npcSpawns }
```

- [ ] **Step 2: Implement `loadNPCs`**

In `pkg/gamemap/load.go`, replace the existing stub:

```go
func (gm *GameMap) loadNPCs(data []byte, mapSquareX, mapSquareZ int) {
	p := packet.NewPacket(data)
	for p.Len() >= 3 {
		packed := int(p.G2())
		count := int(p.G1())
		// packed: top 2 bits level, then 6+6 local x/z.
		level := (packed >> 12) & 0x3
		localX := (packed >> 6) & 0x3F
		localZ := packed & 0x3F
		absX := mapSquareX*64 + localX
		absZ := mapSquareZ*64 + localZ
		for i := 0; i < count && p.Len() >= 2; i++ {
			typeID := int(p.G2())
			gm.npcSpawns = append(gm.npcSpawns, NpcSpawn{
				TypeID: typeID, X: absX, Z: absZ, Level: level,
			})
		}
	}
}
```

- [ ] **Step 3: Write test**

Append to `pkg/gamemap/gamemap_test.go`:

```go
func TestLoadNpcsParsesSpawns(t *testing.T) {
	tmp := t.TempDir()
	mapsDir := filepath.Join(tmp, "maps")
	if err := os.MkdirAll(mapsDir, 0755); err != nil {
		t.Fatal(err)
	}
	// Create a minimal m file (loadGround needs at least one byte to not be empty).
	if err := os.WriteFile(filepath.Join(mapsDir, "m50_50"), []byte{0}, 0644); err != nil {
		t.Fatal(err)
	}

	// Build n50_50: one record at local (10, 20) level 0 with 2 spawns of types 100 and 200.
	// packed = (0<<12) | (10<<6) | 20 = 660 = 0x0294
	// Bytes: G2(packed)=0x02 0x94, G1(count)=0x02, G2(100)=0x00 0x64, G2(200)=0x00 0xC8
	nData := []byte{0x02, 0x94, 0x02, 0x00, 0x64, 0x00, 0xC8}
	if err := os.WriteFile(filepath.Join(mapsDir, "n50_50"), nData, 0644); err != nil {
		t.Fatal(err)
	}

	gm := New(discardLogger())
	if err := gm.Init(filepath.Dir(mapsDir)); err != nil {
		t.Fatal(err)
	}

	spawns := gm.NpcSpawns()
	if len(spawns) != 2 {
		t.Fatalf("spawns: got %d, want 2 (got %+v)", len(spawns), spawns)
	}
	wantX, wantZ := 50*64+10, 50*64+20
	for i, want := range []int{100, 200} {
		if spawns[i].TypeID != want {
			t.Errorf("spawn[%d].TypeID: got %d, want %d", i, spawns[i].TypeID, want)
		}
		if spawns[i].X != wantX || spawns[i].Z != wantZ || spawns[i].Level != 0 {
			t.Errorf("spawn[%d] coords: got (%d,%d,%d), want (%d,%d,0)", i, spawns[i].X, spawns[i].Z, spawns[i].Level, wantX, wantZ)
		}
	}
}
```

> **NOTE on `Init` path:** sub-spec 3c was developed after a config change made `gamemap.Init` look at `cacheDir/client/maps/`. The test passes `filepath.Dir(mapsDir)` as `cacheDir` so `Init` then appends `client/maps` — but that doesn't match `tmp/maps`. **Fix the test to put the synthetic files where `Init` expects them.** Inspect `pkg/gamemap/gamemap.go`'s `Init` to confirm the current path layout (likely `filepath.Join(cacheDir, "client", "maps")` based on recent edits). Adjust:
>
> ```go
> mapsDir := filepath.Join(tmp, "client", "maps")  // matches Init's lookup
> // ... mkdir, WriteFile to mapsDir ...
> if err := gm.Init(tmp); err != nil { ... }
> ```

- [ ] **Step 4: Run**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./pkg/gamemap/... -v 2>&1 | tail -20
```

- [ ] **Step 5: Commit**

```bash
git add pkg/gamemap/
git commit --no-gpg-sign -m "$(cat <<'EOF'
feat(gamemap): implement loadNPCs + expose NpcSpawns accessor

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: Extend `pkg/grid` with NPC index

**Files:**
- Modify: `pkg/grid/grid.go`
- Modify: `pkg/grid/grid_test.go`

- [ ] **Step 1: Add fields + methods**

In `pkg/grid/grid.go`, rename existing `zones` field to `playerZones` (or keep `zones` and add `npcZones` if you prefer minimal disruption):

```go
type Grid struct {
	playerZones map[uint32][]int
	npcZones    map[uint32][]int
}

func New() *Grid {
	return &Grid{
		playerZones: map[uint32][]int{},
		npcZones:    map[uint32][]int{},
	}
}
```

Update existing `Add`, `Remove`, `NearbyPlayers` to use `playerZones`. Add NPC equivalents:

```go
func (g *Grid) AddNpc(nid, x, z, level int) {
	key := packZone(x, z, level)
	g.npcZones[key] = append(g.npcZones[key], nid)
}

func (g *Grid) RemoveNpc(nid, x, z, level int) {
	key := packZone(x, z, level)
	slots := g.npcZones[key]
	for i, s := range slots {
		if s == nid {
			g.npcZones[key] = append(slots[:i], slots[i+1:]...)
			if len(g.npcZones[key]) == 0 {
				delete(g.npcZones, key)
			}
			return
		}
	}
}

func (g *Grid) NearbyNpcs(x, z, level, zoneRadius int) []int {
	zoneX := x >> 3
	zoneZ := z >> 3
	out := []int{}
	for dx := -zoneRadius; dx <= zoneRadius; dx++ {
		for dz := -zoneRadius; dz <= zoneRadius; dz++ {
			zx := zoneX + dx
			zz := zoneZ + dz
			if zx < 0 || zz < 0 {
				continue
			}
			key := packZone(zx<<3, zz<<3, level)
			out = append(out, g.npcZones[key]...)
		}
	}
	return out
}
```

- [ ] **Step 2: Tests**

Append to `pkg/grid/grid_test.go`:

```go
func TestAddNpcAndNearby(t *testing.T) {
	g := New()
	g.AddNpc(7, 3094, 3106, 0)

	near := g.NearbyNpcs(3094, 3106, 0, 1)
	if len(near) != 1 || near[0] != 7 {
		t.Errorf("NearbyNpcs: got %v, want [7]", near)
	}
}

func TestRemoveNpc(t *testing.T) {
	g := New()
	g.AddNpc(7, 3094, 3106, 0)
	g.RemoveNpc(7, 3094, 3106, 0)
	if got := g.NearbyNpcs(3094, 3106, 0, 1); len(got) != 0 {
		t.Errorf("after remove: got %v, want empty", got)
	}
}

func TestPlayerAndNpcSeparateIndexes(t *testing.T) {
	g := New()
	g.Add(5, 3094, 3106, 0)        // player
	g.AddNpc(7, 3094, 3106, 0)     // npc at same coord

	if p := g.NearbyPlayers(3094, 3106, 0, 1); len(p) != 1 || p[0] != 5 {
		t.Errorf("players: got %v, want [5]", p)
	}
	if n := g.NearbyNpcs(3094, 3106, 0, 1); len(n) != 1 || n[0] != 7 {
		t.Errorf("npcs: got %v, want [7]", n)
	}
}
```

- [ ] **Step 3: Run + commit**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./pkg/grid/... -v 2>&1 | tail -10
git add pkg/grid/
git commit --no-gpg-sign -m "$(cat <<'EOF'
feat(grid): extend with NPC index alongside player index

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: Extend `pkg/buildarea` with `Npcs` set

**Files:**
- Modify: `pkg/buildarea/buildarea.go`
- Modify: `pkg/buildarea/buildarea_test.go`

- [ ] **Step 1: Add field + initialise**

Add to `BuildArea` struct:

```go
Npcs map[int]struct{}
```

In `New()`, add to the struct initialiser:

```go
Npcs: map[int]struct{}{},
```

- [ ] **Step 2: Test**

Append to `buildarea_test.go`:

```go
func TestNpcsSetAddRemove(t *testing.T) {
	ba := New()
	if _, ok := ba.Npcs[7]; ok {
		t.Error("new BuildArea should have empty Npcs")
	}
	ba.Npcs[7] = struct{}{}
	if _, ok := ba.Npcs[7]; !ok {
		t.Error("add should succeed")
	}
	delete(ba.Npcs, 7)
	if _, ok := ba.Npcs[7]; ok {
		t.Error("remove should succeed")
	}
}
```

- [ ] **Step 3: Run + commit**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./pkg/buildarea/... -v
git add pkg/buildarea/
git commit --no-gpg-sign -m "$(cat <<'EOF'
feat(buildarea): add Npcs tracked-set field

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: Add `OpNpcInfo` opcode

**Files:**
- Modify: `pkg/io/protocol/game/server/prot.go`
- Modify: `pkg/io/protocol/game/server/prot_test.go`

- [ ] **Step 1: Add the opcode**

Append inside the existing `var ( ... )` block in `prot.go`:

```go
OpNpcInfo = Op{Opcode: 1, PayloadSize: -2}
```

- [ ] **Step 2: Add test**

Append to `prot_test.go`:

```go
func TestSubSpec3COpcodes(t *testing.T) {
	if OpNpcInfo.Opcode != 1 {
		t.Errorf("OpNpcInfo.Opcode = %d, want 1", OpNpcInfo.Opcode)
	}
	if OpNpcInfo.PayloadSize != -2 {
		t.Errorf("OpNpcInfo.PayloadSize = %d, want -2", OpNpcInfo.PayloadSize)
	}
}
```

- [ ] **Step 3: Run + commit**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./pkg/io/protocol/game/server/... -v
git add pkg/io/protocol/game/server/
git commit --no-gpg-sign -m "$(cat <<'EOF'
feat(prot): add OpNpcInfo server opcode

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: `pkg/rsbuf/npc_source.go` + mask payload encoders

**Files:**
- Create: `pkg/rsbuf/npc_source.go`
- Create: `pkg/rsbuf/npc_mask_payload.go`
- Create: `pkg/rsbuf/npc_mask_payload_test.go`

- [ ] **Step 1: Create `npc_source.go`**

```go
package rsbuf

// NPC mask bit constants — matches rsbuf branch 225 NpcInfoProt.
const (
	NpcMaskAnim       = 0x2
	NpcMaskFaceEntity = 0x4
	NpcMaskSay        = 0x8
	NpcMaskDamage     = 0x10
	NpcMaskChangeType = 0x20
	NpcMaskSpotAnim   = 0x40
	NpcMaskFaceCoord  = 0x80
)

// NpcSource exposes an NPC's state to the encoder.
type NpcSource interface {
	Nid() int
	TypeID() int
	Coords() (x, z, level int)
	Active() bool

	Masks() int
	EntityMask() int

	AnimID() int
	AnimDelay() int
	FaceEntity() int
	SayText() []byte
	DamageAmt() int
	DamageType() int
	CurHP() int
	BaseHP() int
	ChangeTypeID() int
	SpotAnimID() int
	SpotAnimHeight() int
	SpotAnimDelay() int
	FaceSquareX() int
	FaceSquareZ() int

	WalkDir() int
	RunDir() int
	Tele() bool
	LastTickX() int
	LastTickZ() int
	LastLevel() int
}
```

- [ ] **Step 2: Write failing tests**

Create `pkg/rsbuf/npc_mask_payload_test.go`:

```go
package rsbuf

import (
	"testing"

	"github.com/zsrv/goscape/pkg/io/packet"
)

// fakeNpcSource is the NPC analogue of fakeSource for byte-level tests.
type fakeNpcSource struct {
	nid, typeID                              int
	x, z, level                              int
	active                                   bool
	masks, entityMask                        int
	animID, animDelay                        int
	faceEntity                               int
	sayText                                  []byte
	damageAmt, damageType, curHP, baseHP     int
	changeTypeID                             int
	spotanimID, spotanimHeight, spotanimDelay int
	faceSquareX, faceSquareZ                 int
}

func (f *fakeNpcSource) Nid() int                { return f.nid }
func (f *fakeNpcSource) TypeID() int             { return f.typeID }
func (f *fakeNpcSource) Coords() (int, int, int) { return f.x, f.z, f.level }
func (f *fakeNpcSource) Active() bool            { return f.active }
func (f *fakeNpcSource) Masks() int              { return f.masks }
func (f *fakeNpcSource) EntityMask() int         { return f.entityMask }
func (f *fakeNpcSource) AnimID() int             { return f.animID }
func (f *fakeNpcSource) AnimDelay() int          { return f.animDelay }
func (f *fakeNpcSource) FaceEntity() int         { return f.faceEntity }
func (f *fakeNpcSource) SayText() []byte         { return f.sayText }
func (f *fakeNpcSource) DamageAmt() int          { return f.damageAmt }
func (f *fakeNpcSource) DamageType() int         { return f.damageType }
func (f *fakeNpcSource) CurHP() int              { return f.curHP }
func (f *fakeNpcSource) BaseHP() int             { return f.baseHP }
func (f *fakeNpcSource) ChangeTypeID() int       { return f.changeTypeID }
func (f *fakeNpcSource) SpotAnimID() int         { return f.spotanimID }
func (f *fakeNpcSource) SpotAnimHeight() int     { return f.spotanimHeight }
func (f *fakeNpcSource) SpotAnimDelay() int      { return f.spotanimDelay }
func (f *fakeNpcSource) FaceSquareX() int        { return f.faceSquareX }
func (f *fakeNpcSource) FaceSquareZ() int        { return f.faceSquareZ }
func (f *fakeNpcSource) WalkDir() int            { return -1 }
func (f *fakeNpcSource) RunDir() int             { return -1 }
func (f *fakeNpcSource) Tele() bool              { return false }
func (f *fakeNpcSource) LastTickX() int          { return -1 }
func (f *fakeNpcSource) LastTickZ() int          { return -1 }
func (f *fakeNpcSource) LastLevel() int          { return -1 }

func TestNpcAnimPayload(t *testing.T) {
	n := &fakeNpcSource{masks: NpcMaskAnim, animID: 100, animDelay: 5}
	buf := packet.NewPacket(nil)
	writeNpcMaskPayloads(buf, n, NpcMaskAnim)
	want := []byte{0x00, 0x64, 0x05}
	got := buf.Data
	if len(got) != 3 || got[0] != want[0] || got[1] != want[1] || got[2] != want[2] {
		t.Errorf("got %v, want %v", got, want)
	}
}

func TestNpcDamagePayload(t *testing.T) {
	n := &fakeNpcSource{masks: NpcMaskDamage, damageAmt: 10, damageType: 1, curHP: 40, baseHP: 50}
	buf := packet.NewPacket(nil)
	writeNpcMaskPayloads(buf, n, NpcMaskDamage)
	want := []byte{10, 1, 40, 50}
	for i := range want {
		if buf.Data[i] != want[i] {
			t.Errorf("byte[%d]: got %d, want %d", i, buf.Data[i], want[i])
		}
	}
}

func TestNpcSpotAnimPayload(t *testing.T) {
	n := &fakeNpcSource{masks: NpcMaskSpotAnim, spotanimID: 0x1234, spotanimHeight: 50, spotanimDelay: 10}
	buf := packet.NewPacket(nil)
	writeNpcMaskPayloads(buf, n, NpcMaskSpotAnim)
	// p2(0x1234) = [0x12, 0x34]; p4((50<<16)|10 = 0x0032000A) = [0x00, 0x32, 0x00, 0x0A]
	want := []byte{0x12, 0x34, 0x00, 0x32, 0x00, 0x0A}
	for i := range want {
		if buf.Data[i] != want[i] {
			t.Errorf("byte[%d]: got %#x, want %#x", i, buf.Data[i], want[i])
		}
	}
}

func TestNpcFaceCoordPayload(t *testing.T) {
	n := &fakeNpcSource{masks: NpcMaskFaceCoord, faceSquareX: 0x0182, faceSquareZ: 0x0184}
	buf := packet.NewPacket(nil)
	writeNpcMaskPayloads(buf, n, NpcMaskFaceCoord)
	want := []byte{0x01, 0x82, 0x01, 0x84}
	for i := range want {
		if buf.Data[i] != want[i] {
			t.Errorf("byte[%d]: got %#x, want %#x", i, buf.Data[i], want[i])
		}
	}
}

func TestNpcMaskHeader1Byte(t *testing.T) {
	buf := packet.NewPacket(nil)
	writeNpcMaskHeader(buf, NpcMaskAnim|NpcMaskFaceCoord) // 2|128 = 130
	if buf.Data[0] != 130 {
		t.Errorf("header: got %d, want 130", buf.Data[0])
	}
}
```

- [ ] **Step 3: Verify failure**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./pkg/rsbuf/... -run "TestNpc" 2>&1 | head -5
```

- [ ] **Step 4: Create `npc_mask_payload.go`**

```go
package rsbuf

import "github.com/zsrv/goscape/pkg/io/packet"

// writeNpcMaskHeader writes the NPC mask header. Always 1 byte (NPC mask values
// fit in 0x80; no MaskBig equivalent for NPCs).
func writeNpcMaskHeader(buf *packet.Packet, masks int) {
	buf.P1(uint8(masks))
}

// writeNpcMaskPayloads writes payloads in fixed order:
// ANIM → FACE_ENTITY → SAY → DAMAGE → CHANGE_TYPE → SPOT_ANIM → FACE_COORD
func writeNpcMaskPayloads(buf *packet.Packet, n NpcSource, forceMasks int) {
	if forceMasks&NpcMaskAnim != 0 {
		buf.P2(uint16(n.AnimID()))
		buf.P1(uint8(n.AnimDelay()))
	}
	if forceMasks&NpcMaskFaceEntity != 0 {
		buf.P2(uint16(n.FaceEntity()))
	}
	if forceMasks&NpcMaskSay != 0 {
		for _, b := range n.SayText() {
			buf.P1(b)
		}
		buf.P1(10)
	}
	if forceMasks&NpcMaskDamage != 0 {
		buf.P1(uint8(n.DamageAmt()))
		buf.P1(uint8(n.DamageType()))
		buf.P1(uint8(n.CurHP()))
		buf.P1(uint8(n.BaseHP()))
	}
	if forceMasks&NpcMaskChangeType != 0 {
		buf.P2(uint16(n.ChangeTypeID()))
	}
	if forceMasks&NpcMaskSpotAnim != 0 {
		buf.P2(uint16(n.SpotAnimID()))
		buf.P4(uint32(n.SpotAnimHeight())<<16 | uint32(n.SpotAnimDelay()))
	}
	if forceMasks&NpcMaskFaceCoord != 0 {
		buf.P2(uint16(n.FaceSquareX()))
		buf.P2(uint16(n.FaceSquareZ()))
	}
}
```

- [ ] **Step 5: Run tests + commit**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./pkg/rsbuf/... -v 2>&1 | tail -15
git add pkg/rsbuf/npc_source.go pkg/rsbuf/npc_mask_payload.go pkg/rsbuf/npc_mask_payload_test.go
git commit --no-gpg-sign -m "$(cat <<'EOF'
feat(rsbuf): add NpcSource interface and 7 NPC mask payload encoders

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: Renderer NPC compute pass

**Files:**
- Modify: `pkg/rsbuf/renderer.go`
- Modify: `pkg/rsbuf/renderer_test.go`

- [ ] **Step 1: Extend renderer**

In `pkg/rsbuf/renderer.go`, add fields to `Renderer`:

```go
npcHighDef [8192][]byte
npcLowDef  [8192][]byte
```

(No need to initialise — array zero-value is fine.)

Add methods:

```go
// ComputeNpcs builds per-nid NPC payload caches for the current tick.
func (r *Renderer) ComputeNpcs(npcs []NpcSource) {
	for _, n := range npcs {
		nid := n.Nid()
		if nid < 1 || nid >= len(r.npcHighDef) {
			continue
		}
		masks := n.Masks()
		if masks == 0 && n.EntityMask() == 0 {
			r.npcHighDef[nid] = nil
		} else {
			buf := packet.NewPacket(nil)
			writeNpcMaskHeader(buf, masks)
			writeNpcMaskPayloads(buf, n, masks)
			r.npcHighDef[nid] = append([]byte(nil), buf.Data...)
		}
		// Low-def: force FACE_COORD baseline so new observers know where to look.
		lowMasks := masks | NpcMaskFaceCoord
		if lowMasks != 0 {
			buf := packet.NewPacket(nil)
			writeNpcMaskHeader(buf, lowMasks)
			writeNpcMaskPayloads(buf, n, lowMasks)
			r.npcLowDef[nid] = append([]byte(nil), buf.Data...)
		} else {
			r.npcLowDef[nid] = nil
		}
	}
}

func (r *Renderer) NpcHighDefOf(nid int) []byte {
	if nid < 1 || nid >= len(r.npcHighDef) {
		return nil
	}
	return r.npcHighDef[nid]
}

func (r *Renderer) NpcLowDefOf(nid int) []byte {
	if nid < 1 || nid >= len(r.npcLowDef) {
		return nil
	}
	return r.npcLowDef[nid]
}
```

- [ ] **Step 2: Tests**

Append to `pkg/rsbuf/renderer_test.go`:

```go
func TestComputeNpcsHighDef(t *testing.T) {
	r := NewRenderer()
	n := &fakeNpcSource{nid: 5, masks: NpcMaskAnim, animID: 100, animDelay: 2, active: true}
	r.ComputeNpcs([]NpcSource{n})
	got := r.NpcHighDefOf(5)
	// header NpcMaskAnim=2, then ANIM payload p2(100) p1(2) = [0x00, 0x64, 0x02]
	want := []byte{2, 0, 100, 2}
	if len(got) != len(want) {
		t.Fatalf("len: got %d, want %d (%v)", len(got), len(want), got)
	}
	for i := range want {
		if got[i] != want[i] {
			t.Errorf("byte[%d]: got %#x, want %#x", i, got[i], want[i])
		}
	}
}

func TestComputeNpcsLowDefForcesFaceCoord(t *testing.T) {
	r := NewRenderer()
	n := &fakeNpcSource{nid: 5, masks: 0, faceSquareX: 100, faceSquareZ: 200, active: true}
	r.ComputeNpcs([]NpcSource{n})
	low := r.NpcLowDefOf(5)
	if len(low) == 0 {
		t.Fatal("low-def should include FACE_COORD")
	}
	if low[0]&NpcMaskFaceCoord == 0 {
		t.Errorf("header byte: FACE_COORD bit should be set; got %d", low[0])
	}
}

func TestComputeNpcsHighDefSkipsZero(t *testing.T) {
	r := NewRenderer()
	n := &fakeNpcSource{nid: 5, masks: 0, entityMask: 0, active: true}
	r.ComputeNpcs([]NpcSource{n})
	if r.NpcHighDefOf(5) != nil {
		t.Error("HighDef should be nil for zero-mask")
	}
}
```

- [ ] **Step 3: Run + commit**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./pkg/rsbuf/... -v 2>&1 | tail -15
git add pkg/rsbuf/
git commit --no-gpg-sign -m "$(cat <<'EOF'
feat(rsbuf): extend Renderer with NPC high-def + low-def caches

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: `EncodeNpc` 3-phase encoder

**Files:**
- Create: `pkg/rsbuf/npcinfo.go`
- Create: `pkg/rsbuf/npcinfo_test.go`

- [ ] **Step 1: Failing test**

Create `pkg/rsbuf/npcinfo_test.go`:

```go
package rsbuf

import (
	"testing"

	"github.com/zsrv/goscape/pkg/buildarea"
	"github.com/zsrv/goscape/pkg/grid"
)

func TestEncodeNpcEmpty(t *testing.T) {
	self := &fakeSource{slot: 1, x: 3094, z: 3106, level: 0, originX: 3094, originZ: 3106}
	ba := buildarea.New()
	g := grid.New()
	r := NewRenderer()
	r.ComputeNpcs(nil)

	payload := EncodeNpc(self, nil, ba, g, r)
	if len(payload) == 0 {
		t.Fatal("EncodeNpc should produce at least the pbit(8, 0) byte")
	}
	// First 8 bits = pbit(8, 0) = 0x00.
	if payload[0] != 0 {
		t.Errorf("first byte: got %#x, want 0x00 (count=0)", payload[0])
	}
}

func TestEncodeNpcAddsNew(t *testing.T) {
	self := &fakeSource{slot: 1, x: 3094, z: 3106, level: 0, originX: 3094, originZ: 3106}
	npc := &fakeNpcSource{nid: 7, typeID: 100, x: 3095, z: 3106, level: 0, active: true}

	ba := buildarea.New()
	g := grid.New()
	g.AddNpc(npc.nid, npc.x, npc.z, npc.level)
	r := NewRenderer()
	r.ComputeNpcs([]NpcSource{npc})

	payload := EncodeNpc(self, []NpcSource{npc}, ba, g, r)
	if len(payload) == 0 {
		t.Fatal("EncodeNpc should produce non-empty payload")
	}
	if _, ok := ba.Npcs[7]; !ok {
		t.Errorf("ba.Npcs should contain 7 after EncodeNpc; got %v", ba.Npcs)
	}
}
```

- [ ] **Step 2: Verify failure**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./pkg/rsbuf/... -run TestEncodeNpc 2>&1 | head -5
```

- [ ] **Step 3: Create `pkg/rsbuf/npcinfo.go`**

```go
package rsbuf

import (
	"github.com/zsrv/goscape/pkg/buildarea"
	"github.com/zsrv/goscape/pkg/grid"
	"github.com/zsrv/goscape/pkg/io/packet"
)

const (
	NpcViewDistanceZones = 15
	PreferredNpcs        = 255
	NpcTerminator        = 8191
)

// EncodeNpc produces the NpcInfo payload for `self` (no opcode/length prefix).
func EncodeNpc(self PlayerSource, all []NpcSource, ba *buildarea.BuildArea, g *grid.Grid, r *Renderer) []byte {
	byNid := make(map[int]NpcSource, len(all))
	for _, n := range all {
		byNid[n.Nid()] = n
	}

	main := packet.NewPacket(nil)
	updates := packet.NewPacket(nil)

	main.AccessBits()

	// Phase 1: tracked-npcs delta loop.
	main.PBit(8, len(ba.Npcs))
	slots := make([]int, 0, len(ba.Npcs))
	for nid := range ba.Npcs {
		slots = append(slots, nid)
	}
	selfX, selfZ, selfLevel := self.Coords()
	for _, nid := range slots {
		n, ok := byNid[nid]
		if !ok || !n.Active() {
			main.PBit(1, 1)
			main.PBit(2, 3) // remove
			delete(ba.Npcs, nid)
			continue
		}
		nx, nz, nl := n.Coords()
		if nl != selfLevel || zoneDist(selfX, selfZ, nx, nz) > NpcViewDistanceZones {
			main.PBit(1, 1)
			main.PBit(2, 3)
			delete(ba.Npcs, nid)
			continue
		}
		extend := 0
		payload := r.NpcHighDefOf(nid)
		if len(payload) > 0 && fits(main, updates, len(payload)) {
			extend = 1
		}
		switch {
		case n.RunDir() != -1:
			main.PBit(1, 1)
			main.PBit(2, 2)
			main.PBit(3, n.WalkDir())
			main.PBit(3, n.RunDir())
			main.PBit(1, extend)
		case n.WalkDir() != -1:
			main.PBit(1, 1)
			main.PBit(2, 1)
			main.PBit(3, n.WalkDir())
			main.PBit(1, extend)
		case n.Masks() != 0:
			main.PBit(1, 1)
			main.PBit(2, 0)
			extend = 1
		default:
			main.PBit(1, 0)
		}
		if extend == 1 && len(payload) > 0 {
			for _, b := range payload {
				updates.P1(b)
			}
		}
	}

	// Phase 2: new-npcs loop.
	candidates := g.NearbyNpcs(selfX, selfZ, selfLevel, NpcViewDistanceZones)
	for _, nid := range candidates {
		if _, already := ba.Npcs[nid]; already {
			continue
		}
		if len(ba.Npcs) >= PreferredNpcs {
			break
		}
		n, ok := byNid[nid]
		if !ok || !n.Active() {
			continue
		}
		payload := r.NpcLowDefOf(nid)
		if !fits(main, updates, len(payload)+5) { // 5 bytes ≈ 35-bit add header
			main.PBit(13, NpcTerminator)
			break
		}
		nx, nz, _ := n.Coords()
		dx := clamp(nx-selfX, -15, 15)
		dz := clamp(nz-selfZ, -15, 15)

		main.PBit(13, nid)
		main.PBit(11, n.TypeID())
		main.PBit(5, dx&0x1f)
		main.PBit(5, dz&0x1f)
		main.PBit(1, boolToInt(len(payload) > 0))

		ba.Npcs[nid] = struct{}{}
		if len(payload) > 0 {
			for _, b := range payload {
				updates.P1(b)
			}
		}
	}

	main.AccessBytes()
	for _, b := range updates.Data {
		main.P1(b)
	}
	return main.Data
}
```

This file reuses `fits`, `clamp`, `zoneDist`, `boolToInt` already defined in `playerinfo.go` (same package).

- [ ] **Step 4: Run + commit**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./pkg/rsbuf/... -v 2>&1 | tail -15
git add pkg/rsbuf/npcinfo.go pkg/rsbuf/npcinfo_test.go
git commit --no-gpg-sign -m "$(cat <<'EOF'
feat(rsbuf): implement 3-phase EncodeNpc with 35-bit add header

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: `Npc` struct + `NewNpc` + lifecycle constants

**Files:**
- Create: `modules/world/npc.go`

- [ ] **Step 1: Create the file**

```go
package world

import (
	"github.com/zsrv/goscape/pkg/objtype"
)

// NPC lifecycle constants.
const (
	NpcLifecycleForever = 0
	NpcLifecycleRespawn = 1
	NpcLifecycleDespawn = 2
)

// NPC AI mode constants.
const (
	NpcModeNone    = -1
	NpcModeWander  = 0
	NpcModePatrol  = 1
)

// Npc is a non-player game entity.
type Npc struct {
	nid    int
	typeId int
	typ    *objtype.NpcType

	// === lifecycle ===
	lifecycle     int
	lifecycleTick int
	respawnRate   int
	dead          bool
	startX, startZ, startLevel int

	// === coords ===
	x, z, level                     int
	lastTickX, lastTickZ, lastLevel int
	originX, originZ                int

	// === movement (PathingEntity-equivalent) ===
	moveSpeed     MoveSpeed
	moveRestrict  MoveRestrict
	moveStrategy  MoveStrategy
	walkDir, runDir int
	waypointIndex int
	waypoints     [25]int
	tele          bool
	stepsTaken    int

	// === AI ===
	targetOp        int
	wanderCounter   int
	nextPatrolTick  int
	nextPatrolPoint int
	delayedPatrol   bool

	// === interaction ===
	target     entity
	faceEntity int

	// === masks ===
	masks      int
	entitymask int

	// === mask state ===
	animID, animDelay int
	sayText []byte
	damageAmt, damageType int
	curHP, baseHP         int
	spotanimID, spotanimHeight, spotanimDelay int
	faceSquareX, faceSquareZ int
	changeTypeID int
}

// NewNpc constructs an Npc at the given coordinate, anchoring its spawn point
// for wander/respawn behaviour.
func NewNpc(nid, typeId, x, z, level int, typ *objtype.NpcType) *Npc {
	mode := NpcModeNone
	if len(typ.PatrolCoord) > 0 {
		mode = NpcModePatrol
	} else if typ.WanderRange > 0 {
		mode = NpcModeWander
	}

	return &Npc{
		nid:             nid,
		typeId:          typeId,
		typ:             typ,
		lifecycle:       NpcLifecycleRespawn,
		respawnRate:     typ.RespawnRate,
		startX:          x,
		startZ:          z,
		startLevel:      level,
		x:               x,
		z:               z,
		level:           level,
		originX:         x,
		originZ:         z,
		lastTickX:       -1,
		lastTickZ:       -1,
		lastLevel:       -1,
		moveSpeed:       MoveSpeedInstant,
		moveStrategy:    MoveStrategyNaive,
		moveRestrict:    MoveRestrict(typ.MoveRestrict),
		walkDir:         -1,
		runDir:          -1,
		waypointIndex:   -1,
		targetOp:        mode,
		nextPatrolPoint: 0,
		faceEntity:      -1,
		animID:          -1,
		animDelay:       -1,
		damageAmt:       -1,
		damageType:      -1,
		curHP:           -1,
		baseHP:          -1,
		spotanimID:      -1,
		spotanimHeight:  -1,
		spotanimDelay:   -1,
		faceSquareX:     -1,
		faceSquareZ:     -1,
		changeTypeID:    -1,
	}
}

// Slot returns the NPC's nid (so it can satisfy the entity interface).
func (n *Npc) Slot() int { return n.nid }
```

- [ ] **Step 2: Build**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go build ./modules/world/...
```

If `MoveRestrict(typ.MoveRestrict)` causes a type mismatch (e.g., `typ.MoveRestrict` is already a typed enum), drop the cast.

- [ ] **Step 3: Commit**

```bash
git add modules/world/npc.go
git commit --no-gpg-sign -m "$(cat <<'EOF'
feat(world): add Npc struct + NewNpc with lifecycle and AI defaults

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: NPC mask setters + ResetMasks

**Files:**
- Create: `modules/world/npc_masks.go`
- Create: `modules/world/npc_test.go`

- [ ] **Step 1: Failing test**

Create `modules/world/npc_test.go`:

```go
package world

import (
	"testing"

	"github.com/zsrv/goscape/pkg/objtype"
	"github.com/zsrv/goscape/pkg/rsbuf"
)

func newTestNpc(nid int) *Npc {
	typ := &objtype.NpcType{
		ConfigType:  objtype.ConfigType{ID: 0, DebugName: "test"},
		WanderRange: 0,
		RespawnRate: 50,
	}
	return NewNpc(nid, 0, 3094, 3106, 0, typ)
}

func TestNpcAnimateSetsMask(t *testing.T) {
	n := newTestNpc(1)
	n.Animate(123, 5)
	if n.masks&rsbuf.NpcMaskAnim == 0 {
		t.Error("NpcMaskAnim should be set")
	}
	if n.animID != 123 || n.animDelay != 5 {
		t.Errorf("animID/Delay: got (%d,%d), want (123,5)", n.animID, n.animDelay)
	}
}

func TestNpcSaySetsMask(t *testing.T) {
	n := newTestNpc(1)
	n.Say([]byte("hi"))
	if n.masks&rsbuf.NpcMaskSay == 0 {
		t.Error("NpcMaskSay should be set")
	}
	if string(n.sayText) != "hi" {
		t.Errorf("sayText: got %q, want %q", n.sayText, "hi")
	}
}

func TestNpcChangeTypeSetsMask(t *testing.T) {
	n := newTestNpc(1)
	n.ChangeType(42)
	if n.masks&rsbuf.NpcMaskChangeType == 0 {
		t.Error("NpcMaskChangeType should be set")
	}
	if n.changeTypeID != 42 {
		t.Errorf("changeTypeID: got %d, want 42", n.changeTypeID)
	}
}

func TestNpcFaceCoord(t *testing.T) {
	n := newTestNpc(1)
	n.FaceCoord(100, 200)
	if n.faceSquareX != 201 || n.faceSquareZ != 401 {
		t.Errorf("faceSquareX/Z: got (%d,%d), want (201,401)", n.faceSquareX, n.faceSquareZ)
	}
}

func TestNpcResetMasksClearsEphemerals(t *testing.T) {
	n := newTestNpc(1)
	n.Animate(123, 5)
	n.Say([]byte("hi"))
	n.ShowHit(10, 1, 40, 50)
	n.ResetMasks()

	if n.masks != 0 {
		t.Errorf("masks: got %d, want 0", n.masks)
	}
	if n.sayText != nil {
		t.Error("sayText should be nil after reset")
	}
	if n.damageAmt != -1 {
		t.Errorf("damageAmt: got %d, want -1", n.damageAmt)
	}
	// Persistent: animID retained.
	if n.animID != 123 {
		t.Errorf("animID should persist: got %d, want 123", n.animID)
	}
}
```

- [ ] **Step 2: Verify failure**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./modules/world/... -run TestNpc 2>&1 | head -10
```

- [ ] **Step 3: Create `modules/world/npc_masks.go`**

```go
package world

import "github.com/zsrv/goscape/pkg/rsbuf"

func (n *Npc) Animate(id, delay int) {
	n.animID = id
	n.animDelay = delay
	n.masks |= rsbuf.NpcMaskAnim
}

func (n *Npc) Say(msg []byte) {
	n.sayText = msg
	n.masks |= rsbuf.NpcMaskSay
}

func (n *Npc) ShowHit(amount, dmgType, cur, base int) {
	n.damageAmt = amount
	n.damageType = dmgType
	n.curHP = cur
	n.baseHP = base
	n.masks |= rsbuf.NpcMaskDamage
}

func (n *Npc) ChangeType(newType int) {
	n.changeTypeID = newType
	n.masks |= rsbuf.NpcMaskChangeType
}

func (n *Npc) SpotAnim(id, height, delay int) {
	n.spotanimID = id
	n.spotanimHeight = height
	n.spotanimDelay = delay
	n.masks |= rsbuf.NpcMaskSpotAnim
}

func (n *Npc) FaceCoord(x, z int) {
	n.faceSquareX = x*2 + 1
	n.faceSquareZ = z*2 + 1
	n.masks |= rsbuf.NpcMaskFaceCoord
}

func (n *Npc) SetFaceEntity(entityIndex int) {
	n.faceEntity = entityIndex
	n.masks |= rsbuf.NpcMaskFaceEntity
}

// ResetMasks clears mask bits + ephemeral state. Persistent fields (animID,
// faceEntity, faceSquareX/Z, changeTypeID) retained.
func (n *Npc) ResetMasks() {
	n.masks = 0
	n.sayText = nil
	n.damageAmt = -1
	n.damageType = -1
	n.curHP = -1
	n.baseHP = -1
	n.spotanimID = -1
	n.spotanimHeight = -1
	n.spotanimDelay = -1
}
```

- [ ] **Step 4: Run + commit**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./modules/world/... -run TestNpc -v 2>&1 | tail -15
git add modules/world/npc_masks.go modules/world/npc_test.go
git commit --no-gpg-sign -m "$(cat <<'EOF'
feat(world): add Npc mask setters and ResetMasks

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Task 11: NPC accessors satisfying `rsbuf.NpcSource`

**Files:**
- Create: `modules/world/npc_source.go`

- [ ] **Step 1: Create the file**

```go
package world

import "github.com/zsrv/goscape/pkg/rsbuf"

func (n *Npc) Nid() int                { return n.nid }
func (n *Npc) TypeID() int             { return n.typeId }
func (n *Npc) Coords() (x, z, level int) { return n.x, n.z, n.level }
func (n *Npc) Active() bool            { return !n.dead }
func (n *Npc) Masks() int              { return n.masks }
func (n *Npc) EntityMask() int         { return n.entitymask }
func (n *Npc) AnimID() int             { return n.animID }
func (n *Npc) AnimDelay() int          { return n.animDelay }
func (n *Npc) FaceEntity() int         { return n.faceEntity }
func (n *Npc) SayText() []byte         { return n.sayText }
func (n *Npc) DamageAmt() int          { return n.damageAmt }
func (n *Npc) DamageType() int         { return n.damageType }
func (n *Npc) CurHP() int              { return n.curHP }
func (n *Npc) BaseHP() int             { return n.baseHP }
func (n *Npc) ChangeTypeID() int       { return n.changeTypeID }
func (n *Npc) SpotAnimID() int         { return n.spotanimID }
func (n *Npc) SpotAnimHeight() int     { return n.spotanimHeight }
func (n *Npc) SpotAnimDelay() int      { return n.spotanimDelay }
func (n *Npc) FaceSquareX() int        { return n.faceSquareX }
func (n *Npc) FaceSquareZ() int        { return n.faceSquareZ }
func (n *Npc) WalkDir() int            { return n.walkDir }
func (n *Npc) RunDir() int             { return n.runDir }
func (n *Npc) Tele() bool              { return n.tele }
func (n *Npc) LastTickX() int          { return n.lastTickX }
func (n *Npc) LastTickZ() int          { return n.lastTickZ }
func (n *Npc) LastLevel() int          { return n.lastLevel }

// Compile-time check.
var _ rsbuf.NpcSource = (*Npc)(nil)
```

- [ ] **Step 2: Build**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go build ./modules/world/...
```

If "missing method X" — add it. If "method X already declared" — `Slot()` from `npc.go` may conflict; check and remove duplicates.

- [ ] **Step 3: Commit**

```bash
git add modules/world/npc_source.go
git commit --no-gpg-sign -m "$(cat <<'EOF'
feat(world): add Npc accessors satisfying rsbuf.NpcSource

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Task 12: NPC AI — `turn`, wander, patrol, Kill, waypoint helpers

**Files:**
- Create: `modules/world/npc_ai.go`
- Create: `modules/world/npc_ai_test.go`

- [ ] **Step 1: Failing tests**

Create `modules/world/npc_ai_test.go`:

```go
package world

import (
	"math/rand/v2"
	"testing"

	"github.com/zsrv/goscape/pkg/objtype"
	"github.com/zsrv/goscape/pkg/rsbuf"
)

func newWanderNpc(t *testing.T) *Npc {
	t.Helper()
	typ := &objtype.NpcType{
		ConfigType:  objtype.ConfigType{ID: 0, DebugName: "wanderer"},
		WanderRange: 5,
		RespawnRate: 50,
	}
	return NewNpc(1, 0, 3094, 3106, 0, typ)
}

func TestKillSetsDeadAndLifecycleTick(t *testing.T) {
	n := newWanderNpc(t)
	n.Kill()
	if !n.dead {
		t.Error("Kill should set dead=true")
	}
	if n.lifecycleTick != 50 {
		t.Errorf("lifecycleTick: got %d, want 50", n.lifecycleTick)
	}
}

func TestTeleportHomeAfterStuck(t *testing.T) {
	n := newWanderNpc(t)
	n.x, n.z = 3094+10, 3106+10 // move away from spawn
	n.wanderCounter = 501
	s := &Server{}
	n.turn(s)
	if n.x != n.startX || n.z != n.startZ {
		t.Errorf("teleport home: got (%d,%d), want (%d,%d)", n.x, n.z, n.startX, n.startZ)
	}
	if !n.tele {
		t.Error("tele flag should be set after teleport home")
	}
}

func TestRespawnAfterKill(t *testing.T) {
	n := newWanderNpc(t)
	n.x, n.z = 3094+5, 3106+5
	n.Kill()
	s := &Server{}
	// Tick down lifecycleTick to 1; expect respawn on next tick.
	for i := 0; i < n.respawnRate; i++ {
		n.turn(s)
	}
	// After respawnRate ticks, dead -> false, coords reset.
	if n.dead {
		t.Error("should have respawned by now")
	}
	if n.x != n.startX || n.z != n.startZ {
		t.Errorf("respawn coords: got (%d,%d), want (%d,%d)", n.x, n.z, n.startX, n.startZ)
	}
	if n.masks&rsbuf.NpcMaskChangeType == 0 {
		t.Error("respawn should raise NpcMaskChangeType (visual reset)")
	}
}

func TestWanderModeQueuesWaypointWithSeededRng(t *testing.T) {
	// Seed rand globally so the 1-in-8 roll is deterministic in this test.
	// math/rand/v2 doesn't have global seed, but rand.IntN uses the auto-seeded
	// global. We can't easily make this fully deterministic without injecting
	// a *rand.Rand. Instead, run wanderMode many times and assert the empirical
	// frequency is near 12.5%.
	n := newWanderNpc(t)
	s := &Server{}
	hits := 0
	for i := 0; i < 8000; i++ {
		n.waypointIndex = -1
		n.wanderMode(s)
		if n.waypointIndex >= 0 {
			hits++
		}
	}
	// Expect ~1000; allow ±25%.
	if hits < 750 || hits > 1250 {
		t.Errorf("wander hit rate: got %d/8000, want ~1000 (12.5%%)", hits)
	}
	_ = rand.IntN // keep import alive
}
```

> **Note on the deterministic-RNG concern:** math/rand/v2 doesn't expose easy global seeding. The plan uses an empirical-frequency assertion instead. If a future sub-spec needs strict determinism, inject `*rand.Rand` into `wanderMode` and seed in tests.

- [ ] **Step 2: Verify failure**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./modules/world/... -run "TestKillSets|TestTeleportHome|TestRespawnAfter|TestWanderMode" 2>&1 | head -10
```

- [ ] **Step 3: Create `modules/world/npc_ai.go`**

```go
package world

import (
	"math/rand/v2"

	"github.com/zsrv/goscape/pkg/coordgrid"
	"github.com/zsrv/goscape/pkg/rsbuf"
)

// turn runs once per tick from processNpcs.
func (n *Npc) turn(s *Server) {
	if n.dead {
		n.lifecycleTick--
		if n.lifecycleTick <= 0 && n.lifecycle == NpcLifecycleRespawn {
			n.dead = false
			n.x, n.z, n.level = n.startX, n.startZ, n.startLevel
			n.tele = true
			n.masks |= rsbuf.NpcMaskChangeType
		}
		return
	}
	if n.moveRestrict == MoveRestrictNoMove {
		return
	}

	n.lastTickX, n.lastTickZ, n.lastLevel = n.x, n.z, n.level
	n.tele = false

	if n.waypointIndex >= 0 {
		n.advanceWaypoint(s)
		n.wanderCounter = 0
	} else {
		n.wanderCounter++
		switch n.targetOp {
		case NpcModeWander:
			n.wanderMode(s)
		case NpcModePatrol:
			n.patrolMode(s)
		}
		if n.wanderCounter > 500 && (n.x != n.startX || n.z != n.startZ) {
			n.x, n.z, n.level = n.startX, n.startZ, n.startLevel
			n.tele = true
			n.wanderCounter = 0
		}
	}
}

// wanderMode picks a random destination within WanderRange of spawn (12.5%/tick).
func (n *Npc) wanderMode(s *Server) {
	if n.typ.WanderRange == 0 {
		return
	}
	if rand.IntN(8) != 0 {
		return
	}
	rng := n.typ.WanderRange
	dx := rand.IntN(rng*2+1) - rng
	dz := rand.IntN(rng*2+1) - rng
	n.queueWaypoint(n.startX+dx, n.startZ+dz)
}

// patrolMode advances through PatrolCoord with PatrolDelay between steps.
func (n *Npc) patrolMode(s *Server) {
	if len(n.typ.PatrolCoord) == 0 {
		return
	}
	if s.currentTick < n.nextPatrolTick {
		return
	}
	coord := n.typ.PatrolCoord[n.nextPatrolPoint]
	pos := coordgrid.UnpackCoord(coord)
	n.queueWaypoint(pos.X, pos.Z)
	delay := 0
	if n.nextPatrolPoint < len(n.typ.PatrolDelay) {
		delay = n.typ.PatrolDelay[n.nextPatrolPoint]
	}
	n.nextPatrolTick = s.currentTick + delay
	n.nextPatrolPoint = (n.nextPatrolPoint + 1) % len(n.typ.PatrolCoord)
}

// queueWaypoint clears any existing path and sets a single destination.
func (n *Npc) queueWaypoint(x, z int) {
	n.waypoints[0] = coordgrid.PackCoord(n.level, x, z)
	n.waypointIndex = 0
}

// advanceWaypoint moves one tile toward the current waypoint, or clears the
// path if the next step is blocked. NPCs use straight-line walking (no run).
func (n *Npc) advanceWaypoint(s *Server) {
	dest := coordgrid.UnpackCoord(n.waypoints[n.waypointIndex])
	dir := coordgrid.Face(n.x, n.z, dest.X, dest.Z)
	if dir == -1 {
		n.waypointIndex--
		n.walkDir = -1
		n.runDir = -1
		return
	}
	dx := coordgrid.DeltaX(dir)
	dz := coordgrid.DeltaZ(dir)

	if s != nil && s.gamemap != nil {
		if !s.gamemap.CanTravel(n.level, n.x, n.z, dx, dz) {
			n.waypointIndex = -1
			n.walkDir = -1
			n.runDir = -1
			return
		}
	}

	n.x += dx
	n.z += dz
	n.walkDir = int(dir)
	n.runDir = -1
	n.stepsTaken++

	if n.x == dest.X && n.z == dest.Z {
		n.waypointIndex--
	}
}

// Kill is a test-only helper that marks the NPC dead and schedules respawn
// (RESPAWN lifecycle) or removal (DESPAWN lifecycle).
func (n *Npc) Kill() {
	n.dead = true
	n.lifecycleTick = n.respawnRate
	if n.lifecycleTick <= 0 {
		n.lifecycleTick = 50
	}
}
```

- [ ] **Step 4: Run + commit**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./modules/world/... -run "TestKillSets|TestTeleportHome|TestRespawnAfter|TestWanderMode" -v 2>&1 | tail -15
git add modules/world/npc_ai.go modules/world/npc_ai_test.go
git commit --no-gpg-sign -m "$(cat <<'EOF'
feat(world): add NPC AI — wander, patrol, teleport-home, Kill, lifecycle

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Task 13: NPC registry on `Server`

**Files:**
- Create: `modules/world/npc_registry.go`
- Modify: `modules/world/server.go` — add fields

- [ ] **Step 1: Add fields to `Server`**

In `modules/world/server.go`, append to the `Server` struct:

```go
npcTypes    *objtype.NPCTypeConfigs
npcs        [8192]*Npc
npcLoop     []*Npc
nextNpcSlot int
```

- [ ] **Step 2: Create `modules/world/npc_registry.go`**

```go
package world

import "errors"

var errNpcsFull = errors.New("npc registry full")

// allocNpcSlot returns a free nid (1..8191) and bumps nextNpcSlot for the next
// allocation. Returns -1 if all slots are taken.
func (s *Server) allocNpcSlot() int {
	for offset := 0; offset < len(s.npcs)-1; offset++ {
		i := s.nextNpcSlot + offset
		if i < 1 {
			i = 1
		}
		if i >= len(s.npcs) {
			i = (i % (len(s.npcs) - 1)) + 1
		}
		if s.npcs[i] == nil {
			s.nextNpcSlot = (i + 1) % len(s.npcs)
			if s.nextNpcSlot < 1 {
				s.nextNpcSlot = 1
			}
			return i
		}
	}
	return -1
}

// addNpc places the npc into a free slot. Sets n.nid to the assigned slot.
// Caller must hold playersMu (or call before goroutines exist, as during NewServer).
func (s *Server) addNpc(n *Npc) error {
	nid := s.allocNpcSlot()
	if nid < 0 {
		return errNpcsFull
	}
	n.nid = nid
	s.npcs[nid] = n
	s.npcLoop = append(s.npcLoop, n)
	return nil
}

// removeNpc clears the npc's slot and removes it from npcLoop.
func (s *Server) removeNpc(n *Npc) {
	if n.nid < 1 || n.nid >= len(s.npcs) || s.npcs[n.nid] != n {
		return
	}
	s.npcs[n.nid] = nil
	for i, ln := range s.npcLoop {
		if ln == n {
			s.npcLoop = append(s.npcLoop[:i], s.npcLoop[i+1:]...)
			return
		}
	}
}
```

- [ ] **Step 3: Build**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go build ./modules/world/...
```

- [ ] **Step 4: Commit**

```bash
git add modules/world/server.go modules/world/npc_registry.go
git commit --no-gpg-sign -m "$(cat <<'EOF'
feat(world): add NPC registry on Server (allocNpcSlot/addNpc/removeNpc)

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Task 14: Load NpcTypes + spawn NPCs in `NewServer`

**Files:**
- Modify: `modules/world/server.go`

- [ ] **Step 1: Extend `NewServer`**

Find the block in `NewServer` (sub-spec 3a) that ends with `s.invTypes = invTypes`. Append:

```go
npcTypes, err := objtype.LoadNPCTypes(cfg.CachePath)
if err != nil {
    return nil, fmt.Errorf("load npc types: %w", err)
}
s.npcTypes = npcTypes

for _, spawn := range s.gamemap.NpcSpawns() {
    if spawn.TypeID < 0 || spawn.TypeID >= len(npcTypes.Configs) {
        continue
    }
    typ := npcTypes.Configs[spawn.TypeID]
    if typ == nil {
        continue
    }
    n := NewNpc(0, spawn.TypeID, spawn.X, spawn.Z, spawn.Level, typ)
    if err := s.addNpc(n); err != nil {
        s.log.Warn("npc registry full; dropping remaining spawns", "err", err)
        break
    }
    s.grid.AddNpc(n.nid, n.x, n.z, n.level)
}
```

> **Note on `LoadNPCTypes` signature:** Task 1 may have produced a `LoadNPCTypes(dir string, ptc *ParamTypeConfigs)` variant. If so, use:
>
> ```go
> npcTypes, err := objtype.LoadNPCTypes(cfg.CachePath, params)
> ```
>
> where `params` is the existing `s.paramTypes`. Match what the vendored signature actually is.

- [ ] **Step 2: Build**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go build ./modules/world/...
```

- [ ] **Step 3: Run existing tests** (none are affected since `newTestServer` bypasses `NewServer`)

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./modules/world/... 2>&1 | tail -5
```

- [ ] **Step 4: Commit**

```bash
git add modules/world/server.go
git commit --no-gpg-sign -m "$(cat <<'EOF'
feat(world): NewServer loads NpcTypes and spawns NPCs from gamemap.NpcSpawns

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Task 15: `processNpcs` + extend `processInfo`/`processCleanup`

**Files:**
- Modify: `modules/world/tick.go`

- [ ] **Step 1: Add `processNpcs`**

In `modules/world/tick.go`, append:

```go
func (s *Server) processNpcs() {
	for _, n := range s.npcLoop {
		prevX, prevZ, prevLevel := n.x, n.z, n.level
		n.turn(s)
		if n.x != prevX || n.z != prevZ || n.level != prevLevel {
			s.grid.RemoveNpc(n.nid, prevX, prevZ, prevLevel)
			s.grid.AddNpc(n.nid, n.x, n.z, n.level)
		}
	}
}
```

- [ ] **Step 2: Extend `processInfo`**

Find `processInfo`. After `s.renderer.ComputePlayers(sources)`, add:

```go
npcSources := make([]rsbuf.NpcSource, len(s.npcLoop))
for i, n := range s.npcLoop {
    npcSources[i] = n
}
s.renderer.ComputeNpcs(npcSources)
```

- [ ] **Step 3: Extend `processCleanup`**

Find `processCleanup`. After the player ResetMasks loop, add:

```go
for _, n := range s.npcLoop {
    n.ResetMasks()
}
```

- [ ] **Step 4: Insert `processNpcs` in tick loop**

In `runTickLoopWithRate`, find the existing tick body. Insert `s.processNpcs()` after `s.processPathing()` (and before `s.processLogouts()`):

```go
s.processClientsIn()
s.processPathing()
s.processNpcs()       // NEW
s.processLogouts()
s.processLogins()
s.processInfo()
s.processClientsOut()
s.processCleanup()
```

- [ ] **Step 5: Build + test**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./modules/world/... 2>&1 | tail -5
```

- [ ] **Step 6: Commit**

```bash
git add modules/world/tick.go
git commit --no-gpg-sign -m "$(cat <<'EOF'
feat(world): add processNpcs phase; extend processInfo/processCleanup for NPCs

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Task 16: Implement `Player.updateNpcs()` (replace stub)

**Files:**
- Create: `modules/world/player_npc_info.go`
- Modify: `modules/world/player.go` — remove stub

- [ ] **Step 1: Create `modules/world/player_npc_info.go`**

```go
package world

import (
	gameserver "github.com/zsrv/goscape/pkg/io/protocol/game/server"
	"github.com/zsrv/goscape/pkg/rsbuf"
)

// updateNpcs runs during processClientsOut. Snapshots all NPCs, feeds to
// rsbuf.EncodeNpc, writes the result as an OpNpcInfo packet.
func (p *Player) updateNpcs() {
	s := p.client.server
	if s == nil || p.buildArea == nil || s.renderer == nil || s.grid == nil {
		return
	}

	s.playersMu.RLock()
	snapshot := make([]*Npc, len(s.npcLoop))
	copy(snapshot, s.npcLoop)
	s.playersMu.RUnlock()

	sources := make([]rsbuf.NpcSource, len(snapshot))
	for i, n := range snapshot {
		sources[i] = n
	}

	payload := rsbuf.EncodeNpc(p, sources, p.buildArea, s.grid, s.renderer)
	p.writeOut(gameserver.OpNpcInfo, payload)
}
```

- [ ] **Step 2: Remove the stub in `player.go`**

Find `func (p *Player) updateNpcs()     {}` (or similar one-line stub) and delete it.

- [ ] **Step 3: Build + test**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./modules/world/... 2>&1 | tail -5
```

- [ ] **Step 4: Commit**

```bash
git add modules/world/player_npc_info.go modules/world/player.go
git commit --no-gpg-sign -m "$(cat <<'EOF'
feat(world): implement Player.updateNpcs using rsbuf.EncodeNpc

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Task 17: Integration test — player sees NPC + NPC says hello

**Files:**
- Create: `modules/world/player_npc_test.go`

- [ ] **Step 1: Write the integration test**

```go
package world

import (
	"bytes"
	"testing"

	"github.com/zsrv/goscape/pkg/buildarea"
	"github.com/zsrv/goscape/pkg/gamemap"
	"github.com/zsrv/goscape/pkg/grid"
	"github.com/zsrv/goscape/pkg/objtype"
	"github.com/zsrv/goscape/pkg/rsbuf"
)

func setupNpcInfoPlayer(t *testing.T, s *Server, slot, x, z, level int) *Player {
	t.Helper()
	p, _ := newTestPlayer(t)
	p.client.server = s
	enc, dec := isaacPair([4]uint32{uint32(slot), 2, 3, 4})
	p.client.encryptor = enc
	p.client.decryptor = dec
	p.x, p.z, p.level = x, z, level
	p.originX, p.originZ = x, z
	p.lastTickX, p.lastTickZ, p.lastLevel = x, z, level
	p.buildArea = buildarea.New()
	p.slot = slot
	s.players[slot] = p
	s.playerLoop = append(s.playerLoop, p)
	p.active = true
	s.grid.Add(slot, x, z, level)
	return p
}

func setupNpc(t *testing.T, s *Server, x, z, level int) *Npc {
	t.Helper()
	typ := &objtype.NpcType{
		ConfigType:  objtype.ConfigType{ID: 0, DebugName: "test_npc"},
		WanderRange: 0, // stationary so coords don't drift
		RespawnRate: 50,
	}
	n := NewNpc(0, 0, x, z, level, typ)
	if err := s.addNpc(n); err != nil {
		t.Fatal(err)
	}
	s.grid.AddNpc(n.nid, x, z, level)
	return n
}

func TestPlayerSeesNearbyNpc(t *testing.T) {
	s := newTestServer(t)
	s.gamemap = gamemap.New(discardLogger())
	if err := s.gamemap.Init(t.TempDir()); err != nil {
		t.Fatal(err)
	}
	s.renderer = rsbuf.NewRenderer()
	s.grid = grid.New()

	p := setupNpcInfoPlayer(t, s, 1, 3094, 3106, 0)
	npc := setupNpc(t, s, 3095, 3106, 0)

	s.processInfo()
	p.updateNpcs()

	if _, ok := p.buildArea.Npcs[npc.nid]; !ok {
		t.Errorf("player should track npc after updateNpcs; got %v", p.buildArea.Npcs)
	}
}

func TestNpcSayProducesSayMaskInRenderer(t *testing.T) {
	s := newTestServer(t)
	s.gamemap = gamemap.New(discardLogger())
	if err := s.gamemap.Init(t.TempDir()); err != nil {
		t.Fatal(err)
	}
	s.renderer = rsbuf.NewRenderer()
	s.grid = grid.New()

	npc := setupNpc(t, s, 3095, 3106, 0)
	npc.Say([]byte("hello"))
	s.processInfo()

	highDef := s.renderer.NpcHighDefOf(npc.nid)
	if len(highDef) == 0 {
		t.Fatal("NpcHighDefOf should be non-empty after Say()")
	}
	if highDef[0]&rsbuf.NpcMaskSay == 0 {
		t.Errorf("header byte: NpcMaskSay (0x8) should be set; got %d", highDef[0])
	}
	// Payload should contain the bytes of "hello" followed by line-feed terminator.
	if !bytes.Contains(highDef, []byte("hello")) {
		t.Errorf("expected 'hello' bytes in payload; got %v", highDef)
	}
}
```

- [ ] **Step 2: Run**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./modules/world/... -run "TestPlayerSees|TestNpcSay" -v 2>&1 | tail -10
```

Expected: both pass.

- [ ] **Step 3: Commit**

```bash
git add modules/world/player_npc_test.go
git commit --no-gpg-sign -m "$(cat <<'EOF'
test(world): integration — player sees nearby NPC, NPC Say raises NpcMaskSay

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Task 18: Final verification

- [ ] **Step 1: Full test suite**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test ./...
```

Expected: all `ok` (some may `skip` if no cache data).

- [ ] **Step 2: Race detector**

```bash
GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go test -race ./... 2>&1 | grep -E "FAIL|DATA RACE|^ok" | tail -25
```

Expected: zero races.

- [ ] **Step 3: Binary builds**

```bash
CGO_ENABLED=0 GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go build -trimpath ./cmd/goscape && rm -f goscape && echo "build OK"
```

Expected: `build OK`.

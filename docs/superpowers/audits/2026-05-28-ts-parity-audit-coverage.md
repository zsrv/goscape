# TS-Parity Coverage Addendum (2026-05-28) — Deviation Ledger

**Subject:** goscape `146e759d`
**Reference (source of truth):** Engine-TS `e1dea19f`

> This is the **coverage addendum** to [`2026-05-28-ts-parity-audit-fresh.md`](./2026-05-28-ts-parity-audit-fresh.md). It records the ~10 audit units that closed the coverage gaps the main fresh audit left open: client decoders, client/model carriers, server codec encoders + models, the `runScript` protected-access guard, the login wire, the friend/social bridge, the world reload/relay-events pipeline, the configs-snapshot / network-base seam, the PlayerOps opcode seams, and the db/datastruct layer. **TypeScript remains the source of truth.** Findings carrying an adversarial verdict (`verdict.real === false`, or a `corrected_severity`) have had that verdict applied: any refuted high/medium finding is moved to the **Refuted** appendix; corrected severities override the original walk severity.

10 units; 332 functions/structures compared across them.

---

## Running summary

### By severity (survived verification, corrected_severity applied)

| Severity | Count |
|---|---:|
| CRITICAL | 1 |
| HIGH | 1 |
| MEDIUM | 10 |
| LOW | 34 |
| **Refuted / false-positive** | 0 |
| Deferred markers (separate table) | 26 |

> As in the main ledger, many LOW findings carry `class = CONFIRMED-EXCEPTION` / `N-A` — these are verified-faithful re-checks kept for traceability. The actionable regression set is CRITICAL + HIGH + the `DEVIATION`/`MISSING` MEDIUMs.

### By class

| Class | Count |
|---|---:|
| DEVIATION | 30 |
| MISSING | 4 |
| COMMENT-LIE | 2 |
| CONFIRMED-EXCEPTION | 4 |
| EXTENSION | 1 |
| N-A | 5 |

### Severity corrections applied from adversarial verdicts

- `gap-login-wire-1` CRITICAL→CRITICAL (held — unauthenticated login-RSA panic crashes whole process).
- `gap-world-reload-events-1` HIGH→HIGH (held — RELAY_RELOAD full re-pack + inventory clobber).
- `gap-configs-snapshot-netbase-1` HIGH→MEDIUM (USER_EVENT limit not gated on handler success; Go is stricter, no dupe/security exposure).
- `gap-db-datastruct-1` HIGH→MEDIUM (same-node non-reconnect re-login admitted; bounded to single-world self re-login).
- `gap-friend-social-bridge-1` LOW→LOW (stale slice-1 comment-lie confirmed, zero behavioral impact).
- `gap-world-reload-events-5` LOW→LOW (content-watcher dynamic dir-add comment overstates TS parity — confirmed).

> Note: the `gap-configs-snapshot-netbase-1` and `gap-client-models-1` findings describe the **same** root cause (the dropped `handle():boolean` success contract) observed from two units; both are retained for traceability, with `gap-configs-snapshot-netbase-1` carrying the corrected MEDIUM verdict.

---

## Findings (CRITICAL → LOW, grouped by unit)

### CRITICAL

#### Unit: gap-login-wire (login wire / RSA / framing — 14 fns)

| id | go_loc | ts_loc | class | title | detail / evidence |
|---|---|---|---|---|---|
| gap-login-wire-1 | `pkg/io/packet/packet.go:495-521` (RSADec) + `modules/world/server.go:747-852` (handleTCPConn, **no recover**) | `src/io/Packet.ts:438-461` (rsadec) + `src/server/tcp/TcpServer.ts:29-41` (try/catch → `client.terminate()`) | DEVIATION | **Malformed login RSA block crashes the whole server (no recover) where TS terminates one connection.** | RSADec reads attacker-controlled `numBytes := p.G1()` then `GData(rsax, int(numBytes))`, which does a raw slice `p.Data[Pos:Pos+length]` with **no bounds check** → slice-OOB panic when `numBytes` exceeds bytes present. Post-decrypt `G1`/`G4` (req.go:114-120) panic `io.EOF` on a short buffer. `UnmarshalHeader` validates only the *outer* payload size, never the inner RSA prefix. The conn goroutine's defer is cleanup-only (no `recover()`), so the unrecovered panic kills the whole process and every online player. Unauthenticated and reachable: `rev=225` is hardcoded and the 9 archive CRCs are served at the asset `/crc` endpoint. **Verdict: real, high confidence, CRITICAL.** The codebase already adds `recover()` for this exact class in LoadSave / tick_recovery but missed login. TS wraps `onClientData` in try/catch→terminate so a `RangeError` drops only that socket. |

### HIGH

#### Unit: gap-world-reload-events (reload + relay-events pipeline — 18 fns)

| id | go_loc | ts_loc | class | title | detail / evidence |
|---|---|---|---|---|---|
| gap-world-reload-events-1 | `modules/world/world_state_ops.go:149-153` (RelayReload) + `rebuild_worker.go:92` (`reloadFn(true)`) | `src/engine/World.ts:2035-2036` | DEVIATION | **RELAY_RELOAD: TS does a config-only `reload(false)`; goscape triggers a full PackAll + `reload(true)`.** | TS `RELAY_RELOAD` = `this.reload(false)`: a pure in-place type-config/script reload that **preserves** SCOPE_SHARED/SCOPE_TEMP inventories (clearInvs=false skips L221-236). TS only packs via the *local* DevThread `dev_reload`, never on a relay. goscape's path is live (subscriber → `RelayReload()` → `dispatchRebuildRequest()` → `packFn(PackAll)` then `reloadFn(true)`), so on every cross-world relay-reload it (a) re-PACKs the entire content tree from source and (b) clears SHARED + deletes TEMP player inventories via `reconcileInvs`. `Reload(false)` exists but is never called on the live path. **Verdict: real, high confidence, HIGH.** Cluster-only reachability is the sole mitigant; impact is live cluster-wide mutation of shared/temp inventories. |

### MEDIUM

#### Unit: gap-client-models (client/model semantic-field carriers — 22 fns)

| id | go_loc | ts_loc | class | title | detail / evidence |
|---|---|---|---|---|---|
| gap-client-models-1 | `modules/world/player.go:1187-1194` | `src/engine/entity/NetworkPlayer.ts:146-152` | DEVIATION | **USER_EVENT per-tick limit counted unconditionally in Go; TS gates it on handler success.** | TS increments `userLimit` ONLY when the handler returned `true`; a USER_EVENT whose handler returns false (MoveClick delayed/distance gate, a failing OpX gate) falls through and burns a CLIENT_EVENT slot instead. Go's gameHandlers are `func(...) error` with no success bool, so the `switch Category` counts every USER_EVENT regardless of outcome. Under a flood of failing user-events, Go stops decoding after 5 (userEventLimit) while TS keeps decoding up to 20 (clientLimit). *Same root cause as `gap-configs-snapshot-netbase-1`, which carries the authoritative MEDIUM verdict.* |

#### Unit: gap-server-codec-models (server codec encoders + models — 62 fns)

| id | go_loc | ts_loc | class | title | detail / evidence |
|---|---|---|---|---|---|
| gap-server-codec-models-1 | `modules/world/inv_update.go:19-36` | `src/network/game/server/codec/UpdateInvFullEncoder.ts:13-22` | DEVIATION | **UpdateInvFull size clamp conditional on grid>0 in Go; TS uses an unconditional `Math.min(capacity, w*h)`.** | TS computes `size = Math.min(inv.capacity, comType.width*comType.height)` unconditionally, so a zero-grid / missing component yields size=0 (empty send). goscape only narrows when `grid>0 && grid<size` and otherwise sends the FULL `inv.Capacity`. On a zero-grid/missing component goscape sends full capacity where TS sends 0. Latent because inventory-routed components carry positive grids, but the gate semantics differ. |

#### Unit: gap-world-reload-events (reload + relay-events pipeline — 18 fns)

| id | go_loc | ts_loc | class | title | detail / evidence |
|---|---|---|---|---|---|
| gap-world-reload-events-2 | `modules/world/rebuild_worker.go:38-103` (no progress broadcast) | `tools/pack/PackAll.ts:18-22,46-50` + `src/engine/World.ts:1759-1766` | DEVIATION | **`dev_progress` "Packing changes" / "Reloading with changes" broadcasts to all players are not reproduced.** | TS posts two `dev_progress` messages during a rebuild and `createDevThread` broadcasts `msg.broadcast` to ALL players via `broadcastMes` (no NODE_DEBUG gate). goscape's rebuild worker emits no in-progress player-visible broadcast; players see only the final staff-only "Rebuilt: Xms.". The live "Packing changes" world-wide announcement is silently omitted. |
| gap-world-reload-events-3 | `modules/world/rebuild_worker.go:101-129` (broadcastRebuildStaff) + `handlers_game.go:771` | `src/engine/World.ts:1750-1758,1803-1811` + `ClientCheatHandler.ts:151-153` | DEVIATION | **Rebuild outcome broadcast scope narrowed to staff (modlvl≥4)+invoker vs TS broadcast to ALL players.** | TS surfaces rebuild lifecycle to every connected player via `broadcastMes` (dev_reload "Loaded N scripts.", dev_failure error). goscape filters lifecycle messages to `staffModLevel>=4` plus the invoker, so a normal bystander who in TS sees the world-wide reload notice never sees it. (Step-9 "Loaded N scripts." IS broadcast to all under NodeDebug, matching TS; only the rebuild-specific lifecycle messaging is staff-scoped.) |
| gap-world-reload-events-8 | `modules/world/reload.go:161` (D4-NO-CATEGORYTYPES skip) | `src/engine/World.ts:216` | MISSING | **`reload()` omits the `CategoryType.load` that TS performs at L216.** | TS `reload()` calls `CategoryType.load('data/pack')` between SpotanimType and EnumType. goscape has no CategoryType loader at all; the reload comment frames L216 as "no goscape analog," but L216 plainly DOES load CategoryType. Truthful framing: goscape lacks the type system, so category configs are never (re)loaded. If categories are consulted anywhere in the NPC/script runtime they are stale/absent after any reload. Classed MISSING because the cited "no analog" claim is contradicted by L216. |

#### Unit: gap-configs-snapshot-netbase (configs snapshot + network-base — 9 fns)

| id | go_loc | ts_loc | class | title | detail / evidence |
|---|---|---|---|---|---|
| gap-configs-snapshot-netbase-1 | `modules/world/player.go:1187-1194` (processIn) + `player.go:1294-1300` (readPacket) | `src/engine/entity/NetworkPlayer.ts:143-152` + `ClientGameMessageHandler.ts:5` | MISSING | **USER_EVENT decode-limit counter not gated on handler success (the `handle():boolean` contract is dropped).** | The entire behavioral payload of the ClientMessageHandler base class is its `handle(message,player): boolean` return. TS routes failed USER_EVENTs to the 20-budget clientLimit; Go's handlers are `func(*Player,[]byte) error` (no success bool), readPacket returns `ok=true` on any complete packet, and processIn increments userLimit for every USER_EVENT regardless of outcome. A client spamming failing OpHeld/IfButton/Op* is throttled far harder in Go. **Verdict: real, high confidence, HIGH→MEDIUM** — confirmed via OpHeldHandler.ts:16-46 false-returns and grep showing no Go handler signals soft failure; downgraded because the effect makes Go *stricter* (no dupe/security exposure), only manifesting under failing-packet spam. |
| gap-configs-snapshot-netbase-3 | `modules/world/player.go:1174,1186,1196-1199` (readAny) + `tick.go:408` | `src/engine/entity/NetworkPlayer.ts:68-82` (bytesRead) + `World.ts:753` | DEVIATION | **`lastResponse` / idle-timeout refresh keys off complete-packet read, not bytes-consumed.** | TS refreshes `lastResponse` when `bytesRead>0`; because TS `read()` consumes the opcode + size prefix *before* the partial-return, a tick that delivers only a packet header still advances `in.pos` and refreshes lastResponse. Go sets `readAny=true` only when a COMPLETE packet is dispatched, so a header-only tick leaves readAny=false and lastResponse is not refreshed. A client trickling partial packets could be idle-timed-out by Go while TS keeps it alive. Edge/latent (sustained sub-packet trickle), hence MEDIUM. |

#### Unit: gap-db-datastruct (db + datastruct layer — 38 fns)

| id | go_loc | ts_loc | class | title | detail / evidence |
|---|---|---|---|---|---|
| gap-db-datastruct-1 | `modules/login/handler.go:148-189` (already-logged-in / reconnect block) | `src/server/login/LoginServer.ts:271-327` | DEVIATION | **Same-node non-reconnect login is accepted (re-login) instead of rejected as already-logged-in.** | TS keys the reconnect branch on `reconnecting && account.logged_in === nodeId`; the chained `else if (logged_in !== null && !== 0)` catches a same-node login that did NOT request reconnect and returns response 3. goscape returns ALREADY_LOGGED_IN only when `NodeID != req.NodeId`; with `LoggedIn==1 && NodeID==nodeId && !Reconnecting` it falls through to the full-login transaction, re-inserting session/login rows for a still-active session. **Verdict: real, high confidence, HIGH→MEDIUM** — `world_startup` resets logged_in=0 at node start so the gap only triggers for a currently-online same-node session (single-world self re-login), hence bounded → MEDIUM. |
| gap-db-datastruct-4 | `modules/world/server.go` playerLoop (`[]*Player`); `reboot.go`/`session_log.go`/`rebuild_worker.go` iteration | `src/datastruct/HashTable.ts`; `src/engine/World.ts:146,917-932` | DEVIATION | **World player-tick iteration uses a flat slice, not the TS `playerLoop` IP-bucketed HashTable.** | TS iterates players each tick via `playerLoop.all()` walking 8 IP-derived buckets in bucket-then-insertion order. goscape ports playerLoop as a plain slot/insertion-ordered slice with no HashTable and no IP bucketing, changing intra-tick processing order. Order-dependent player-vs-player effects could diverge. No Go file ports HashTable; this is the genuine HashTable-equivalent locus. |
| gap-db-datastruct-9 | `modules/login/handler.go:295-304` (PlayerLogout, documented no-op); no hiscore migration | `src/server/login/LoginServer.ts:19-109` (updateHiscores) + `:450` + prisma `hiscore`/`hiscore_large` | MISSING | **`updateHiscores` / `hiscore` + `hiscore_large` tables not ported (logout XP export).** | On player_logout TS exports per-stat XP/levels into `hiscore` (INTEGER) and `hiscore_large` (BIGINT total XP) with staffmodlevel>1/banned skips and the level≥15 gate. goscape ports none of it (no tables, no gate, no export). The PlayerLogout comment accurately documents this as a deliberate deferral, so it is documented MISSING, not a comment-lie; the leaderboard subsystem is genuinely absent. |

### LOW

#### Unit: gap-client-decoders (client `*Decoder.ts` decode bodies — 36 fns)

| id | go_loc | ts_loc | class | title | detail / evidence |
|---|---|---|---|---|---|
| gap-client-decoders-1 | `modules/world/handler_social_list.go:39`, `handler_message_private.go:34`, `handler_reportabuse.go:41`; `pkg/io/packet/packet.go:222` | `src/io/Packet.ts:257` (g8 = getBigInt64 SIGNED); `src/util/JString.ts` fromBase37 | N-A | **`g8()` signed-vs-unsigned for username decode is parity-by-threshold (not a bug).** | TS `g8()` returns a SIGNED bigint and `fromBase37` rejects `value<0n` as 'invalid_name'. goscape `G8()` returns uint64 and FromBase37 rejects `v>=37^12 (6582952005840035281)`. A high-bit-set wire value becomes negative in TS (→invalid_name) and a uint64≥2^63 in goscape (>threshold→invalid_name). Both reject identically; normal usernames never set the high bit. Verified clean, no divergence on any input. |

#### Unit: gap-client-models (client/model semantic-field carriers — 22 fns)

| id | go_loc | ts_loc | class | title | detail / evidence |
|---|---|---|---|---|---|
| gap-client-models-2 | `modules/world/player.go:1187-1194` + `pkg/io/protocol/game/client/prot.go:33-50` | `src/engine/entity/NetworkPlayer.ts:137-153` | DEVIATION | **Decoder-less opcodes (anticheat / camera) consume a CLIENT_EVENT limit slot in Go but none in TS.** | In TS the limit counters increment ONLY inside `if (decoder) {...}`; AnticheatOpLogic1-9, AnticheatCycleLogic1-6 and EventCameraPosition have no bound decoder, so they read bytes but increment no counter. Go registers them with handler-less entries; the category switch fires on every read packet, so each anticheat/camera packet increments clientLimit. A client spamming anticheat opcodes could exhaust Go's 20/tick clientLimit and defer other decoding to the next tick, which TS would not. |
| gap-client-models-3 | `modules/world/handler_event_tracking.go:41-43` | `src/network/game/client/handler/EventTrackingHandler.ts:18-21` | DEVIATION | **EventTracking handler returns nil where TS returns true on the `!shouldSubmit` branch (benign under current counting).** | TS returns `true` when `!shouldSubmitTrackingDetails()` and `false` on other gates; goscape collapses all paths to `return nil`. Harmless for EVENT_TRACKING because it is RESTRICTED_EVENT (both engines count unconditionally). Only the structural success-signal differs; flagged for completeness as the same root cause as finding gap-client-models-1. |

#### Unit: gap-server-codec-models (server codec encoders + models — 62 fns)

| id | go_loc | ts_loc | class | title | detail / evidence |
|---|---|---|---|---|---|
| gap-server-codec-models-2 | `modules/world/inv_update.go:33-36` | `src/network/game/server/codec/UpdateInvFullEncoder.ts:16-17` | DEVIATION | **UpdateInvFull adds a `size>0xff` clamp absent in TS.** | goscape adds `if size > 0xff { size = 0xff }` before `p1(size)`; TS has none and relies on `Packet.p1` byte-masking. Unreachable for real capacities (all <256), so wire output is identical in practice, but it is a goscape-only branch; for a hypothetical >255-slot grid Go caps at 255 (writes 255 slots) whereas TS writes `size & 0xff` for the count byte but loops over the full count — edge-only divergent byte counts. |
| gap-server-codec-models-3 | `pkg/rsbuf/zone_encoders.go:30-39,122-149` | `ObjAddEncoder.ts:12`, `ObjCountEncoder.ts:12-14`, `ObjRevealEncoder.ts:12-13` | DEVIATION | **`clampU16` zero-floors negative obj counts; TS `Math.min(count,65535)` does not floor negatives.** | goscape `clampU16` returns 0 for n<0; TS only caps the upper bound and would write a negative count as its two's-complement 16-bit value via `p2`. Obj counts are always ≥0 in both engines so this never fires on a live path; only the negative-handling differs (goscape=0, TS=wrapped). ObjReveal.receiverId is unclamped in both — that part matches. |

#### Unit: gap-runscript-guard (protected-access guard — 11 fns)

| id | go_loc | ts_loc | class | title | detail / evidence |
|---|---|---|---|---|---|
| gap-runscript-guard-1 | `modules/world/handler_opheld.go:41`, `handler_inv_button.go:24/117`, `player_interaction_trigger.go:102` | `OpHeldHandler.ts:16`, `InvButtonHandler.ts:14`, `InvButtonDHandler.ts:40` | DEVIATION | **Immediate-run handlers use a tick-comparison delayed gate vs TS bare `this.delayed`; one-tick window leaks pre-script side effects (but NOT the dupe vector).** | The four immediate-run handlers gate on `p.delayed && s.currentTick < p.delayedUntil`, while TS gates on bare `if (player.delayed)`. Because `processClientsIn()` runs before `processActiveScripts()` resets `p.delayed=false`, there is a one-tick window where a delay has expired but `p.delayed` is still true: goscape's handler gate PASSES (runs lastItem/lastSlot/ClearPendingAction/faceEntity/masks/session-log side effects) then the runScript guard (bare `p.delayed`) BLOCKS the script. TS drops at the handler before any side effect. The dupe-relevant script execution is correctly blocked in both engines; only pre-script side effects diverge. LOW because the load-bearing guard backstops the dupe. |

#### Unit: gap-login-wire (login wire / RSA / framing — 14 fns)

| id | go_loc | ts_loc | class | title | detail / evidence |
|---|---|---|---|---|---|
| gap-login-wire-2 | `pkg/io/packet/packet.go:236-252` (GJStr) | `src/io/Packet.ts:267-276` (gjstr) | DEVIATION | **GJStr returns a trailing byte that TS `gjstr` drops when no terminator is found.** | TS loops `while ((b = getUint8(pos++)) !== terminator && pos < length)`: when the last byte is read and is not the terminator, `pos < length` is false so the loop exits WITHOUT appending it — the last byte is dropped. Go GJStr's no-terminator branch returns all remaining bytes. They diverge by exactly one trailing byte in the no-terminator case. Latent on the login path (username/password are LF-terminated), but the primitive is not byte-faithful. |
| gap-login-wire-3 | `modules/world/server.go:908-914`; `pkg/cache/crctable.go:58-77` | `src/engine/World.ts:2129-2136`; `src/cache/CrcTable.ts:5-34` | DEVIATION | **CRC gate compares 9 raw archive CRCs element-wise vs TS CRC-of-the-36-byte-buffer.** | TS computes `Packet.getcrc(clientCrcs, 0, 36)` over the 36 client-sent CRC bytes and compares to the precomputed CrcBuffer32. Go compares the 9 decoded uint32 values element-wise (`slices.Equal`). Table layout (slot0=0, same 8-archive order) is verified identical to TS and the Java client. Element-wise is functionally equivalent and arguably stricter (no CRC-collision window) — benign mechanism deviation, not a defect. |
| gap-login-wire-4 | `pkg/io/packet/packet.go:475-491` (RSAEnc) | `Client-Java jagex2/io/Packet.java:303-314` (rsaenc, BigInteger.toByteArray()) | DEVIATION | **MarshalBinary RSA block uses `big.Int.Bytes()` (minimal) not Java `BigInteger.toByteArray()` (sign/pad) layout.** | Go RSAEnc emits `P1(len(big.Int.Bytes()))` — a minimal big-endian encoding with no leading sign byte and no fixed 64-byte width. The real client / TS forge uses BigInteger.toByteArray() which can prepend 0x00 (65-byte) or shrink below 64. Go's server-side RSADec normalizes the 65/63 cases (inbound parity holds), but Go's own MarshalBinary (bots/tests only) would not byte-match a real client's RSA block in the leading-zero case. Off the live server path. |
| gap-login-wire-5 | `pkg/io/protocol/login/req/req.go:84-90` | `src/engine/World.ts:2090-2103` (framing reads exactly `waiting` bytes) | N-A | **UnmarshalHeader `ErrPayloadTooLarge` branch has no TS analog but is unreachable on the framed path.** | UnmarshalHeader rejects `r.Len() > payloadSize`; TS has no such rejection (it reads `waiting` bytes and ignores surplus). In Go the caller slices via `c.in.Next(pLen)` (exact headerSize+payloadSize), so after consuming opcode+size `r.Len()` always equals payloadSize and neither branch fires. Defensive-only; no live-path divergence. |

#### Unit: gap-friend-social-bridge (friend / social PM-visibility-mode bridge — 22 fns)

| id | go_loc | ts_loc | class | title | detail / evidence |
|---|---|---|---|---|---|
| gap-friend-social-bridge-1 | `modules/world/friends_client.go:191-201` | `src/server/friend/FriendServer.ts:256-285` | COMMENT-LIE | **Stale slice-1 stub comment on `grpcFriendsClient.PrivateMessage` contradicts the real RPC it now makes.** | The doc-comment says "Slice 1 logs and returns; slice 4 will fan out to the target's world," describing an obsolete stub. The body actually performs the production RPC `c.client.PrivateMessage(ctx, req)` and logs only on error — it DOES fan out (server-side delivery in the friends module). **Verdict: real, high confidence, LOW** — path is live (handler_message_private.go:52 → bridge → this method), module builds clean, neighboring comments at bridges.go:31-32 correctly describe the fan-out; line-192 is the lone stale comment. Pure comment-lie, zero behavioral impact. |
| gap-friend-social-bridge-2 | `modules/world/server_pmid.go:32` | `src/engine/World.ts:1638` | DEVIATION | **`nextPmId` uses bitwise-OR while TS uses arithmetic addition; differs only after 65536 PMs/world lifetime.** | TS computes `pmId = (NODE_ID<<24) + ((rand*0xff)<<16) + pmCount++` with `+` and an unbounded pmCount. Go uses `|`. For `pre <= 0xffff` the fields are bit-disjoint and `|` == `+`, byte-identical in practice. They diverge only after >65535 PMs from one world without restart; pmId is a client-side dedup correlation id, so the divergence is behaviorally inert. Unreachable edge. |
| gap-friend-social-bridge-3 | `modules/world/handlers_game.go:434-438` | `src/engine/World.ts:648-650, 1578-1585` + `MessagePublicHandler.ts:32` | DEVIATION | **PublicMessage audit-log skipped when session is empty/"headless"; TS logs unconditionally.** | TS `logPublicChat` fires whenever `logMessage !== null` with no session guard (session always populated). Go gates the `friendsBridge.PublicMessage` call on `p.session != "" && p.session != "headless"`. In production both run with a real per-login session UUID so behaviors converge; the guard only diverges for the unbridged headless/test sentinel where audit logging is meaningless. coord (PackCoord) and decoded text both match TS. |
| gap-friend-social-bridge-4 | `modules/world/bridges.go:123-130,138-143,151-157` | `src/engine/World.ts:1958-1996` (onFriendMessage) | N-A | **Friends-update "player not found" is silent-skip in Go vs `printError` in TS.** | TS logs `printError('FriendThread: player X not found')` then returns when `getPlayerByUsername` fails for UPDATE_FRIENDLIST/UPDATE_IGNORELIST/PRIVATE_MESSAGE. Go resolves via `lookupPlayerByUsername37` inside the tick closure and returns silently on nil. Behaviorally identical for the player (no packet emitted either way); only the diagnostic log line is dropped. |

#### Unit: gap-world-reload-events (reload + relay-events pipeline — 18 fns)

| id | go_loc | ts_loc | class | title | detail / evidence |
|---|---|---|---|---|---|
| gap-world-reload-events-4 | `modules/world/rebuild_worker.go:87-90` (handleRebuildResult error arm) | `src/engine/World.ts:1752-1758` | DEVIATION | **`dev_failure` error message script-dir prefix strip not reproduced.** | On rebuild failure TS broadcasts the raw error to all players after stripping the `${BUILD_SRC_DIR}/scripts/` prefix, plus a second line "Check the console for more information." goscape broadcasts "Rebuild failed: "+err to staff only, no path normalization and no follow-up line. Cosmetic, but the player-facing error text differs. |
| gap-world-reload-events-5 | `modules/world/content_watcher.go:157-163` | `src/cache/DevThread.ts:67-90` | COMMENT-LIE | **content_watcher dynamic CREATE-dir add comment overstates TS parity (TS `trackDir` does not dynamically watch new dirs).** | goscape dynamically `w.Add()`s any dir created mid-session with the comment "Mirrors TS DevThread.trackDir recursion." TS `trackDir` runs ONCE at startup; its fs.watch callback only calls `trackFileChange` and never re-adds newly-created subdirectories. So a runtime-created dir is watched in goscape but NOT in TS. **Verdict: real, high confidence, LOW** — confirmed benign behavioral extension with a misleading comment (TS recurses at init only, not on CREATE); dev-only (ContentWatch=false by default). |
| gap-world-reload-events-6 | `modules/world/world_events_dispatcher.go` + `world_events_subscriber.go` (all) | `src/engine/World.ts:2001-2052` + `src/server/friend/FriendThread.ts` | EXTENSION | **`world_events` dispatcher/subscriber is a gRPC re-implementation of the TS FriendThread RELAY_* relay (transport + payload differ).** | The pair faithfully covers all 9 RELAY_* opcodes — it is goscape's replacement for the TS FriendThread→World relay push, over a gRPC `SubscribeWorldEvents` server-stream instead of a Node worker_threads channel. Payload differs: TS relays string `username`; goscape relays `username37 uint64` (lossless equivalent). The default-case "unknown event variant" warn is harmless. No correctness defect. |
| gap-world-reload-events-7 | `modules/world/world_state_ops.go:167-179` | `src/engine/World.ts:2039-2040` | CONFIRMED-EXCEPTION | **`ClearLogouts` no-op is architecturally justified (no goscape logout-request queue).** | TS `RELAY_CLEARLOGOUTS` does `this.logoutRequests.clear()`. goscape has no logoutRequests queue (logouts use the per-player `loggingOut` flag drained by processLogouts), so ClearLogouts logs Info and does nothing. Clearing `loggingOut` from a relay goroutine would be unsafe and semantically wrong. Genuine architectural difference, tagged NAI-S5B-D-CLEARLOGOUTS-NO-GOSCAPE-QUEUE; no equivalent state to clear. |

#### Unit: gap-configs-snapshot-netbase (configs snapshot + network-base — 9 fns)

| id | go_loc | ts_loc | class | title | detail / evidence |
|---|---|---|---|---|---|
| gap-configs-snapshot-netbase-2 | `modules/world/player.go:1187-1194` + readPacket return at 1300 | `src/engine/entity/NetworkPlayer.ts:137-154` | DEVIATION | **Go counts decode-limit for opcodes with no registered handler; TS only counts inside `if(decoder)`.** | TS only touches the limit counters inside `if (decoder) {...}`; an opcode with a known type but no decoder consumes no budget. Go increments a category counter for every fully-read known opcode even when `gameHandlers[opcode]==nil`. Latent/LOW because every known opcode currently has a handler, but the contract differs: a future opcode in `gameclient.Ops` without a gameHandlers entry would burn its category budget in Go and not in TS. |
| gap-configs-snapshot-netbase-4 | `modules/world/configs_snapshot.go:30-91`; `server.go:166-171,578`; `reload.go:280`; read sites `client.go:146-148`, `player.go:632-636` | `InvType.get / SeqType.get` (global singletons); `src/engine/World.ts:206` reload() | CONFIRMED-EXCEPTION | **configs_snapshot atomic build-then-swap is behavior-preserving vs TS direct global config access.** | TS config registries are process-global singletons mutated in place by the single-threaded reload(); no concurrent reader, so no synchronization needed. Go runs login on a per-connection goroutine concurrent with the tick goroutine's Reload, so a torn pointer read of invTypes/seqTypes is possible without the atomic. Only two config fields are read off the conn goroutine (both via `loginConfigs()`); `storeConfigsSnapshot` is called after all 20 fields are assigned in both NewServer and Reload step-11. Purely a concurrency-correctness adaptation forced by Go's threading model; no wire/field/encoding change. |
| gap-configs-snapshot-netbase-5 | `modules/world/handlers_game.go:33` (`gameHandlers [256]func`); `pkg/io/protocol/game/client/prot.go` | `ClientMessage.ts:1-2`; `ServerMessage.ts:1-2`; `ClientGameMessage.ts:4-6`; `ServerGameMessage.ts:3-4` | N-A | **ClientMessage/ServerMessage/ClientGameMessage/ServerGameMessage marker classes have no Go named analogue (no contract content lost).** | The TS bases are empty/near-empty; only `ClientGameMessage` adds `abstract readonly category`. Go dispatches raw `[]byte` through a flat opcode-indexed handler table and carries `category` on the opcode descriptor (`gameclient.Op.Category`). The only contract content that mattered (category + limit constants 20/5/2) is preserved 1:1. Abstraction shape differs by design (data-oriented Go vs OO TS) with no behavioral loss beyond findings 1-2. |
| gap-configs-snapshot-netbase-6 | `pkg/world/connhandler/connhandler.go:13-15`; `modules/world/conn_handler.go:12-16` | (none — TS asset/world are one process; no inter-module conn handoff) | N-A | **`connhandler.ConnHandler` / conn_handler.go are a goscape-only WS-bridge seam with no TS counterpart.** | `ConnHandler.HandleConn(net.Conn)` lets the asset module's WebSocket bridge hand accepted connections to the world TCP state machine without importing modules/world. TS has no equivalent (asset+world co-located). The impl wraps `tcpWg.Add/Done` around `handleTCPConn`, faithfully taking ownership (closes conn) and blocking for the connection lifetime. Pure module-decoupling plumbing; no TS behavior to deviate from. |

#### Unit: gap-playerops-seams (PlayerOps opcode seams — 132 fns)

| id | go_loc | ts_loc | class | title | detail / evidence |
|---|---|---|---|---|---|
| gap-playerops-seams-1 | `pkg/script/handlers_player.go:609-628` (handleStatRandom) | `src/engine/script/handlers/PlayerOps.ts:578-586` | DEVIATION | **STAT_RANDOM uses int truncation where TS `Math.floor`s each term (diverges on boosted level >99).** | TS computes `floor((low*(99-level))/98) + floor((high*(level-1))/98) + 1`, flooring each term. Go uses Go integer division (truncate toward zero). STAT_RANDOM reads the CURRENT boosted level which can exceed 99 (up to 255), making `(99-level)` negative; on a negative dividend Go truncates toward 0 while Math.floor rounds toward -inf, differing by 1 and shifting the `value>chance` probability by one unit. |
| gap-playerops-seams-2 | `pkg/script/handlers_player.go:616` (handleStatRandom) | `src/engine/script/handlers/PlayerOps.ts:578-579` | DEVIATION | **STAT_RANDOM adds a `checkStatID` gate that TS omits (abort vs silent push-0).** | TS STAT_RANDOM does NOT validate the stat id; an out-of-range stat reads `levels[stat]===undefined` → NaN arithmetic → `value>chance` false → silently pushes 0 and continues. Go inserts `checkStatID(id)` which ABORTS the script. Error-vs-silent-skip; reachable only with a malformed stat operand, so latent/low. |
| gap-playerops-seams-3 | `pkg/script/handlers_player.go:1712-1728` (handleDamage) | `src/engine/script/handlers/PlayerOps.ts:768-779` | MISSING | **DAMAGE drops TS `NumberNotNull` validation on `amount` and `uid`.** | TS validates all three operands (amount=NumberNotNull, type=HitTypeValid, uid=NumberNotNull). Go only validates hitType. A -1 uid is harmless (lookup → nil → silent return), but a -1 amount TS would reject reaches ApplyDamage in Go. Real but low since content does not pass -1 amounts. |
| gap-playerops-seams-4 | `pkg/script/handlers_player.go:2065-2086` (handleWealthEvent) | `src/engine/script/handlers/PlayerOps.ts:1191-1202` | DEVIATION | **WEALTH_EVENT uses objID=-1 for an unknown name where TS records `undefined` id.** | TS builds `account_items:[{id: objType?.id, ...}]` where id is undefined when `getByName` misses; Go substitutes -1. WEALTH_EVENT is a custom (non-wire) telemetry op feeding addWealthEvent; -1 vs undefined could differ in downstream serialization but has no live-path game effect. |

#### Unit: gap-db-datastruct (db + datastruct layer — 38 fns)

| id | go_loc | ts_loc | class | title | detail / evidence |
|---|---|---|---|---|---|
| gap-db-datastruct-2 | `modules/friends/repository.go:240-249` (atomicUpsertList cap check) | `src/server/friend/FriendServerRepository.ts:230-235, 266-269` | DEVIATION | **Friend/ignore 100-entry cap counts per-profile in Go but across all profiles in TS.** | TS counts the cap `where('account_id','=',account.id)` with no profile predicate, so the 100 limit is shared across every profile. goscape filters `WHERE profile = ? AND owner_username37 = ?`, applying the limit independently per profile (up to 100 each). Latent since most deployments run one profile. |
| gap-db-datastruct-3 | `modules/login/migrations/000001_init.up.sql`; `modules/login/db.go:149-162` insertAccount | prisma `account.registration_date DEFAULT CURRENT_TIMESTAMP`; `LoginServer.ts:215` | DEVIATION | **`account.registration_date` column dropped — registration timestamp never persisted.** | TS account schema carries `registration_date` (defaulted, set at auto-register). goscape's migration has no such column; insertAccount writes only (username,password,registration_ip). The field is write-only in TS (not read in login), so this is a lost audit datum, not a behavioral break. |
| gap-db-datastruct-5 | `modules/friends/migrations/000001_init.up.sql ignorelist.target_username37 INTEGER`; `repository.go` AddIgnore/GetIgnores | `src/db/types.ts ignorelist.value:string`; `FriendServerRepository.ts:277,383` | CONFIRMED-EXCEPTION | **ignorelist stores integer username37 instead of TS's username string column.** | TS persists ignore entries as the decoded username string and round-trips through base37 on read. goscape stores the raw base37 integer and never decodes. The round-trip is lossless for valid names, so reads/writes are equivalent; deliberate federation/schema-decoupling exception documented in friends/db.go. |
| gap-db-datastruct-6 | `pkg/zone/list.go:68-84` All(); `modules/world/tick.go:926-945` processZones snapshot; `loc_tracker.go:50-55` | `src/datastruct/DoublyLinkList.ts:73-87` *all(); `LinkList.ts:97-111` | CONFIRMED-EXCEPTION | **DoublyLinkList iteration not safe under mid-iteration removal (TS cursor save/restore) — Go relies on caller snapshot.** | TS `all()` saves/restores the cursor around the yield so unlinking the just-yielded node mid-iteration is safe. Go's `All()` is a bare next-pointer walk that terminates early if the current node is Unlinked. goscape compensates by requiring callers to snapshot before mutating (processZones does; PlayersSafe/NpcsSafe callers only filter). Behavior preserved via caller discipline, not the structure; comments accurate. |
| gap-db-datastruct-7 | `modules/login/db.go:198-208` insertSessionTx; `handler.go:75` `uuid.NewString()` | `src/server/login/LoginServer.ts:273-283 / 329-340` | DEVIATION | **session row stores server-local `now()` + generated UUID rather than world `nodeTime` + socket id.** | TS keys the session table on `uuid=socket` (world-supplied socket id, also PK) and stamps `timestamp=toDbDate(nodeTime)`. goscape generates its own `uuid.NewString()` and stamps `login_time=time.Now().UTC()`. Decouples session id from socket and uses server time. Audit/transport-level deviation, not a login-result break. |
| gap-db-datastruct-8 | `modules/login/migrations/000001` account_login; `db.go:182-219` upsert/clearWorldSessions | `src/db/types.ts account_login.logged_in:number`; `LoginServer.ts:162-169, 385-401` | DEVIATION | **goscape splits `logged_in` (0/1 flag) + `node_id`; TS overloads `logged_in` to hold the nodeId.** | In TS the single `logged_in` column doubles as the node id (set to nodeId, matched by `where logged_in = nodeId`, tested `!== 0`). goscape splits this into a boolean `logged_in` plus a separate `node_id`, and clearWorldSessions matches `node_id = ?` regardless of the flag and does NOT clear node_id (stale node bookkeeping). Equivalent for single-node-per-account; differs in column semantics. |
| gap-db-datastruct-10 | `modules/login/db.go:39-102` openDB/migrateDB (modernc.org/sqlite); `friends/db.go:37-99` | `src/db/dialect/BunSqliteDriver.ts:63-104` | CONFIRMED-EXCEPTION | **BunSqlite driver retry/locked-DB + silent-empty-result handling not ported (transport differs).** | TS retries up to 3x with sleep(100) on SQLITE_BUSY (errno 5), serializes via a single-connection mutex, and on persistent failure returns an empty result + logs (rather than throwing). goscape uses database/sql + modernc.org/sqlite with WAL, relies on driver busy handling, and returns errors up the stack. Allowed transport exception; the silent-empty-result-on-failure is NOT reproduced (Go surfaces errors), arguably safer. |
| gap-db-datastruct-11 | `modules/login/handler.go:128-134` ban check; `410-416` muted check | `src/server/login/LoginServer.ts:244, :298` | DEVIATION | **banned/muted time parse requires exact layout and fails open on malformed value.** | TS compares `banned_until` via JS Date parsing (any Date-parseable string) and forwards `muted_until` raw. goscape parses TEXT with the fixed layout `2006-01-02 15:04:05` with no else on parse failure — a malformed timestamp is silently treated as not-banned/not-muted. TS `new Date(badstring)` is Invalid Date whose `> now` is false (also fails open), so the open-fail direction matches; the difference is Go requires the exact layout where TS accepts any parseable string. |

---

## Refuted

No finding in this addendum carries `verdict.real === false`. All adversarial verdicts confirmed `real: true`; two HIGH findings were down-corrected to MEDIUM (`gap-configs-snapshot-netbase-1`, `gap-db-datastruct-1`) and are retained in the main ledger above under their corrected severity.

---

## Deferred markers

26 deferred markers were swept across the 10 units. Assessment legend: **CONFIRMED-EXCEPTION** = the deferral/comment is accurate and TS-faithful; **STALE-DEFER** = the comment's TS-parity claim is contradicted by the frozen reference (surfaced as a finding where behavioral).

| unit | go_loc | marker | assessment | ts_status / note |
|---|---|---|---|---|
| gap-client-decoders | `handler_event_tracking.go:52-69` | NAI-Phase2: MouseMoveEvent X/Y stay 0; EVENT_TRACKING payload opaque | CONFIRMED-EXCEPTION | TS EventTrackingHandler treats bytes as opaque (`input.record(bytes)`), never decodes coords server-side. |
| gap-client-decoders | `handler_opheld.go:77` + OpHeldU:349-354, oploc/opnpc/opobj | ObjType/LocType nil → drop (goscape defensive; TS throws) | CONFIRMED-EXCEPTION | TS calls `.debugname` which throws on missing config; goscape converts the throw into a silent drop, reached only on a registry miss TS would crash on. Wire-decode behavior unaffected. |
| gap-client-models | `handler_event_tracking.go:52-59` | NAI-Phase2: EVENT_TRACKING payload opaque; X/Y emitted as 0 | CONFIRMED-EXCEPTION | TS copies len bytes verbatim and never decodes mouse coords; comment's TS-parity claim accurate. |
| gap-client-models | `movement.go:167-172` | NAI-176-D-FLY-NO-CONTENT-WIRES: MoveStrategyFly bypasses collision | CONFIRMED-EXCEPTION | TS `PathingEntity.takeStep:663-665` bypasses collision for `MoveStrategy.FLY`; engine-fidelity-only path present in both. |
| gap-client-models | `handlers_game.go:212-216` | IdleTimer todo: staff command to stay logged in | CONFIRMED-EXCEPTION | TS IdleTimerHandler carries the identical comment and same `if(!NODE_DEBUG) requestIdleLogout=true` body; matches line-for-line. |
| gap-runscript-guard | `script.go:116-117` | "Resumes bypass runScript — they call resumeOrFinish directly, the force=true path" | CONFIRMED-EXCEPTION | VERIFIED TRUE: TS resume paths call `executeScript(...,true,true)` with force=true; goscape sets Execution=Running and calls resumeOrFinish/Execute directly, never re-entering runScript. |
| gap-runscript-guard | `player_script.go:389-390` | "World-shutdown early-return from TS is omitted" | CONFIRMED-EXCEPTION | TS canAccess returns true unconditionally on World.shutdown; goscape has no such flag, making it STRICTER, not a dupe risk. |
| gap-runscript-guard | `handler_opheld.go:77` | "objType == nil // goscape defensive; TS throws here" | CONFIRMED-EXCEPTION | TS OpHeldHandler `ObjType.get(objId)` throws on bad id; goscape returns nil (drop). Defensive, not guard-related. |
| gap-login-wire | `server.go:1002-1004` | `// TODO: save var from msg` / `// TODO: save + reconnecting check` | STALE-DEFER | TS LoginThread/World post-accept loads the save buffer and gates reconnecting on `hasSave`; Go has not wired the save/reconnect-hasSave path into the wire-accept flow yet. |
| gap-login-wire | `packet.go:511` | `// better: take decrypt() from crypto/rsa/rsa.go` | CONFIRMED-EXCEPTION | Cosmetic implementation note; raw modPow path is correct for this protocol. |
| gap-friend-social-bridge | `friends_client.go:192` | `// Slice 1 logs and returns; slice 4 will fan out...` | STALE-DEFER | Body makes the real RPC; slice-4 fan-out implemented server-side. Comment describes obsolete slice-1 stub. See finding gap-friend-social-bridge-1. |
| gap-friend-social-bridge | `bridges.go:84-90` | NAI-S4A/S4B-D-NO-INGAME-(PACKET/PM)-EMIT — RETIRED 2026-05-19 | CONFIRMED-EXCEPTION | Tags explicitly RETIRED; OnFriendlistUpdate/OnIgnorelistUpdate/OnPrivateMessage now emit real packets matching TS onFriendMessage. Verified live. |
| gap-friend-social-bridge | `bridges.go:200-205` | NAI-S5B-D-CLEARLOGOUTS-NO-GOSCAPE-QUEUE — permanent architectural divergence | CONFIRMED-EXCEPTION | goscape has no logout-request queue analogous to TS `logoutRequests`; ClearLogouts is a tagged no-op consistent with the absent-queue architecture. |
| gap-world-reload-events | `reload.go:13-48` (resizeVarShared) + NAI-190-D3 | D3-CANDIDATE-VARSHARED-CLOBBER: TS clobbers copied non-string values; mirrored verbatim | CONFIRMED-EXCEPTION | VERIFIED FAITHFUL: TS loops ALL indices and resets non-string slots to 0/-1 even after the overlap copy, so copied values ARE clobbered; goscape reproduces this exactly. |
| gap-world-reload-events | `reload.go:110-118` NAI-190-D2-HALF-SWAP | post-step-3 mid-pipeline errors leave `s.*` partially mutated (TS-parity) | CONFIRMED-EXCEPTION | VERIFIED: TS reload() has no try/rollback; mutates package singletons in place. goscape committing the step-3 swap before later load errors is consistent with TS's non-transactional posture. |
| gap-world-reload-events | `reload.go:161,116-118` D4-NO-CATEGORYTYPES | goscape has no CategoryType loader; reload omits TS L216 | STALE-DEFER | TS L216 DOES call `CategoryType.load('data/pack')` in reload(); comment's "no goscape analog" is contradicted. Surfaced as finding gap-world-reload-events-8 (MISSING). |
| gap-world-reload-events | `reload.go:110-115` NAI-190-D1-GAMEMAP-RE-INJECT | step 11 re-injects loc/obj types into GameMap via setters | CONFIRMED-EXCEPTION | VERIFIED reasonable: TS reads LocType/ObjType package singletons directly; goscape passes types by reference so it must re-inject via setters. Behaviorally equivalent; SetMembers re-application idempotent. |
| gap-configs-snapshot-netbase | ClientGameProtCategory equiv (no Go port of the comment) | TS todos: "measure how many events..." / "determine which packets belong in which category" | CONFIRMED-EXCEPTION | TS itself carries these as unresolved todos; goscape inherits the same hardcoded 20/5/2 limits and packet→category mapping. Not a goscape regression. |
| gap-configs-snapshot-netbase | `client.go:122` | `// TODO: return error?` on client.write (bufw.Write error discarded) | CONFIRMED-EXCEPTION | TS NetworkPlayer write path likewise does not surface a per-write error to the decode loop; the error is observed later at flush. |
| gap-playerops-seams | `handlers_player.go:1988-1993` (handlePOpHeld) | `return fmt.Errorf("P_OPHELD: unimplemented")` | CONFIRMED-EXCEPTION | TS P_OPHELD body is `throw new Error('unimplemented')`. Both raise at the same point (Go returns error, TS throws). Faithful parity. |
| gap-playerops-seams | `handlers_player.go:2023-2030` (handlePLocMerge area) | `// TODO: check active loc too (carried from TS PlayerOps.ts:921)` | CONFIRMED-EXCEPTION | TS carries the same `// TODO: check active loc too` above P_LOCMERGE; Go preserves it. TS-side deferral faithfully mirrored. |
| gap-db-datastruct | `handler.go:295-304` | L46: TS calls updateHiscores here ... goscape does not port the hiscores subsystem | STALE-DEFER | TS DOES call updateHiscores on player_logout; the defer is real and accurately described, but TS does NOT omit it — genuine MISSING feature (finding gap-db-datastruct-9). |
| gap-db-datastruct | `friends/db.go:21-35` | PORTING.md Arc 18 DB-2 — friendlist/ignorelist store bare username37 ints without FK | CONFIRMED-EXCEPTION | TS stores friendlist as (account_id, friend_account_id) and ignorelist.value as a username string; federated-no-FK design is a deliberate goscape exception. Comment accurate. |
| gap-db-datastruct | `pkg/zone/list.go:9-19, 38-45` | "Mirrors TS DoublyLinkable/DoublyLinkList ... same iteration order, same visible state" | CONFIRMED-EXCEPTION | Iteration order and O(1) cost match, but removal-during-iteration safety is NOT identical (TS cursor save/restore vs Go bare next-pointer walk). "Same visible state" holds only because callers snapshot before mutating (finding gap-db-datastruct-6). Slightly over-broad but true in practice. |
| gap-db-datastruct | `migrations/000002_session_uuid_check.up.sql` | Pre-slice-7 rows hold RemoteAddr().String(); coercing session_uuid to '' loses no audit data | CONFIRMED-EXCEPTION | No TS equivalent (TS uuid=socket as PK). goscape-specific schema-hardening migration; not a TS-parity claim. |
| gap-db-datastruct | `pkg/zone/zone.go:284-290 AddObj` | Unconditional assignment correct today: all respawn-lifecycle callers pass dropperAccountID=0 | CONFIRMED-EXCEPTION | Self-referential to-future guard, not a TS-omission. Out of db/datastruct scope. |

---

## Still-uncovered

Per-unit residue not exercised by these coverage units (carried forward for any future audit pass):

- **gap-client-decoders / gap-client-models** — handler-side GATE logic beyond pure wire-decode (component visibility, invListener resolution, viewport 52-tile checks, members-only gates, trigger-fallback arms, session-logging) was read for context but is the province of the handler audits; the per-opcode OpX interaction side effects (pathToTarget, targetOp encoding APPLAYER3/OPPLAYER3→3, opcalled) and the `unsetMapFlag()` write-payload byte format were not opened; CoordGrid pathfinder route translation (routeToPacked/FindPathPlain) is a pathfinder-unit concern.
- **gap-server-codec-models** — PlayerInfoEncoder/NpcInfoEncoder are pdata pass-throughs; the rsbuf builders that *produce* those bytes were not walked. WordPack.Pack bit-encoding not re-verified at bit level (assumed covered by the wordenc audit). `sendMessagePrivate` dereferences `wordenc` with no nil-guard (friends_emit.go:63) — runtime robustness only. The ObjReveal members/tradeable gate at `(*Server).RevealObj` was not examined.
- **gap-runscript-guard** — the overlapping-script edge (nested protected-A-then-nonprotected-B suspend: exact `this.protect` value parity during nested non-protected suspends) was not fully traced; the dupe path is confirmed closed regardless. handlers_game.go DebugProc (protect=false) and the ProtectedActivePlayer2 secondary-player protect were not audited beyond the primary path.
- **gap-login-wire** — the internal world↔login-server protocol (LoginClient.ts WebSocket JSON / Go loginpb gRPC) is a separate transport, not the client wire. Full Java-client downstream ISAAC keystream consumption beyond seed handoff, the remaining Packet primitives (Gsmart/Pbit/IG2/IP4), the post-accept save-buffer / reconnect-hasSave gating (stubbed in Go, server.go:1002-1004), and the bit-level PacketBit reader/writer were not compared.
- **gap-friend-social-bridge** — RELAY_* admin round-trips (Mute/Kick/Shutdown/Broadcast/Track/Reload/ClearLogins/ClearLogouts/QueueScript) per-field payload parity vs FriendServer.ts:298-396 + WorldEventsDispatcher inbound effects were name-checked present but only spot-checked. The friends MODULE server-side (visibility/getFollowers/cap-100/loadFriends ordering) is the audited counterpart, intentionally out of scope. friendspb generated marshalling and the onReconnect social re-emit path not compared.
- **gap-world-reload-events** — pkg/cache MakeCRCs/PreloadClient internals vs TS file-list parity (a candidate cache-layer audit), friendspb WorldEvent proto field widths vs TS relay payload encoding, whether the NPC/script runtime ever consults CategoryType (impact sizing for finding-8), and rebuildManualInvoker races beyond confirming `rebuildMu` is held on both sides.
- **gap-configs-snapshot-netbase** — per-opcode mapping of which specific USER_EVENT handlers most often hit `success=false` in TS (OpHeld/IfButton sampled; full blast radius not enumerated), the encodeOut/writeInner server-message encode path (separate unit), and BANDWIDTH_IN stat parity (goscape appears not to track WorldStat.BANDWIDTH_IN at all).
- **gap-playerops-seams** — queue fixed-arg variants (ScriptProvider.get-missing path), FINDUID/P_FINDUID pointer math, FINDHERO/BOTH_HEROPOINTS hash64 ledger, HUNTALL/HUNTNEXT iterator, P_EXACTMOVE/P_LOCMERGE coord plumbing, and P_ARRIVEDELAY/P_DELAY suspend-tick math were confirmed registered at matching opcode numbers but their entity-layer side effects were not re-derived line-by-line (covered by the main audit).
- **gap-db-datastruct** — friends/handler.go broadcast/world-placement parity vs TS getFriends world-tuple bundling, obj_delayed_queue.go/npc.go list usage for mid-iteration mutation (processZones snapshot spot-checked only), the bucket iteration-order impact of finding-4 on specific order-dependent mechanics, TS `account_login.logged_out` node-id consumers (Go drops the column), and a **broader schema gap**: TS `session_log`, `session_wealth`, `report`, `input_report` tables appear absent in goscape (only private_chat/public_chat ported in friends) — beyond the 11 reported db findings.

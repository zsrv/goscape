# NAI-142 — `rebuildZones` cached-client coverage smoke (post-bundle)

**Date:** 2026-05-09
**Spec:** `docs/superpowers/specs/2026-05-09-nai-142-rebuildzones-cached-client-coverage-design.md` @ `06b60e8`
**Plan:** `docs/superpowers/plans/2026-05-09-nai-142-rebuildzones-cached-client-coverage.md` @ `88db12f`
**Implementation commits:**
- `2abc07b` — `feat(player): NAI-142 — retire rebuildScenery activeZones dual-write + port per-zone-change rebuildZones`
- `09e78cd` — `docs(player): NAI-142 review fixups — processOut comment + T5 line-number breadcrumbs`

**Reviewers (Sonnet):**
- Spec compliance — ✅ compliant; cited every spec §2.2 / §3 / §7 item against `git show 2abc07b`.
- Code quality — Approved-with-fixes; surfaced two doc-only issues (stale `processOut` comment + stale T5 line-number breadcrumbs); both addressed in `09e78cd`.

**Pre-state:** All 6 new T1–T6 tests green at HEAD `09e78cd`; full-repo `go test ./...` and `go vet ./...` clean.

**Cascade origin:** No user-reported symptom — bundle is parity-driven cleanup. Pre-bundle behavior was benign-broader (rebuildScenery's 13×13 level-0 superset masked the missing per-zone-change call site), never benign-narrower; cached clients DID receive zone deltas, just for a 169-entry over-set instead of the canonical 49-entry 7×7.

**Theory of fix:** Two-part bundle. R-D2 (drop hardcoded level-0 dual-write inside `rebuildScenery`) + R-D3 (port TS `NetworkPlayer.ts:269-271` lastZone-transition slice via `Player.lastZone int = -1` + `(*Player).updateBuildArea()` inserted at top of `processOut`, mirroring TS `World.ts:1097` ordering). Bundle is mandatory: R-D2 alone breaks cached-client delta delivery; R-D3 alone is redundant until R-D2 retires.

---

## §1 PRIMARY criteria (per spec §1.1)

User-driven smoke at HEAD `09e78cd`:

1. Login at any populated tutorial/varrock area.
2. Walk continuously through ≥3 zone boundaries.
3. Tele across the 13×13 build-area window (e.g. `::tele` to a far coord).
4. NAI-141 regression-fence: open Lumbridge kitchen door, descend trapdoor, ascend.

### Strict pin

| # | Criterion | Pre-bundle observation | Post-bundle expectation |
|---|-----------|------------------------|--------------------------|
| 1 | **Login + walk through 2-3 zones streams Loc deltas correctly.** Observed scenery (locs with shared per-tick events: opened doors, NPCs interacting with locs, objs from `obj_add`) renders without duplication, dropout, or stale-state artifacts in destination zones. | Worked (benign-broader 13×13 set was a superset, never narrower). | Still works; `activeZones` is now the canonical level-aware 7×7 set per zone transition. |
| 2 | **REBUILD_GETMAPS round-trip still works.** Tele across the 13×13 build-area window. | Worked. | Still works; `handleRebuildGetMaps` still calls `rebuildZones` on the REBUILD path. The new `updateBuildArea` may fire idempotently on the same tick (matches TS; see spec §2.5). |
| 3 | **No regression in NAI-141 §1 criteria 1-2** (kitchen-door plane round-trip). The bundle does not touch `writeFullFollows`; smoke re-witnesses the green NAI-141 path as a regression-fence. | NAI-141 PASS at `45c2b0a`. | Still PASS. |

### Non-criterion (informational only)

The cached-client cross-zone path (in-window tele or zone-crossing walk without a forced REBUILD_GETMAPS) is the bundle-value path. Pre-bundle, `activeZones` stayed at the post-rebuildScenery 13×13 superset until the next forced REBUILD; post-bundle, `updateBuildArea` fires `rebuildZones` on every transition, producing the canonical 7×7 set centered on the new zone. T6 unit-pins this; the smoke walking-through-zones criterion exercises it.

## §2 Smoke procedure

1. **Server start.** Per `smoke_test_server_handoff.md`, server is user-launched (sandbox cannot reach the host's Java client):
   ```bash
   CGO_ENABLED=0 GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go run -trimpath ./cmd/goscape --config.file config.yaml
   ```
2. **Java client connect.** Standard `Client-Java` build pointing at the local server.
3. **Login at populated tutorial/varrock area.**
4. **Walk continuously through ≥3 zone boundaries.** Compare against §1 pin 1.
5. **`::tele` to a far coord.** Compare against §1 pin 2.
6. **Open Lumbridge kitchen door → trapdoor down → trapdoor up.** Compare against §1 pin 3.
7. **Server log check.** No new warnings/panics during the round-trip.

## §3 Adjacent consumers (not blocked-on at close)

Per spec §2.6, the following TS lines in `NetworkPlayer.updateMap` are NOT ported in this bundle and are tracked as standalone follow-ups:

- `NAI-142-D-R-D1` — Camera packet processing (`NetworkPlayer.ts:243-253`).
- `NAI-142-D-R-D2` — `lastMapZone` tracking + `triggerMapzone`/`triggerMapzoneExit` (`NetworkPlayer.ts:256-266`).
- `NAI-142-D-R-D3` — `triggerZone`/`triggerZoneExit` + `SetMultiway` write (`NetworkPlayer.ts:274-285`).
- `NAI-142-D-R-D4` — Cosmetic rename of goscape's `updateMap` → `rebuildNormal` to free the `updateMap` name for a future full TS-shape port.

Future smoke surfacing a divergence at these surfaces routes as a separate sub-spec, not a NAI-142 reopener (per `smoke_surfaces_adjacent_divergences.md`).

## §4 Reporting

After running the smoke, report back:

- **Criterion 1 (3-zone walk, scenery rendering):** PASS / FAIL — describe any duplication, dropout, or stale-state.
- **Criterion 2 (cross-window tele, REBUILD_GETMAPS):** PASS / FAIL — describe any client-side AIOOBE or missing locs/objs.
- **Criterion 3 (NAI-141 regression fence):** PASS / FAIL — describe door state after trapdoor round-trip.
- **Server log:** clean / note any new warnings.
- **Adjacent observations:** any unexpected zone-state anomaly elsewhere (informational).

## §5 Closure conditions (per spec §6)

NAI-142 closes when:

- All 6 new tests + full repo green at HEAD `09e78cd` ✓ (verified pre-handoff).
- Reviewers (Sonnet) verdict: spec-compliant ✓ + code-quality approved-with-fixes ✓ (fixes in `09e78cd`).
- Smoke §1 criteria 1, 2, 3 all PASS.

On smoke PASS:
- Close commit: `chore(close): NAI-142 — retire R-D2 + R-D3; bundle smoke green` with `Closes memory:` trailer per `close_commit_memory_trailer.md`.
- `nai_followups.md` updated: retire `NAI-84-D-R-D2`/`NAI-84-D-R-D3`, add `NAI-142-D-R-D{1,2,3,4}`, update NAI-141 cross-reference.

On smoke FAIL of criterion 1 (zone-cross scenery dropout/duplication):
- Most likely cause: `updateBuildArea` not firing in the right tick slice. Re-trace `processOut` ordering and confirm `updateBuildArea` runs before `updateZones`. Fall back to spec §2.4 call-order trace.

On smoke FAIL of criterion 2 (REBUILD round-trip broken):
- Most likely cause: `handleRebuildGetMaps` and `updateBuildArea` both firing `rebuildZones` on the same tick caused unexpected ordering. Re-read spec §2.5; both callers expect idempotence (rebuildZones resets activeZones at top).

On smoke FAIL of criterion 3 (NAI-141 regression):
- Bundle should not touch `writeFullFollows`. If door-state regresses, suspect interaction between the new `updateBuildArea`-driven zone-refresh cadence and `writeFullFollows` consumers.

---

## §6 Smoke result

**2026-05-09 — User-confirmed PASS.** All three pin criteria green; server log clean.

NAI-142 closes here.

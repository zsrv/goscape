# NAI-140 — Quest Journal color smoke (post-T2 fix)

**Date:** 2026-05-09
**Spec:** `docs/superpowers/specs/2026-05-09-nai-140-rgb24to15-quest-color-design.md` @ `8793b9e`
**Implementation commits:**
- `0fab4ce` — `test(colorconv): NAI-140 T1 red — rgb24to15 + writer goldens fail`
- `2cfe15e` — `feat(colorconv): NAI-140 T2 green — port ColorConversion + apply rgb24to15 in IfSetColour`
**Reviewer:** `superpowers:code-reviewer` (Sonnet) — APPROVED, no Critical/Important issues.
**Pre-state:** All 9 unit/integration tests green at HEAD `2cfe15e`; `go test ./...` and `go vet ./...` clean.
**Cascade origin:** NAI-139 SECONDARY S1 (`docs/superpowers/handoffs/2026-05-09-nai-139-stage-1-smoke.md` §2 S1).
**Theory of fix:** Single writer-level conversion (`rgb24→rgb15` inside `Player.IfSetColour`) is exhaustive across all `if_setcolour` consumers — quest tabs, scroll boxes, quest-complete UI, etc.

---

## §1 PRIMARY criteria (per spec §1)

User-driven re-run of NAI-139 fresh-tutorial smoke at HEAD `2cfe15e`:

1. Fresh tutorial-stage account → complete Tutorial Island → Magic Instructor "Yes, go to mainland" → arrive Lumbridge spawn (3222, 3222, level 0).
2. Open Quest Journal tab.

### Strict pins

| # | Criterion | Pre-fix observation (NAI-139 S1) | Post-fix expectation |
|---|-----------|----------------------------------|----------------------|
| 1 | **Not-started quests render red** (`^red_rgb=0xFF0000` → rgb15 `0x7C00`). Sample fresh-tutorial quests: `runemysteries`, `doric`, `cook`. | **Black** (writer emitted low 16 bits of `0xFF0000` = `0x0000`) | **Red** (writer emits `0x7C00`) |
| 2 | **In-progress quests render yellow** (`^yellow_rgb=0xFFFF00` → rgb15 `0x7FE0`). Fresh-tutorial accounts have none in this state by default. | **Orange** (writer emitted `0xFF00`) | **Yellow** (writer emits `0x7FE0`) — verify by stat/cheat-driven mid-progress varp set, OR defer to first organic encounter |
| 3 | **No regression in NAI-139 §1 criteria 0-6.** Tutorial-completion cascade still PASS; tabs still clickable + populated. | All 6 PASS | All 6 still PASS |

### Non-criterion (informational only, per `cascade_theory_smoke_binding.md`)

Completed-quest **green** branch (`^green_rgb=0x00FF00` → rgb15 `0x03E0`) is not smoke-bound on fresh-tutorial path — no completed quests reachable. Production correctness for green follows from theory-of-fix (single writer-level conversion) and is unit-pinned at `pkg/colorconv/colorconv_test.go::TestRgb24to15_Goldens` (case `0x00FF00 → 0x03E0`).

## §2 Smoke procedure

1. **Server start.** Sandbox cannot reach the user's host; **server must be user-launched** per `smoke_test_server_handoff.md`. From repo root:
   ```bash
   CGO_ENABLED=0 GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go run -trimpath ./cmd/goscape --config.file config.yaml
   ```
2. **Java client connect.** Use the standard `Client-Java` build pointing at the local server.
3. **Fresh tutorial account.** Use NodeDebug/cheat reset path or fresh login per the NAI-139 procedure.
4. **Cascade.** Complete Tutorial Island → Magic Instructor → arrive Lumbridge.
5. **Open Quest Journal tab.**
6. **Observe colors.** Compare against §1 strict pins.
7. **Regression sweep.** Confirm NAI-139 §1 criteria 0–6 still PASS (inventory, worn, bank, stats, tabs, weapon-category).
8. **Server log check.** No `script not protected`, `proc not found`, opcode-dispatch warnings, panics, or stack traces during cascade.

## §3 Adjacent consumers (not blocked-on at close)

Per spec §4, theory-of-fix (single writer-level conversion) covers all `if_setcolour` callers. Adjacent consumers not in the smoke path:

- **Scroll boxes** (`general/scripts/scroll.rs2`).
- **Quest-complete UI** (`send_quest_complete`).

Future smoke surfacing a divergence at these surfaces routes as a separate sub-spec, not a NAI-140 reopener.

## §4 Reporting

After running the smoke, report back:

- **Criterion 1 (red not-started):** PASS / FAIL — name a specific quest observed (e.g. "runemysteries renders red").
- **Criterion 2 (yellow in-progress):** PASS / FAIL / DEFERRED (no in-progress quest reachable).
- **Criterion 3 (regression sweep):** PASS / FAIL — note any NAI-139 §1 regression.
- **Server log:** clean / note any new warnings.
- **Adjacent observations:** any unexpected color anomaly elsewhere (informational).

## §5 Closure conditions (per spec §6)

NAI-140 closes when:

- All 9 unit/integration tests green at HEAD `2cfe15e` ✓ (verified pre-handoff).
- Reviewer (Sonnet) verdict: no Critical/Important issues ✓ (verified pre-handoff).
- Smoke §1 criterion 1 PASS.
- Smoke §1 criterion 3 PASS (no NAI-139 regression).
- Smoke §1 criterion 2: best-effort (PASS or DEFERRED both acceptable per spec §4 — unit golden T1.1 case `0xFFFF00 → 0x7FE0` independently pins the yellow branch).

On smoke PASS:
- Close commit: `chore(close): NAI-140 — quest-color cascade closed; rgb24to15 in writer` with `Closes memory:` trailer per `close_commit_memory_trailer.md`.
- Memory entry: `colorconv_rgb24to15_in_writer.md` (project memory) pinning the architectural split.
- `nai_followups.md` updated: NAI-140 close section + `NAI-140-D-REVERSEHSL-DEAD-API` deviation tracker (retire on next sub-spec close that touches `pkg/colorconv` if no consumer materializes).

On smoke FAIL of criterion 1:
- Re-open as NAI-140 Stage 2 (likely an under-diagnosed cascade-blocker per `smoke_unchanged_means_multiple_blockers.md`).
- Theory-of-fix premise (single writer suffices) becomes the binding assumption to investigate — possibly an additional consumer path (e.g. `IF_SETMODEL_COLOUR` or a separate color-attribute writer) needs the same conversion.

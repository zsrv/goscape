# NAI-141 — `writeFullFollows` respawn-replay smoke (post-T2 fix)

**Date:** 2026-05-09
**Spec:** `docs/superpowers/specs/2026-05-09-nai-141-writeFullFollows-respawn-replay-design.md` @ `6495589`
**Implementation commits:**
- `f6555ad` — `test(player_zone): NAI-141 T1 red — writeFullFollows respawn-replay branches fail`
- `c2def54` — `feat(player_zone): NAI-141 T2 green — port writeFullFollows respawn-loc replay branches`
**Reviewer:** `superpowers:code-reviewer` (Sonnet) — APPROVED, no Critical/Important issues.
**Pre-state:** All 8 new T1.0–T1.7 tests green at HEAD `c2def54`; full-repo `go test ./...` and `go vet ./...` clean.
**Cascade origin:** User-reported bug 2026-05-09 — Lumbridge kitchen door duplicate-state after plane round-trip.
**Theory of fix:** Three-arm `switch` mirroring TS `Engine-TS/src/engine/zone/Zone.ts:133-165` is exhaustive across all `change_loc`/`loc_del`-mutated statics replayed via `writeFullFollows`. Closes the documented TS-asymmetry (stale `TODO(beyond-4b)` at `modules/world/player_zone.go:23-24` retired).

---

## §1 PRIMARY criteria (per spec §1)

User-driven smoke at HEAD `c2def54`:

1. Spawn or walk to Lumbridge Castle ground floor (level 0, ~3209,3216).
2. Open the kitchen door (the door dividing the kitchen from the great-hall — `~open_and_close_door` proc fires `change_loc` on the static and `loc_add` for the dynamic open-variant).
3. Walk to the trapdoor inside the kitchen; click "Climb-down" → land on level -1.
4. Walk to the trapdoor in the basement; click "Climb-up" → return to level 0.

### Strict pin

| # | Criterion | Pre-fix observation | Post-fix expectation |
|---|-----------|---------------------|----------------------|
| 1 | **Kitchen door at the original coord renders in exactly one state** after the trapdoor round-trip — open OR closed, whichever the server holds. | Both visible: dynamic Despawn `opened_door` replayed via existing branch + cache-default `closed_door` rendered at the static coord because the Respawn+`isChanged` branch was missing. | Exactly one variant visible. (If the server-state is "open", the new Respawn+`isChanged` branch emits `LOC_ADD_CHANGE` with the mutated `inviswall` type, suppressing the cache-default render at that tile; only the dynamic open-variant remains visible.) |
| 2 | **No regression in NAI-140 §1 criteria 0-3** (quest-color cascade) and NAI-139 §1 criteria 0-6 (tutorial-completion cascade). | All PASS at HEAD `fcec331` (NAI-140 close). | All still PASS. |
| 3 | **No script-execute errors, opcode-dispatch warnings, panics, or stack traces** during the round-trip. | Clean. | Clean. |

### Non-criterion (informational only)

The dynamic open-variant `LOC_ADD` (Despawn-lifecycle, added by `~open_and_close_door`'s `loc_add`) replays correctly today via the existing Despawn+IsActive branch. Smoke pin (1) verifies the closed-variant side; the open-variant side is already-green production behavior re-witnessed.

The Respawn+`!IsActive` (LOC_DEL) branch is unit-pinned only — no fresh-tutorial path mutates a static via `loc_del` and exercises the full-replay surface. Adjacent smoke binding deferred per `cascade_theory_smoke_binding.md`.

## §2 Smoke procedure

1. **Server start.** Per `smoke_test_server_handoff.md`, server is user-launched (sandbox cannot reach the host's Java client):
   ```bash
   CGO_ENABLED=0 GOPATH=$TMPDIR/go GOCACHE=$TMPDIR/go-cache go run -trimpath ./cmd/goscape --config.file config.yaml
   ```
2. **Java client connect.** Standard `Client-Java` build pointing at the local server.
3. **Walk or teleport to Lumbridge Castle ground floor.**
4. **Open the kitchen door.**
5. **Climb down trapdoor → climb back up.**
6. **Observe door state.** Compare against §1 pin 1.
7. **Regression sweep.** Confirm NAI-140 §1 + NAI-139 §1 still PASS.
8. **Server log check.** No new warnings/panics during the round-trip.

## §3 Adjacent consumers (not blocked-on at close)

Per spec §4, theory-of-fix (three-arm TS-mirror) covers all `change_loc` / `loc_del`-mutated statics replayed via `writeFullFollows`. Adjacent consumers not in the smoke path:

- Any door across any region after a plane round-trip (via trapdoor, ladder, stairs).
- Statics removed via `loc_del` script (covered by T1.1 unit only).
- Statics permanently changed (no auto-revert duration) and persisted across plane changes.

Future smoke surfacing a divergence at these surfaces routes as a separate sub-spec, not a NAI-141 reopener (per `smoke_surfaces_adjacent_divergences.md`).

## §4 Reporting

After running the smoke, report back:

- **Criterion 1 (single-state door):** PASS / FAIL — describe what was visible at the door coord after climb-up.
- **Criterion 2 (regression):** PASS / FAIL — note any NAI-140 / NAI-139 regression observed.
- **Criterion 3 (server log):** clean / note any new warnings.
- **Adjacent observations:** any unexpected loc-state anomaly elsewhere (informational).

## §5 Closure conditions (per spec §6)

NAI-141 closes when:

- All 8 new tests + full repo green at HEAD `c2def54` ✓ (verified pre-handoff).
- Reviewer (Sonnet) verdict: no Critical/Important issues ✓ (verified pre-handoff).
- Smoke §1 criterion 1 PASS.
- Smoke §1 criterion 2 PASS (no NAI-140 / NAI-139 regression).
- Smoke §1 criterion 3 PASS (clean server log).

On smoke PASS:
- Close commit: `chore(close): NAI-141 — writeFullFollows respawn-replay; door-state survives plane round-trip` with `Closes memory:` trailer per `close_commit_memory_trailer.md`.
- Memory entry: `writeFullFollows_three_branch_parity.md` (project memory) pinning the three-branch invariant + the loc-`IsActive`-vs-obj-`CheckLifecycle` distinction.
- `nai_followups.md` updated: NAI-141 close section + retire stale `TODO(beyond-4b)` tracker entry at `modules/world/player_zone.go:23-24`.

On smoke FAIL of criterion 1:
- Investigate per spec §2.6 R4 — adjacent NAI-84-D-R-D2 (rebuildScenery activeZones dual-write) or NAI-84-D-R-D3 (per-zone-change rebuildZones call site) may govern *when* `writeFullFollows` fires; if door-state diverges on plain-walking zone transitions vs trapdoor specifically, escalate as separate sub-spec on NAI-84.

---

## §6 Smoke result

**2026-05-09 — User-confirmed PASS.** Door rendered in exactly one state after the trapdoor round-trip. NAI-140 / NAI-139 regression-clean. Server log clean.

NAI-141 closes here.

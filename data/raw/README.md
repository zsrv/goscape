This directory contains engine-owned raw binary blobs sourced from Engine-TS@9aadcec4 (`data/raw/`). The `wordenc` file is the pre-built word-encoding Jagfile consumed by the wordenc pack stage (`pkg/pack/wordenc/pack.go`) per `tools/pack/chat/pack.ts:packClientWordenc` at revision 244, which replaced the four-txt builder with a direct `cache.write(0, 7, fs.readFileSync('data/raw/wordenc'))` pass-through. The runtime filter loader (`pkg/wordenc/encfilter`) also reads this file at startup from the hardcoded path `data/raw/wordenc`, mirroring `Engine-TS/src/cache/wordenc/WordEnc.ts:35-37`.

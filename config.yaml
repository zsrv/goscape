target: all
log_level: debug
log_format: text

asset:
  enable: true
  http_listen_network: tcp
  http_listen_address: 127.0.0.1
  http_listen_port: 8888

world:
  enable: true
  tcp_keepalive_period: 30s
